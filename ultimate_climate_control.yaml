blueprint:
  name: Ultimate Smart Climate Control - v2.17.6

  description: |
    **Ultimate Smart Climate Control v2.17.6**

    🌡️ **INTELLIGENT ADAPTIVE CLIMATE CONTROL**

    Advanced power-efficient climate automation with **adaptive fan control**, **proximity-based pre-conditioning** and **intelligent presence detection**. Control multiple A/C units with full customization of temperatures, distances, and operating modes.

    **🎯 NEW: ENHANCED ADAPTIVE FAN CONTROL:**

    • **Progressive Escalation:** Temperature tiers now adapt fan speeds based on actual cooling/heating effectiveness
    • **All 5 Fan Levels:** Intelligent use of Level 1→2→3→4→5 based on performance, not just temperature
    • **Smart Tier Logic:** LOW starts gentle (Level 1), escalates within tier; MEDIUM starts moderate (Level 3), escalates to max; HIGH uses maximum immediately
    • **Both Heating & Cooling:** Same intelligent escalation applies to all temperature scenarios
    • **Performance-Based:** Fan speed increases only when actually needed based on real-world effectiveness

    **POWER SAVING FEATURES:**

    • **Energy Optimization:** Configured to turn OFF in comfort zone (saves 40-60% energy)
    • **Smart Tolerance:** Wider temperature tolerance (±2°C) with extended cycling protection
    • **Adaptive Efficiency:** Progressive fan ramping prevents over-powered responses, maximizing efficiency

    **UNIVERSAL COMPATIBILITY:**

    **Auto-detects and adapts** to different A/C models:

    • **HVAC modes:** auto ↔ heat_cool, supports all Daikin variants
    • **Fan speeds:** Level 5 ↔ 5, Auto ↔ auto, Quiet ↔ Silence
    • **Swing modes:** Both ↔ both, Vertical ↔ vertical, 3D ↔ both

    **SMART MODE PRESENCE:**

    **Room-specific climate control** based on occupancy:

    • **Room occupied:** Normal temperature control
    • **Room empty (timeout):** Configurable eco/off/maintain behavior
    • **Away from house:** Global away mode (separate setting)

    **VERSION UPDATES:**

    **v2.17.6 Temperature History Tracking Fix:**
    • **🚨 CRITICAL FIX** - Fixed temperature history helper updating every minute instead of only when modes change
    • **📊 PROPER BASELINE** - Temperature history now captures initial temp when mode starts for accurate progress tracking
    • **🎯 EFFECTIVENESS CALCULATION** - Fixed effectiveness always showing 0% due to previous_temp matching current_temp
    • **✅ ACCURATE METRICS** - Progress calculation now works correctly with proper initial vs current temperature comparison

    **v2.17.5 Effectiveness Helper Update Fix:**
    • **🚨 CRITICAL FIX** - Fixed effectiveness percentage showing 0% despite correct calculations and working escalation
    • **💾 HELPER PERSISTENCE** - Added missing service call to save calculated effectiveness score to helper entity
    • **📊 VISUAL FEEDBACK** - Users now see actual effectiveness percentages in logs and debug output
    • **✅ COMPLETE SYSTEM** - Dynamic escalation now fully functional with proper effectiveness tracking and display

    **v2.17.2 Adaptive Timer Reset Fix:**
    • **🚨 CRITICAL FIX** - Fixed adaptive timer resetting to 0.0min every minute, preventing 5+ minute escalation
    • **⏰ CONTINUE MODE PROTECTION** - Helper timestamps only update when starting new modes, not when continuing existing ones
    • **🎯 VERIFIED SOLUTION** - Fixed 6 temperature mode conditions (heating_high/medium/low, cooling_high/medium/low)
    • **📊 SMART HYSTERESIS** - Preserves mode transition timing for proper escalation and effectiveness calculations
    • **✅ ADAPTIVE CONTROL ENABLED** - Now allows adaptive control to reach 5+ minutes and function properly

    **v2.17.1 Additional Timer Reset Fixes:**
    • **🚨 CRITICAL FIX** - Resolved remaining timer resets that were still causing cooling_medium mode to reset every few minutes
    • **📊 COMPLETE COVERAGE** - Fixed adaptive_off, smart_*, and multiple cooling mode timer reset locations
    • **⏰ SYSTEMATIC APPROACH** - Applied replace_all to ensure all cooling mode timer conditions include mode change detection
    • **🎯 VERIFIED PATTERN** - Targeted exact timer reset locations causing "running 0 min" resets observed in logs
    • **🔧 14+ LOCATIONS FIXED** - Comprehensive mode change detection applied across all major timer reset points

    **v2.17.0 Dynamic Escalation Timer Fix:**

    **v2.16.3 Manual Override Detection Fix:**
    • **🎯 FIXED STABILITY AUTO-OFF** - No longer interrupts continue mode when AC is pursuing target temperature
    • **📍 TARGET DISTANCE CHECK** - Stability detection now requires being within 0.5°C of actual target, not just comfort zone
    • **🔄 CONTINUE MODE PROTECTION** - Prevents premature auto-off when in cooling_low/medium/high or heating modes
    • **🌡️ ENHANCED DEBUG** - Shows "Continue mode active - pursuing target" status during target pursuit
    • **⚡ PREVENTS SHORT CYCLING** - AC now continues from trigger (24.1°C) to actual target (22°C) without interruption

    **v2.14.3 Fan Speed Inheritance Fix:**
    • **🎮 MANUAL MODE OPTIONS** - Choose between Pure Manual, Safety Timeout, Presence Aware, or Extreme Protection behaviors
    • **⏰ SAFETY TIMEOUT** - Optional timeout to prevent AC running forever in Manual mode (default 8 hours)
    • **🚪 PRESENCE AWARE** - Optional mode where Manual respects presence but ignores temperature
    • **🛡️ EXTREME PROTECTION** - Manual mode that only intervenes for extreme temperatures
    • **🎯 AUTO-DETECTION** - Automatically switches to Manual when user changes AC (from HA or remote)
    • **⏱️ SEPARATE TIMEOUTS** - Different timeouts for manual override vs manual mode safety
    • **✅ BACKWARD COMPATIBLE** - Default "Safety Timeout" mode prevents energy waste while respecting control

    
    📖 **For complete changelog and version history, visit:**
    🔗 https://github.com/Chris971991/Smart-Climate-Control

    **CONTROL MODES:**

    • **AUTO MODE:**
      Full automation with occupancy detection and presence-based control.
      System automatically adjusts temperature when you're home or away.

    • **MANUAL MODE:**
      Full manual control with no automation.
      You control temperature, fan speed, and on/off manually.

    • **SMART MODE:**
      Aggressive presence detection and advanced occupancy sensing.
      Uses motion sensors, door sensors, phone presence, and predictive algorithms
      to optimize temperature before occupancy and maintain comfort during use.
      
      **NEW:** Temperature Stability Auto-Off - automatically turns off AC when
      temperature equilibrium is reached (configurable tolerance & duration).

    **KEY FEATURES:**

    • **Smart Mode Control** - Auto/Manual/Smart modes with presence timeout
    • **Room-Specific Presence** - BLE/PIR sensors for accurate occupancy
    • **Proximity Zones** - Different behaviors based on distance from home
    • **Eco Mode** - Reduces energy use by 30-40% vs on/off cycling
    • **Smart Pre-conditioning** - Activates when approaching home
    • **Multi-Zone Support** - Control 1 or many A/C units
    • **Gradual Adjustments** - Prevents aggressive cycling
    • **Runtime Protection** - Extends equipment life

    **REQUIRED SETUP** - Create unique helpers for EACH room/automation:

    **1. Input Text Helper** (unique per room):
    • Living Room: `input_text.climate_last_mode_living_room`
    • Bedroom: `input_text.climate_last_mode_bedroom`
    • Office: `input_text.climate_last_mode_office`
    • Icon: `mdi:air-conditioner`

    **2. Input DateTime Helper** (unique per room):
    • Living Room: `input_datetime.climate_last_change_living_room`
    • Bedroom: `input_datetime.climate_last_change_bedroom`
    • Office: `input_datetime.climate_last_change_office`
    • Has date: **Yes** ✅
    • Has time: **Yes** ✅
    • Icon: `mdi:clock-outline`

    **OPTIONAL: SMART MODE HELPERS** (for advanced control):
    • `input_select.climate_control_mode_[room]` (Auto/Manual/Smart modes)
    • `input_datetime.presence_last_detected_[room]` (presence timeout tracking)
    • `input_boolean.climate_proximity_override_[room]` (emergency override)

    **OPTIONAL: DYNAMIC ADAPTATION HELPERS** (for smart escalation):
    • `input_number.climate_temp_history_[room]` (temperature tracking)
    • `input_text.climate_trend_direction_[room]` (rising/falling/stable)
    • `input_datetime.climate_mode_start_time_[room]` (when current mode started)
    • `input_number.climate_effectiveness_score_[room]` (0-100, how well it's working)

    **OPTIONAL: STABILITY & HYSTERESIS HELPERS** (for v2.1+ features):
    • `input_datetime.temp_stable_since_[room]` (tracks when temp became stable)
    • `input_text.climate_last_transition_[room]` (for hysteresis tracking)
    
    
    📍 Create via: Settings → Devices & Services → Helpers → Create Helper
    
    
    **3. PROXIMITY SENSORS - Set up via Integration:**
    
    1. Go to Settings → Devices & Services → Integrations
    2. Click "+ ADD INTEGRATION" 
    3. Search for and select "Proximity"
    4. Configure:
       • Name: "Home"
       • Zone: Select your "Home" zone
       • Devices: Select your person entities
       • Tolerance: 50m (prevents GPS jitter updates)
    
    This creates:
    • sensor.home_nearest_distance (distance in meters)
    • sensor.home_nearest_direction_of_travel (towards/away/stationary/arrived)
    
    
    **OPTIONAL: Energy Tracking Helpers:**
    
    • `input_number.climate_power_saving_score` (efficiency %)
    • `sensor.climate_runtime_today` (daily runtime tracking)
    • `sensor.climate_efficiency_rating` (efficiency score)
  domain: automation
  source_url: https://github.com/yourusername/ultimate-climate-control
  input:
    # Basic required settings (always visible)
    room_name:
      name: Room Name *
      description: |
        **Enter the name of this room** for logging and BLE detection.
        
        **Used for:**
        • **Debug logging:** Prefixes all log entries with room name
        • **BLE triangulation:** Must match sensor output exactly for presence detection
        • **Multi-room setup:** Helps distinguish between different AC automations
        
        **Examples:** "Office", "Living Room", "Master Bedroom"
        
        **Important:** If using BLE sensors, check sensor state in Developer Tools for exact string
      selector:
        text:
    
    climate_entities:
      name: Climate Entities *
      description: |
        Select one or more **climate entities** to control with this automation.

        **Supported devices:** All types of A/C units, heat pumps, and climate control devices.
      selector:
        entity:
          multiple: true
          domain: climate
    
    helper_last_mode:
      name: Last Mode Helper Entity *
      description: |
        Input Text helper to track the **last active climate mode**.

        **Create a unique helper for each room/automation instance:**

        • `input_text.climate_last_mode_living_room`

        • `input_text.climate_last_mode_bedroom`

        • `input_text.climate_last_mode_office`

        **Important:** This helper stores state information for proper automation behavior.
      selector:
        entity:
          domain: input_text
    
    helper_last_change:
      name: Last Change Helper Entity *
      description: |
        Input DateTime helper to track **when climate changes occurred**.

        **Create a unique helper for each room/automation instance:**

        • `input_datetime.climate_last_change_living_room`

        • `input_datetime.climate_last_change_bedroom`

        • `input_datetime.climate_last_change_office`

        **Used for:** Timing calculations and manual override protection.
      selector:
        entity:
          domain: input_datetime

    # Temperature Settings - Simple Mode
    temperature_settings:
      name: "Temperature Settings"
      icon: mdi:thermometer
      collapsed: false
      input:
        temperature_sensor:
          name: Temperature Sensor (Optional)
          description: |
            External temperature sensor for **more accurate room temperature readings**.

            If not configured, the system will use your A/C unit's built-in sensor.

            **Best practices for external sensors:**

            • Place at seated height (1-1.5m from floor)
            • Position away from direct sunlight and heat sources
            • Central location in the room for representative readings
            • Avoid drafty areas or corners

            **Benefit:** External sensors provide significantly better temperature control accuracy.
          default: []
          selector:
            entity:
              domain: sensor
              device_class: temperature
        
        use_average_temperature:
          name: Use Average Temperature
          description: |
            Controls how temperature is calculated when using **multiple A/C units**.

            • **ON:** Average temperatures from all A/C units
            • **OFF:** Use temperature from the first A/C unit only

            **Important:** Turn OFF when using a single external sensor for all units to avoid conflicts.
          default: true
          selector:
            boolean:
        
        # SIMPLE CONTROLS (Always visible)
        target_temperature:
          name: 🎯 Target Temperature
          description: |
            Your **ideal room temperature** in degrees Celsius.

            This is the **main temperature control** that affects all other zones proportionally.

            Adjusting this value will shift all temperature thresholds up or down together.

            **Auto mode target:** This is where the system will maintain temperature.
          default: 23
          selector:
            number:
              min: 18
              max: 28
              step: 0.5
              unit_of_measurement: "°C"
              mode: slider
        
        comfort_zone_width:
          name: 📏 Comfort Zone Range
          description: |
            Temperature variation tolerance around target **(±°C)**:

            • **Wider** = more tolerance, less switching, more efficient
            • **Narrower** = tighter control, more activity, less efficient

            **Example:** Target 23°C with ±2°C = comfort zone 21-25°C (saves 40-60% power)
          default: 2
          selector:
            number:
              min: 0.5
              max: 3
              step: 0.5
              unit_of_measurement: "°C"
              mode: slider
        
        enable_heating_mode:
          name: 🔥 Enable Heating Mode
          description: |
            **Control whether heating activates** when temperature drops below comfort zone.
            
            • **ON:** Heating will activate when room gets cold (full climate control)
            • **OFF:** Heating disabled - cooling only (perfect for people who run hot)
            
            **Use cases:** Seasonal control, energy management, personal preference, cooling-only setups.
          default: true
          selector:
            boolean:
        
        enable_cooling_mode:
          name: ❄️ Enable Cooling Mode
          description: |
            **Control whether cooling activates** when temperature rises above comfort zone.
            
            • **ON:** Cooling will activate when room gets warm (full climate control)
            • **OFF:** Cooling disabled - heating only (perfect for cold climates)
            
            **Use cases:** Seasonal control, energy management, heating-only setups, winter operation.
          default: true
          selector:
            boolean:
        
        temperature_aggressiveness:
          name: ⚡ Response Aggressiveness
          description: |
            How quickly system responds **beyond comfort zone**:

            • **1 = Gentle** (extreme modes at ±3°C from comfort, slow, very efficient)
            • **2 = Smooth** (medium at comfort edge, high at +1°C, max at +2°C)
            • **3 = Balanced** (extreme modes at ±2°C from comfort, good balance)
            • **5 = Aggressive** (extreme modes at ±1°C from comfort, fast, less efficient)

            **Example** (Target 23°C, Zone ±2°C, Aggressiveness 2):

            • **Comfort:** 21-25°C (OFF mode for power savings)
            • **Medium:** 25°C & 21°C (gentle response at comfort edge)
            • **High:** 26°C & 20°C (strong response at ±1°C from edge)
          default: 2
          selector:
            number:
              min: 1
              max: 5
              step: 1
              mode: slider
        
        # ADVANCED OVERRIDES (Hidden by default)
        enable_advanced_temp:
          name: 🔧 Enable Advanced Temperature Controls
          description: |
            Manually override **individual temperature thresholds**.

            • For users who want **precise control** over all thresholds
            • Overrides automatic calculations based on target temperature
            • Allows fine-tuning of comfort zones and trigger points

            **Recommendation:** Most users should leave this disabled and use the base target temperature.
          default: false
          selector:
            boolean:
        
        # These are only shown when advanced mode is enabled
        comfort_min_temp:
          name: Comfort Zone Minimum (Advanced)
          description: |
            Override auto-calculated **minimum comfort temperature**.

            • **Bottom of comfort zone** where heating may start
            • Should be lower than comfort maximum
            • Typically 1-2°C below target temperature

            **Behavior:** When room is above this, system stays in eco/off mode.
          default: 21
          selector:
            number:
              min: 18
              max: 25
              step: 0.5
              unit_of_measurement: "°C"
        
        comfort_max_temp:
          name: Comfort Zone Maximum (Advanced)
          description: |
            Override auto-calculated **maximum comfort temperature**.

            • **Top of comfort zone** where cooling may start
            • Should be higher than comfort minimum
            • Typically 1-2°C above target temperature

            **Behavior:** When room is below this, system stays in eco/off mode.
          default: 23
          selector:
            number:
              min: 20
              max: 28
              step: 0.5
              unit_of_measurement: "°C"
        
        cooling_target_temp:
          name: Cooling Target (Advanced)
          description: |
            Override **cooling target temperature**.

            • **What temperature to cool the room to**
            • Used when room exceeds comfort maximum
            • Usually same as or slightly below base target

            **Behavior:** System will cool to this temperature then switch to eco/off.
          default: 22
          selector:
            number:
              min: 18
              max: 28
              step: 0.5
              unit_of_measurement: "°C"
        
        heating_target_temp:
          name: Heating Target (Advanced)
          description: |
            Override **heating target temperature**.

            • **What temperature to heat the room to**
            • Used when room falls below comfort minimum
            • Usually same as or slightly above base target

            **Behavior:** System will heat to this temperature then switch to eco/off.
          default: 23
          selector:
            number:
              min: 18
              max: 28
              step: 0.5
              unit_of_measurement: "°C"
        
        precool_target_temp:
          name: Pre-cooling Target (Advanced)
          description: |
            Override **pre-conditioning target temperature**.

            • **Temperature to achieve before you arrive home**
            • Used with proximity sensors and approaching detection
            • Should be comfortable for immediate occupancy

            **Recommendation:** Usually matches your preferred target temperature.
          default: 22
          selector:
            number:
              min: 18
              max: 28
              step: 0.5
              unit_of_measurement: "°C"
        
        cooling_low_temp:
          name: Low Cooling Threshold (Advanced)
          description: |
            Override **low cooling trigger temperature**.

            • **Temperature that triggers gentle cooling mode**
            • When room just exceeds comfort zone, uses minimum fan speed
            • Should be just above comfort maximum (e.g., 24.1°C)

            **Purpose:** Gentle cooling when slightly warm.
          default: 24.1
          selector:
            number:
              min: 20
              max: 35
              step: 0.1
              unit_of_measurement: "°C"
        
        cooling_medium_temp:
          name: Medium Cooling Threshold (Advanced)
          description: |
            Override **medium cooling trigger temperature**.

            • **Temperature that triggers moderate cooling mode**
            • When room is moderately warm, uses medium fan speed
            • Should be 1°C above comfort maximum (e.g., 25°C)

            **Purpose:** Balanced cooling for moderately warm conditions.
          default: 25
          selector:
            number:
              min: 20
              max: 35
              step: 0.5
              unit_of_measurement: "°C"
        
        cooling_high_temp:
          name: High Cooling Threshold (Advanced)
          description: |
            Override **high cooling trigger temperature**.

            • **Temperature that triggers maximum cooling mode**
            • When room is very hot, uses maximum fan speed
            • Should be 2°C above comfort maximum (e.g., 26°C)

            **Purpose:** Emergency cooling when room gets very hot.
          default: 26
          selector:
            number:
              min: 20
              max: 35
              step: 0.5
              unit_of_measurement: "°C"
        
        heating_low_temp:
          name: Low Heating Threshold (Advanced)
          description: |
            Override **low heating trigger temperature**.

            • **Temperature that triggers gentle heating mode**
            • When room just falls below comfort zone, uses minimum fan speed
            • Should be just below comfort minimum (e.g., 19.9°C)

            **Purpose:** Gentle heating when slightly cool.
          default: 19.9
          selector:
            number:
              min: 10
              max: 25
              step: 0.1
              unit_of_measurement: "°C"
        
        heating_medium_temp:
          name: Medium Heating Threshold (Advanced)
          description: |
            Override **medium heating trigger temperature**.

            • **Temperature that triggers moderate heating mode**
            • When room is moderately cool, uses medium fan speed
            • Should be 1°C below comfort minimum (e.g., 19°C)

            **Purpose:** Balanced heating for moderately cool conditions.
          default: 19
          selector:
            number:
              min: 10
              max: 25
              step: 0.5
              unit_of_measurement: "°C"
        
        heating_high_temp:
          name: High Heating Threshold (Advanced)
          description: |
            Override **high heating trigger temperature**.

            • **Temperature that triggers maximum heating mode**
            • When room is very cold, uses maximum fan speed
            • Should be 2°C below comfort minimum (e.g., 18°C)

            **Purpose:** Emergency heating when room gets very cold.
          default: 18
          selector:
            number:
              min: 10
              max: 25
              step: 0.5
              unit_of_measurement: "°C"

    # Presence & Proximity Settings
    presence_settings:
      name: "Presence & Proximity"
      icon: mdi:account-multiple
      collapsed: true
      input:
        presence_persons:
          name: People to Track
          description: |
            Select people for **presence detection**.

            • **Used for away mode detection**
            • Triggers pre-conditioning when approaching home
            • Works with proximity/direction sensors for advanced features
          default: []
          selector:
            entity:
              multiple: true
              domain: person
        
        presence_devices:
          name: Presence Detection Devices
          description: |
            Additional devices indicating presence when **person entities aren't sufficient**.

            **Examples:**

            • PC power state sensors
            • Smart TV power sensors
            • Gaming console entities
            • Workstation activity sensors

            **Purpose:** These supplement person presence detection for more accurate home/away detection.
          default: []
          selector:
            entity:
              multiple: true
        
        proximity_sensor:
          name: Proximity Distance Sensor
          description: |
            Sensor tracking **nearest person's distance from home**.

            • **Used for proximity-based pre-conditioning**
            • Works with direction sensor for smart approach detection
            • **Example:** `sensor.home_nearest_distance`

            **Requirement:** Home Assistant proximity integration setup.
          default: sensor.home_nearest_distance
          selector:
            entity:
              domain: sensor
        
        direction_sensor:
          name: Direction of Travel Sensor
          description: |
            Sensor tracking **direction of travel** relative to home.

            **States:**

            • **towards:** Approaching home
            • **away:** Moving away from home
            • **arrived:** At home location

            **Example:** `sensor.home_nearest_direction_of_travel`
          default: sensor.home_nearest_direction_of_travel
          selector:
            entity:
              domain: sensor
        
        home_zone_distance:
          name: Home Zone Distance
          description: |
            Distance defining your **"home zone" boundary**. Controls all proximity behaviors:

            • **Within this distance:** Maintain comfort temperature, pre-condition when approaching
            • **Beyond this distance:** Switch to away mode (off or eco mode)
          default: 5000
          selector:
            number:
              min: 1000
              max: 10000
              step: 500
              unit_of_measurement: m

    # Away Mode Settings
    away_settings:
      name: "Away Mode Settings"
      icon: mdi:home-export-outline
      collapsed: true
      input:
        enable_away_mode:
          name: Enable Away Mode
          description: |
            **Turn off or set to eco** when everyone leaves.

            Automatically manages climate control based on home occupancy for **energy savings**.
          default: true
          selector:
            boolean:
        
        away_mode_action:
          name: Away Mode Action
          description: |
            What to do when **everyone leaves home**:

            • **OFF:** Turn AC completely off (saves most energy, slow to cool when returning)
            • **ECO:** Reduce to eco mode with wider temperature range (balanced energy savings)
            • **MAINTAIN:** Keep current temperature (wastes energy but fastest comfort when returning)
          default: "eco"
          selector:
            select:
              options:
                - "off"
                - "eco"
                - "maintain"
        
        enable_pre_conditioning:
          name: Enable Pre-conditioning
          description: |
            **Pre-cool/heat when approaching home**.

            Uses proximity sensors to start conditioning before arrival for **immediate comfort**.
          default: true
          selector:
            boolean:

    # Smart Mode Settings
    smart_mode_settings:
      name: "Smart Mode Settings"
      icon: mdi:brain
      collapsed: true
      input:
        helper_control_mode:
          name: Control Mode Helper Entity
          description: |
            Input Select helper for **control mode selection**.

            **Required options:**

            • **Auto:** Temperature-based control
            • **Manual:** User-controlled operation
            • **Smart:** Advanced presence-aware control

            **Example:** `input_select.climate_control_mode_living_room`
          selector:
            entity:
              domain: input_select
        
        helper_presence_detected:
          name: Last Presence Helper Entity
          description: |
            **Input DateTime helper** to track when presence was last detected.

            • **Used for** presence timeout calculations
            • **Updated automatically** by the system
            • **Required** for Smart Mode functionality

            **Example:** `input_datetime.presence_last_detected_living_room`
          selector:
            entity:
              domain: input_datetime
        
        helper_proximity_override:
          name: Proximity Override Helper Entity
          description: |
            **Input Boolean helper** for emergency temperature override.

            • **Bypasses all automation** when enabled
            • **Allows manual control** during emergencies
            • **Auto-resets** after specified duration

            **Example:** `input_boolean.climate_proximity_override_living_room`
          selector:
            entity:
              domain: input_boolean
        
        presence_timeout_minutes:
          name: Presence Timeout
          description: |
            **Minutes to wait** after leaving room before taking action.

            • **Prevents shutdown** during brief departures
            • **Common scenarios:** bathroom breaks, quick errands
            • **Recommended:** 15-30 minutes for comfort balance

            **Trade-off:** Lower values save more energy but may cause comfort issues.
          default: 15
          selector:
            number:
              min: 5
              max: 60
              step: 5
              unit_of_measurement: minutes
        
        room_presence_sensors:
          name: Room Presence Sensors (Optional)
          description: |
            **BLE, PIR or other sensors** for room-specific presence detection.

            **Supported sensor types:**

            • **BLE area sensors:** Reports room names (e.g., "Office", "Living Room")
            • **PIR sensors:** Binary sensors reporting on/off
            • **mmWave sensors:** Human presence detection
            • **Smart device sensors:** Phone/device presence

            **Example:** `sensor.phone_ble_area`, `binary_sensor.living_room_motion`
          default: []
          selector:
            entity:
              multiple: true
              domain: 
                - binary_sensor
                - sensor
        
        presence_validation_mode:
          name: Presence Validation Mode
          description: |
            **How multiple presence sensors work together** to detect room occupancy:

            • **ANY (Default):** Trigger if ANY sensor detects presence (most responsive)
            • **ALL:** Require ALL sensors to detect presence (most accurate, may miss presence)
            • **SMART:** Require BLE + Motion when both exist, otherwise ANY (balanced)
            • **MAJORITY:** Require majority of sensors to agree (3+ sensors recommended)
            • **BLE_PLUS:** Require BLE + at least one other sensor (prevents false BLE triggers)

            **Recommendations:**
            • **Single sensor:** Use ANY mode
            • **BLE + PIR:** Use SMART or BLE_PLUS mode to prevent kitchen false triggers
            • **Multiple PIR:** Use MAJORITY mode for best accuracy
            • **High accuracy needed:** Use ALL mode but may miss brief presence

            **Note:** SMART mode intelligently validates BLE with motion sensors when available.
          default: "any"
          selector:
            select:
              options:
                - label: "ANY - Any sensor triggers presence"
                  value: "any"
                - label: "ALL - All sensors must agree"
                  value: "all"
                - label: "SMART - BLE + Motion validation"
                  value: "smart"
                - label: "MAJORITY - Most sensors must agree"
                  value: "majority"
                - label: "BLE_PLUS - BLE + one other sensor"
                  value: "ble_plus"
        
        # Room name is now configured at the top as a required field
        
        adjacent_room_names:
          name: Adjacent Room Names (Open-Plan Spaces)
          description: |
            **For open-plan spaces**, specify adjacent room names that should also trigger this AC.
            
            **Use case:** Your Living Room AC covers an open Kitchen/Living/Dining area, but BLE 
            sensors report specific room names. This allows the AC to activate when you're detected
            in ANY of these connected spaces.
            
            **⚠️ CRITICAL: Check Your BLE Sensor Format First!**
            Before configuring, check **Developer Tools → States** to see exactly how your BLE sensor reports room names:
            
            • **If sensor shows:** `living room` → **Enter:** `Living Room`

            • **If sensor shows:** `living_room` (with underscores) → **Enter:** `living_room`

            • **If sensor shows:** `livingroom` → **Enter:** `livingroom`
            
            **Format must match exactly** (case-insensitive but spaces/underscores matter).
            
            **Examples based on your BLE sensor format:**
            • **Spaces format:** Kitchen, Dining Room, Living Room
            • **Underscore format:** kitchen, dining_room, living_room
            • **No spaces format:** kitchen, diningroom, livingroom
            
            **Example Configuration (assuming spaces format):**
            • **Room Name:** Living Room (main room from top of config)
            • **Adjacent Rooms:** Kitchen, Dining Room, Family Room
            • **Result:** AC activates when BLE detects Living Room, Kitchen, Dining Room, OR Family Room
            
            **⚠️ IMPORTANT for SMART/BLE_PLUS modes:**
            When using validation modes that require motion + BLE, you MUST also add the motion 
            sensors from adjacent rooms to the "Room Presence Sensors" list above.
            
            **Example for SMART mode (spaces format):**
            • **Adjacent Room Names:** Kitchen, Family Room  (match BLE sensor format)
            • **Room Presence Sensors must include:**
              - `sensor.phone_ble_area` (BLE sensor)

              - `binary_sensor.living_room_motion` (main room)

              - `binary_sensor.kitchen_motion` (adjacent room motion)
              
              - `binary_sensor.family_room_motion` (adjacent room motion)
            
            **Leave empty if not using open-plan space detection.**
          default: ""
          selector:
            text:
        
        smart_mode_behavior:
          name: Smart Mode Room Absence Behavior
          description: |
            What to do when **room presence sensors detect absence** (after timeout):

            • **ECO:** Reduce to eco mode (saves power, maintains some comfort)
            • **MAINTAIN:** Keep current temperature (comfort over efficiency)
            • **OFF:** Turn off completely (maximum power savings)

            **Note:** Only applies when still home but room is empty.
          default: "eco"
          selector:
            select:
              options:
                - "eco"
                - "maintain"
                - "off"

        temp_stability_enabled:
          name: Temperature Stability Auto-Off
          description: |
            **Automatically turn off AC** when temperature remains stable for specified duration.

            **How it works:** Detects when AC job is complete (thermal equilibrium reached) to save energy.

            **Note:** Works even when people are still in room. **Requires** temperature history helper.
          default: false
          selector:
            boolean:

        stability_tolerance:
          name: Stability Temperature Tolerance
          description: |
            Temperature range considered **"stable"** for auto-off detection.

            **Sensitivity levels:**

            • **±0.3°C:** High sensitivity (recommended for most users)
            • **±0.5°C:** Moderate sensitivity (balanced approach)
            • **±1.0°C:** Low sensitivity (less aggressive)

            **Rule:** Smaller values trigger auto-off sooner, larger values wait longer.
          default: 0.3
          selector:
            number:
              min: 0.1
              max: 5.0
              step: 0.1
              unit_of_measurement: "°C"

        stability_duration:
          name: Stability Duration
          description: |
            **Minutes** temperature must remain stable before auto-off triggers.

            **Timing strategies:**

            • **10-15 minutes:** Aggressive energy savings
            • **20-25 minutes:** Balanced efficiency and comfort
            • **25-30 minutes:** Conservative, prioritizes comfort

            **Trade-off:** Shorter duration saves more energy but may cause temperature fluctuations.
          default: 15
          selector:
            number:
              min: 10
              max: 30
              step: 5
              unit_of_measurement: "min"

        stability_behavior:
          name: Stability Auto-Off Action  
          description: |
            **Triggered when temperature becomes STABLE** (±1°C for 15+ minutes):
            
            **Purpose:** Recognizes AC achieved its goal and temperature isn't changing
            
            • **OFF:** Turn off completely (maximum energy savings)
            • **ECO:** Switch to eco mode (moderate energy savings, maintains some comfort)
            
            **Different from Eco Mode:** This responds to temperature **stability**, not comfort zone
          default: "off"
          selector:
            select:
              options:
                - "off"
                - "eco"

    # Scheduling Settings (Optional)
    scheduling_settings:
      name: "Time-Based Scheduling"
      icon: mdi:calendar-clock
      collapsed: true
      input:
        enable_scheduling:
          name: Enable Time-Based Scheduling
          description: |
            **Automatically adjust temperatures** based on time of day.

            **Benefits:**

            • **Optimized comfort** for different activities
            • **Energy savings** during sleeping hours
            • Cooler mornings, warmer evenings as needed
            • Separate weekend schedule option

            **Note:** Overrides base target temperature when enabled.
          default: false
          selector:
            boolean:
        
        morning_temp:
          name: Morning Temperature (6am-9am)
          description: |
            **Temperature target** for morning hours **(6am-9am)**.

            • **Typically cooler** for waking up comfort
            • Good for productivity during morning routines
            • Consider personal preferences and climate
          default: 22
          selector:
            number:
              min: 18
              max: 28
              step: 0.5
              unit_of_measurement: "°C"
        
        day_temp:
          name: Day Temperature (9am-6pm)
          description: |
            **Temperature target** for daytime hours **(9am-6pm)**.

            • **Optimize** for work/activity comfort
            • Balance productivity and energy efficiency
            • Consider sunlight and heat gain during day
          default: 23
          selector:
            number:
              min: 18
              max: 28
              step: 0.5
              unit_of_measurement: "°C"
        
        evening_temp:
          name: Evening Temperature (6pm-10pm)
          description: |
            **Temperature target** for evening hours **(6pm-10pm)**.

            • **Relaxation** and family time comfort
            • May prefer slightly warmer for leisure
            • Accounts for reduced outdoor temperatures
          default: 23
          selector:
            number:
              min: 18
              max: 28
              step: 0.5
              unit_of_measurement: "°C"
        
        night_temp:
          name: Night Temperature (10pm-6am)
          description: |
            **Temperature target** for sleeping hours **(10pm-6am)**.

            • **Sleep research** recommends 18-21°C for best rest
            • **Cooler temperatures** promote deeper sleep
            • **Energy savings** during extended periods
          default: 24
          selector:
            number:
              min: 18
              max: 28
              step: 0.5
              unit_of_measurement: "°C"
        
        enable_weekend_schedule:
          name: Different Weekend Schedule
          description: |
            Use **different temperature schedule** on weekends.

            • **Later wake times** with extended morning periods
            • Different activity patterns and comfort needs
            • More flexible scheduling for leisure days

            **Note:** Weekend morning period: 6am-10am (vs weekday 6am-9am)
          default: false
          selector:
            boolean:
        
        weekend_morning_temp:
          name: Weekend Morning Temperature
          description: |
            **Weekend morning temperature** **(6am-10am)**.

            • **Extended morning period** for leisurely weekends
            • May prefer warmer for relaxed mornings
            • Adjust based on weekend routines and preferences
          default: 23
          selector:
            number:
              min: 18
              max: 28
              step: 0.5
              unit_of_measurement: "°C"
        
        weekend_day_temp:
          name: Weekend Day Temperature
          description: |
            **Weekend daytime temperature** **(10am-10pm)**.

            • **Extended day period** for weekend activities
            • Covers both day and evening in one setting
            • Simplifies weekend scheduling

            **Note:** Single temperature for entire active weekend period.
          default: 23
          selector:
            number:
              min: 18
              max: 28
              step: 0.5
              unit_of_measurement: "°C"
        
        weekend_night_temp:
          name: Weekend Night Temperature
          description: |
            **Weekend night temperature** **(10pm-6am)**.

            • **Same sleep optimization** as weeknight temperature
            • May differ if weekend bedtime routines vary
            • Maintains consistent sleep comfort

            Consider if weekend sleep patterns require different settings.
          default: 24
          selector:
            number:
              min: 18
              max: 28
              step: 0.5
              unit_of_measurement: "°C"

    # Window Detection Settings (Optional)
    window_detection_settings:
      name: "Window Detection"
      icon: mdi:window-open
      collapsed: true
      input:
        enable_window_detection:
          name: Enable Window Detection
          description: |
            **Automatically turn off AC** when windows or doors are opened.

            **Benefits:**

            • **Prevents** cooling/heating the outdoors
            • **Significant energy savings**
            • Protects AC equipment from overwork
            • **Automatic resumption** when closed

            **Requirement:** Binary sensors on doors/windows.
          default: false
          selector:
            boolean:
        
        window_sensors:
          name: Window/Door Sensors
          description: |
            Select sensors that should **turn off AC** when opened.

            **Compatible sensors:**

            • **Door contact sensors**
            • **Window opening sensors**
            • **Magnetic reed switches**
            • **Smart window/door sensors**

            **Behavior:** All selected sensors will trigger AC shutdown when opened.
          default: []
          selector:
            entity:
              multiple: true
              domain: binary_sensor
        
        window_open_delay:
          name: Window Open Delay
          description: |
            **Minutes to wait** after window opens before turning off AC.

            • **Prevents false triggers** from brief openings
            • **0 minutes:** Immediate shutdown (most responsive)
            • **1-2 minutes:** Balanced (recommended)
            • **3+ minutes:** Delayed response

            **Result:** Brief openings won't unnecessarily stop your AC.
          default: 2
          selector:
            number:
              min: 0
              max: 10
              step: 1
              unit_of_measurement: "minutes"
        
        window_close_delay:
          name: Window Close Delay
          description: |
            **Minutes to wait** after window closes before resuming AC.

            • **Ensures window** is properly closed before restarting
            • Allows time for air exchange to stabilize
            • **Prevents rapid on/off cycling**

            **Recommendation:** Short delay to confirm sensor stability.
          default: 1
          selector:
            number:
              min: 0
              max: 10
              step: 1
              unit_of_measurement: "minutes"

    # Weather Compensation Settings (Optional)
    weather_compensation_settings:
      name: "Weather Compensation"
      icon: mdi:weather-partly-cloudy
      collapsed: true
      input:
        enable_weather_compensation:
          name: Enable Weather Compensation
          description: |
            **Adjust indoor target temperature** based on outdoor conditions.

            **How it works:**

            • **Hot outdoor weather:** Slightly raise AC target (less aggressive cooling)
            • **Mild outdoor weather:** Use normal targets
            • **Cool outdoor weather:** Lower targets for efficiency

            **Result:** Maintains comfort while optimizing energy usage based on natural conditions.
          default: false
          selector:
            boolean:
        
        weather_entity:
          name: Weather Entity
          description: |
            **Weather integration** for outdoor temperature data.

            • **Provides base** outdoor temperature readings
            • Usually `weather.home` or `weather.forecast_home`
            • **Fallback** if no dedicated outdoor sensor available

            **Recommendation:** Most users should start with their default weather integration.
          default: weather.home
          selector:
            entity:
              domain: weather
        
        outdoor_temp_sensor:
          name: Outdoor Temperature Sensor (Optional)
          description: |
            **External temperature sensor** for more accurate readings.

            • **More precise** than weather service data
            • **Real-time local conditions** vs forecast data
            • **Optional:** Falls back to weather entity if not set

            **Recommended** for users with outdoor temperature sensors.
          default: []
          selector:
            entity:
              domain: sensor
              device_class: temperature
        
        compensation_factor:
          name: Compensation Factor
          description: |
            **How much outdoor temperature affects indoor target** (0-1).

            **Factor levels:**

            • **0.0:** No compensation (disabled)
            • **0.1-0.2:** Mild adjustment (recommended)
            • **0.3-0.5:** Moderate adjustment
            • **0.6-1.0:** Aggressive adjustment

            **Example:** 0.2 factor with 30°C outside adds ~1°C to cooling target.
          default: 0.2
          selector:
            number:
              min: 0
              max: 1
              step: 0.1
        
        max_compensation:
          name: Maximum Compensation
          description: |
            **Maximum degrees** to adjust target temperature.

            • **Safety limit** to prevent extreme adjustments
            • **1-2°C:** Conservative, maintains close to base target
            • **3-5°C:** Aggressive, allows significant weather adaptation

            **Prevents** compensation from making indoor temperatures uncomfortable.
          default: 2
          selector:
            number:
              min: 1
              max: 5
              step: 0.5
              unit_of_measurement: "°C"
        
        compensation_base_temp:
          name: Compensation Base Temperature
          description: |
            **Outdoor temperature** considered "neutral" (no compensation).

            • **Temperature where no adjustment** is needed
            • **25°C:** Good baseline for most climates
            • **Above baseline:** Targets increase (less aggressive cooling)
            • **Below baseline:** Targets decrease (more efficient operation)

            **Recommendation:** Adjust based on your local climate and comfort preferences.
          default: 25
          selector:
            number:
              min: 20
              max: 30
              step: 1
              unit_of_measurement: "°C"

    # Fan & Swing Settings
    fan_settings:
      name: "Fan & Swing Settings"
      icon: mdi:fan
      collapsed: true
      input:
        fan_speed_max:
          name: Maximum Fan Speed
          description: |
            **Fan speed** for maximum cooling/heating conditions.

            • **Auto:** Let AC system manage fan speed
            • **Level 5/5:** Maximum airflow for rapid temperature changes
            • System auto-detects your AC's fan speed format

            **Used when:** Temperature is far from target or during initial conditioning.
          default: "Level 5"
          selector:
            select:
              options:
                - "Auto"
                - "auto"
                - "Quiet"
                - "Silence"
                - "Level 1"
                - "Level 2"
                - "Level 3"
                - "Level 4"
                - "Level 5"
                - "1"
                - "2"
                - "3"
                - "4"
                - "5"
                - "high"
                - "medium"
                - "low"
        
        fan_speed_medium:
          name: Medium Fan Speed
          description: |
            **Fan speed** for medium cooling/heating conditions.

            • **Balanced airflow** for moderate temperature adjustments
            • Good for maintaining temperatures within comfort zone
            • System auto-detects your AC's fan speed format

            **Used when:** Temperature is moderately outside target range.
          default: "Level 3"
          selector:
            select:
              options:
                - "Auto"
                - "auto"
                - "Quiet"
                - "Silence"
                - "Level 1"
                - "Level 2"
                - "Level 3"
                - "Level 4"
                - "Level 5"
                - "1"
                - "2"
                - "3"
                - "4"
                - "5"
                - "high"
                - "medium"
                - "low"
        
        fan_speed_eco:
          name: Eco Fan Speed
          description: |
            **Fan speed** for eco mode and comfort zone operation.

            • **Quiet, energy-efficient operation**
            • Used when temperature is within comfort zone
            • **Minimal noise** for continuous operation
            • System auto-detects your AC's fan speed format

            **Purpose:** Balances comfort with energy efficiency and noise levels.
          default: "Level 1"
          selector:
            select:
              options:
                - "Auto"
                - "auto"
                - "Quiet"
                - "Silence"
                - "Level 1"
                - "Level 2"
                - "Level 3"
                - "Level 4"
                - "Level 5"
                - "1"
                - "2"
                - "3"
                - "4"
                - "5"
                - "high"
                - "medium"
                - "low"
        
        swing_mode_active:
          name: Active Swing Mode
          description: |
            **Swing mode** when actively cooling/heating.

            • **both:** Full directional coverage (recommended)
            • **horizontal:** Left-right movement only
            • **vertical:** Up-down movement only
            • **off:** Fixed direction

            **Benefit:** Better air circulation helps achieve target temperatures faster.
          default: "both"
          selector:
            select:
              options:
                - "off"
                - "both"
                - "vertical"
                - "horizontal"

    # Power Efficiency Settings
    efficiency_settings:
      name: "Power Efficiency Settings"
      icon: mdi:leaf
      collapsed: true
      input:
        enable_eco_mode:
          name: Enable Eco Mode
          description: |
            **Triggered when temperature is in COMFORT ZONE** (21-23°C range):
            
            **Purpose:** Maintain temperature efficiently when already comfortable
            
            • **ON** = Eco mode (low power, maintains temp, uses ~150-250W)
            • **OFF** = Turn off completely (zero power, allows drift, saves more energy)
            
            **Different from Stability:** This responds to being in **comfort zone**, not temperature stability
            
            **Recommended:** Use Stability: OFF + Eco Mode: ON for maximum efficiency
          default: false
          selector:
            boolean:
        
        eco_mode_setpoint_offset:
          name: Eco Mode Setpoint Offset
          description: |
            **Degrees to adjust setpoint** in eco mode to reduce cycling.

            • **0°C:** Standard eco mode (maintains exact target)
            • **1°C:** Relaxed eco mode (recommended)
            • **2-3°C:** Very relaxed (maximum efficiency)

            **Rule:** Higher values reduce compressor cycling and save more energy.
          default: 1
          selector:
            number:
              min: 0
              max: 3
              step: 0.5
              unit_of_measurement: "°C"
        
        min_runtime_minutes:
          name: Minimum Runtime
          description: |
            **Minimum minutes** A/C must run before allowing mode change.

            • **Prevents rapid on/off cycling** (protects compressor)
            • **5-10 minutes:** Aggressive efficiency
            • **15-20 minutes:** Balanced approach (recommended)
            • **25-30 minutes:** Conservative, equipment protection

            **Benefit:** Extends compressor lifespan and improves efficiency.
          default: 15
          selector:
            number:
              min: 5
              max: 30
              unit_of_measurement: minutes
        
        min_off_time_minutes:
          name: Minimum Off Time
          description: |
            **Minimum minutes** A/C must be off before restarting.

            • **Allows compressor pressure** to equalize
            • **3-5 minutes:** Minimum for compressor safety
            • **10 minutes:** Recommended balance
            • **15 minutes:** Conservative protection

            **Critical** for compressor longevity and energy efficiency.
          default: 10
          selector:
            number:
              min: 3
              max: 15
              unit_of_measurement: minutes
        
        enable_gradual_adjustment:
          name: Enable Gradual Temperature Adjustment
          description: |
            **Gradually adjust temperature** instead of immediate changes.

            • **Smoother operation** with less temperature overshoot
            • **More comfortable** temperature transitions
            • **Better energy efficiency** through controlled adjustment
            • Reduces wear on AC equipment

            **Recommendation:** For most users for optimal comfort and efficiency.
          default: true
          selector:
            boolean:
        
        extreme_temp_override:
          name: Extreme Temperature Override
          description: |
            **Temperature thresholds** that override Manual mode for safety.

            **Emergency override:** Above high or below low will activate climate even in Manual mode.
          default: true
          selector:
            boolean:
        
        extreme_high_temp:
          name: Extreme High Temperature
          description: |
            **Safety threshold** - activate cooling even in Manual mode.

            • **Emergency override** for extremely hot conditions
            • **Protects health** and equipment from overheating
            • Only activates when indoor temperature exceeds threshold
            • **Returns to Manual mode** once temperature normalizes

            **Recommendation:** Set based on your comfort limits and local climate conditions.
          default: 30
          selector:
            number:
              min: 28
              max: 35
              step: 0.5
              unit_of_measurement: "°C"
        
        extreme_low_temp:
          name: Extreme Low Temperature
          description: |
            **Safety threshold** - activate heating even in Manual mode.

            • **Emergency override** for extremely cold conditions
            • **Prevents pipes freezing** and health risks
            • Only activates when indoor temperature falls below threshold
            • **Returns to Manual mode** once temperature normalizes

            **Recommendation:** Set based on your heating needs and local climate conditions.
          default: 15
          selector:
            number:
              min: 10
              max: 18
              step: 0.5
              unit_of_measurement: "°C"

    # Advanced Settings
    advanced_settings:
      name: "Advanced Settings"
      icon: mdi:cog
      collapsed: true
      input:
        enable_dynamic_adaptation:
          name: Enable Dynamic Adaptation
          description: |
            **Automatically escalate/de-escalate** cooling/heating power based on effectiveness.

            **How it works:**

            • **Monitors temperature progress** toward target
            • **Increases power** when progress stalls
            • **Reduces power** when target is approaching
            • **Learns** your AC's effectiveness over time

            **Requirement:** Helper entities for temperature tracking and analysis.
          default: true
          selector:
            boolean:
        
        effectiveness_check_minutes:
          name: Effectiveness Check Interval
          description: |
            **Minutes to wait** before checking if current mode is working.

            • **5-10 minutes:** Responsive adaptation
            • **15 minutes:** Balanced approach (recommended)
            • **20-30 minutes:** Conservative, gives more time for AC to work

            **Trade-off:** Too short may cause excessive mode switching; too long delays optimization.
          default: 10
          selector:
            number:
              min: 5
              max: 30
              unit_of_measurement: minutes
        
        escalation_temp_tolerance:
          name: Temperature Stall Tolerance
          description: |
            **Degrees of acceptable temperature drift** before escalating power.

            **Tolerance levels:**

            • **0.5-1.0°C:** Aggressive response (tight control)
            • **1.0-2.0°C:** Balanced approach (recommended)
            • **2.0-3.0°C:** Patient response (allows more drift)

            **Trade-off:** Lower values provide tighter control but may increase cycling.
          default: 1.0
          selector:
            number:
              min: 0.5
              max: 3.0
              step: 0.5
              unit_of_measurement: "°C"
        
        deescalation_approach_threshold:
          name: De-escalation Approach Threshold
          description: |
            **Degrees from target** when system can de-escalate power.

            • **Prevents overshooting** target temperature
            • **1.0-2.0°C:** Aggressive de-escalation
            • **2.0-3.0°C:** Balanced approach (recommended)
            • **3.0-4.0°C:** Conservative, maintains power longer

            **Note:** Works when temperature is trending correctly toward target.
          default: 2.0
          selector:
            number:
              min: 1.0
              max: 4.0
              step: 0.5
              unit_of_measurement: "°C"
        
        helper_temp_history:
          name: Temperature History Helper (Optional)
          description: |
            **Input Number helper** to store previous temperature reading.

            • **Tracks temperature changes** over time
            • **Required** for dynamic adaptation features
            • **Automatically updated** by the system
            • **Range:** 0-50°C with 0.1°C steps

            **Example:** `input_number.climate_temp_history_living_room`
          default: []
          selector:
            entity:
              domain: input_number
        
        helper_trend_direction:
          name: Trend Direction Helper (Optional)
          description: |
            **Input Text helper** to track temperature trend direction.

            • **Stores:** rising, falling, or stable
            • **Required** for dynamic adaptation logic
            • **Automatically updated** by the system
            • **Used to determine** when to escalate/de-escalate

            **Example:** `input_text.climate_trend_direction_living_room`
          default: []
          selector:
            entity:
              domain: input_text
        
        helper_mode_start_time:
          name: Mode Start Time Helper (Optional)
          description: |
            **Input DateTime helper** to track when current mode started.

            • **Timestamp** when current AC mode began
            • **Required** for effectiveness calculations
            • **Used to determine** how long to wait before changes
            • **Automatically updated** by the system

            **Example:** `input_datetime.climate_mode_start_time_living_room`
          default: []
          selector:
            entity:
              domain: input_datetime
        
        helper_effectiveness_score:
          name: Effectiveness Score Helper (Optional)
          description: |
            **Input Number helper** to track how well current mode is working (0-100%).

            • **0%:** Mode not working (temperature getting worse)
            • **50%:** Mode working slowly
            • **100%:** Mode working effectively
            • **Used to decide** escalation/de-escalation

            **Example:** `input_number.climate_effectiveness_score_living_room`
          default: []
          selector:
            entity:
              domain: input_number
        
        helper_temp_stable_since:
          name: Temperature Stable Since Helper (Optional)
          description: |
            **Input DateTime helper** to track when temperature became stable.

            • **Timestamp** when temperature entered stable range
            • **Required** for stability auto-off feature
            • **Used to calculate** how long temperature has been stable
            • **Automatically updated** by the system

            **Example:** `input_datetime.temp_stable_since_living_room`
          default: []
          selector:
            entity:
              domain: input_datetime
        
        helper_last_transition:
          name: Last Transition Helper (Optional)
          description: |
            **Input Text helper** to track last temperature zone transition.

            • **Stores:** to_cooling, to_heating, to_comfort, etc.
            • **Used for hysteresis** to prevent rapid switching
            • **Prevents oscillation** at temperature boundaries
            • **Automatically updated** by the system

            **Example:** `input_text.climate_last_transition_living_room`
          default: []
          selector:
            entity:
              domain: input_text
        
        hysteresis_tolerance:
          name: Hysteresis Tolerance
          description: |
            **Temperature tolerance** for preventing rapid switching (±°C).

            • **Prevents oscillation** at temperature boundaries
            • **0.1-0.3°C:** Sensitive (may cause more switching)
            • **0.3-0.5°C:** Balanced approach (recommended)
            • **0.5-1.0°C:** Conservative (less switching)

            **Rule:** System won't change modes unless temperature exceeds threshold by this amount.
          default: 0.3
          selector:
            number:
              min: 0.1
              max: 1.0
              step: 0.1
              unit_of_measurement: "°C"
        
        enable_debug_logging:
          name: Enable Debug Logging
          description: |
            **Enable detailed warning-level logs** for troubleshooting.

            • **Shows decision-making process** and sensor states
            • **Helps diagnose** automation issues
            • **View logs** in Settings → System → Logs
            • **WARNING:** Creates many log entries!

            **Recommendation:** Enable only when troubleshooting - disable for normal operation.
          default: false
          selector:
            boolean:
        
        check_interval_minutes:
          name: Check Interval
          description: |
            **How often to check conditions** and evaluate temperature (in minutes).

            • **1-3 minutes:** Very responsive (more CPU usage)
            • **5 minutes:** Balanced approach (recommended)
            • **10+ minutes:** Conservative (less responsive)

            **Trade-off:** Lower intervals provide tighter control but increase system load.
          default: 5
          selector:
            number:
              min: 1
              max: 30
              step: 1
              unit_of_measurement: minutes
              mode: slider

    # Notification Settings
    notifications:
      name: "Notification Settings"
      icon: mdi:bell
      collapsed: true
      input:
        enable_notifications:
          name: Enable Notifications
          description: |
            **Enable push notifications** when the climate control system makes changes.

            **Notifications include:** Mode switches, temperature adjustments, and system status updates.
          default: true
          selector:
            boolean:
        
        notification_service:
          name: Notification Service
          description: |
            **Primary notification service** to send alerts to.

            **Common options:** `notify.notify`, `notify.mobile_app_yourphone`, `notify.pushbullet`
          default: notify.notify
          selector:
            text:
        
        additional_notify_services:
          name: Additional Notification Services
          description: |
            Additional services to notify beyond the primary service.

            • One service per line
            • Useful for multiple people or notification methods
            • Examples: notify.mobile_app_phone2, notify.telegram

            Leave empty if only one notification service is needed.
          default: []
          selector:
            text:
              multiple: true

    # Adaptive Control Mode Settings
    adaptive_control:
      name: "Adaptive Control Mode Settings"
      icon: mdi:auto-mode
      collapsed: true
      input:
        enable_adaptive_control:
          name: Enable Adaptive Control Mode
          description: |
            **Automatically switches control modes** based on room occupancy patterns.

            **How it works:**

            • **Room becomes occupied** → Switch to Auto/Smart mode after configurable delay
            • **Room becomes vacant** → Switch to Manual mode and turn AC off after delay

            **REQUIREMENTS:**

            • **Room presence sensors** configured in Smart Mode Settings
            • PIR/motion sensors, BLE tracking devices, or occupancy sensors
            • **Last Presence Helper Entity** (input_datetime) to track timing
            • **Smart Mode** must be properly configured

            **Use cases:** Automatic comfort when you enter, energy savings when you leave.
          default: false
          selector:
            boolean:
        
        adaptive_occupied_delay:
          name: Occupied Delay (minutes)
          description: |
            **How long to wait** after detecting room presence before switching from Manual to Auto/Smart mode.

            • **Shorter delays:** More responsive but may switch too frequently
            • **Longer delays:** Less responsive but avoids unnecessary switching

            **Recommended:** 5 minutes for balanced behavior.
          default: 5
          selector:
            number:
              min: 1
              max: 60
              step: 1
              unit_of_measurement: minutes
              mode: slider
        
        adaptive_vacant_delay:
          name: Vacant Delay (minutes)
          description: |
            **How long to wait** after room becomes vacant before switching to Manual mode and turning AC off.

            • **Shorter delays:** Better energy savings but may turn off too quickly
            • **Longer delays:** More comfort for brief departures but wastes energy

            **Recommended:** 15 minutes to handle brief absences without wasting energy.
          default: 15
          selector:
            number:
              min: 5
              max: 120
              step: 5
              unit_of_measurement: minutes
              mode: slider
        
        adaptive_target_mode:
          name: Target Mode When Occupied
          description: |
            **Which control mode** to activate when room occupancy is detected.

            • **Auto Mode:** Standard automatic temperature control
            • **Smart Mode:** Advanced occupancy-based control with presence timeout

            **Recommendation:** Choose Smart Mode if you want the most intelligent behavior.
          default: "auto"
          selector:
            select:
              options:
                - label: "Auto Mode"
                  value: "auto"
                - label: "Smart Mode"
                  value: "smart"
        
        adaptive_override_timeout:
          name: Manual Override Timeout (hours)
          description: |
            **How long to respect small adjustments** you make while automation is running.

            **WHAT THIS DOES:** When you tweak temperature/fan settings while in Auto/Smart mode,
            automation pauses its adjustments for this duration but **stays in the same mode**.

            **WHEN IT ACTIVATES:** Any manual adjustment while automation is active
            **WHAT CONTINUES:** Presence detection, mode switching, all other automation
            **WHAT PAUSES:** Temperature and fan speed adjustments only

            **Examples:**
            • You adjust 23°C → 25°C for comfort → System won't change it back for X hours
            • You turn off AC for pets → System won't turn it back on for X hours
            • You change fan from Auto to Level 3 → System respects your fan choice

            **This is NOT for remote pre-conditioning** - use Manual Mode for that.

            **Recommended:** 2 hours for most situations.
          default: 2
          selector:
            number:
              min: 0
              max: 24
              step: 0.5
              unit_of_measurement: hours
              mode: slider

        manual_mode_timeout:
          name: 🎮 Manual Mode Timeout
          description: |
            **How long to completely bypass ALL automation** when you switch to Manual mode.

            **WHAT THIS DOES:** Manual mode = 100% user control with ZERO automation interference.
            Perfect for **remote pre-conditioning** before arriving home.

            **WHEN IT ACTIVATES:** Only when you switch the control mode to "Manual"
            **WHAT STOPS:** ALL automation - presence detection, temperature control, everything
            **WHAT YOU GET:** Complete control to set any temperature/fan remotely

            **Examples:**
            • Set AC to 18°C cold before coming home → No presence sensors turn it off
            • Heat room to 26°C while away → System won't adjust based on "empty room"
            • Set specific fan speed for noise → Automation won't change it

            **After timeout:** Switches back to Smart mode (AC keeps running at your settings)
            **Then:** Normal automation resumes with presence detection

            **This IS for remote pre-conditioning** - different from Override above!
          default: "4_hours"
          selector:
            select:
              options:
                - label: "Never (Pure Manual - runs until manually changed)"
                  value: "never"
                - label: "1 Hour"
                  value: "1_hour"
                - label: "2 Hours"
                  value: "2_hours"
                - label: "4 Hours (Recommended)"
                  value: "4_hours"
                - label: "8 Hours"
                  value: "8_hours"
                - label: "12 Hours"
                  value: "12_hours"
                - label: "24 Hours"
                  value: "24_hours"

        
        adaptive_override_helper:
          name: Override Helper (Optional)
          description: |
            **Optional input_boolean helper** to manually disable adaptive control.

            When configured and set to **ON**, adaptive control will be **completely disabled** until turned OFF.

            **Example helper names:**

            • `input_boolean.climate_adaptive_override_office`

            • `input_boolean.ac_manual_override_bedroom`

            • `input_boolean.disable_adaptive_living_room`

            **Use this for:** Permanent manual control periods or when you want full override control via dashboard toggles.
          default: ""
          selector:
            entity:
              domain: input_boolean
              multiple: false

variables:
  climate_list: !input climate_entities
  persons: !input presence_persons
  devices: !input presence_devices
  eco_enabled: !input enable_eco_mode
  gradual_enabled: !input enable_gradual_adjustment
  enable_heating: !input enable_heating_mode
  enable_cooling: !input enable_cooling_mode
  away_action: !input away_mode_action
  temp_sensor: !input temperature_sensor
  use_avg: !input use_average_temperature
  helper_mode: !input helper_last_mode
  helper_change: !input helper_last_change
  proximity_sensor_input: !input proximity_sensor
  direction_sensor_input: !input direction_sensor
  
  # Simple temperature inputs
  target_temp: !input target_temperature
  comfort_width: !input comfort_zone_width
  aggressiveness: !input temperature_aggressiveness
  advanced_enabled: !input enable_advanced_temp
  
  # Store advanced inputs if enabled
  comfort_min_input: !input comfort_min_temp
  comfort_max_input: !input comfort_max_temp
  cooling_target_input: !input cooling_target_temp
  heating_target_input: !input heating_target_temp
  cooling_low_input: !input cooling_low_temp
  cooling_medium_input: !input cooling_medium_temp
  cooling_high_input: !input cooling_high_temp
  heating_low_input: !input heating_low_temp
  heating_medium_input: !input heating_medium_temp
  heating_high_input: !input heating_high_temp
  
  # Calculate comfort zones based on base target (before weather adjustment)
  base_comfort_min: "{{ comfort_min_input if advanced_enabled else (target_temp - comfort_width) | float }}"
  base_comfort_max: "{{ comfort_max_input if advanced_enabled else (target_temp + comfort_width) | float }}"
  base_cooling_target: "{{ cooling_target_input if advanced_enabled else target_temp | float }}"
  base_heating_target: "{{ heating_target_input if advanced_enabled else target_temp | float }}"
  
  # Auto-calculate base thresholds based on aggressiveness (smooth graduated response)
  # Cooling thresholds (escalate as temperature rises)
  base_cooling_low: "{{ cooling_low_input if advanced_enabled else (base_comfort_max + 0.1) | round(1) }}"
  base_cooling_medium: "{{ cooling_medium_input if advanced_enabled else (base_comfort_max + 1.0) | round(1) }}"
  base_cooling_high: "{{ cooling_high_input if advanced_enabled else (base_comfort_max + 2.0) | round(1) }}"
  # Heating thresholds (escalate as temperature drops)
  base_heating_low: "{{ heating_low_input if advanced_enabled else (base_comfort_min - 0.1) | round(1) }}"
  base_heating_medium: "{{ heating_medium_input if advanced_enabled else (base_comfort_min - 1.0) | round(1) }}"
  base_heating_high: "{{ heating_high_input if advanced_enabled else (base_comfort_min - 2.0) | round(1) }}"
  
  # Hysteresis logic variables to prevent short cycling
  # Cooling: Turn on at threshold, but continue until target reached
  cooling_on_threshold: "{{ base_cooling_low }}"     # Turn ON when temp reaches this
  cooling_off_threshold: "{{ target_temp }}"         # Turn OFF when temp reaches target
  heating_on_threshold: "{{ base_heating_low }}"     # Turn ON when temp drops to this  
  heating_off_threshold: "{{ target_temp }}"         # Turn OFF when temp reaches target
  
  enable_pre_conditioning: !input enable_pre_conditioning
  enable_away_mode: !input enable_away_mode
  enable_notifications: !input enable_notifications
  eco_mode_setpoint_offset: !input eco_mode_setpoint_offset
  # Fan speed settings
  fan_speed_max: !input fan_speed_max
  fan_speed_medium: !input fan_speed_medium
  # Dynamic adaptation settings
  dynamic_enabled: !input enable_dynamic_adaptation
  effectiveness_check_mins: !input effectiveness_check_minutes
  temp_tolerance: !input escalation_temp_tolerance
  deescalation_threshold: !input deescalation_approach_threshold
  helper_temp_history: !input helper_temp_history
  helper_trend: !input helper_trend_direction
  helper_mode_time: !input helper_mode_start_time
  helper_effectiveness: !input helper_effectiveness_score
  last_mode: "{{ states(helper_mode) | default('off') }}"
  # Check actual AC state, not just internal tracking
  actual_ac_state: >
    {% set ac_states = namespace(all_off=true) %}
    {% for entity in climate_list %}
      {% set hvac_mode = states(entity) %}
      {% if hvac_mode not in ['off', 'Off', 'unavailable', 'unknown'] %}
        {% set ac_states.all_off = false %}
      {% endif %}
    {% endfor %}
    {{ 'off' if ac_states.all_off else 'on' }}
  last_change: "{{ states(helper_change) | default(now()) }}"
  runtime_min: !input min_runtime_minutes
  offtime_min: !input min_off_time_minutes
  # Smart mode settings
  control_mode_helper: !input helper_control_mode
  presence_detected_helper: !input helper_presence_detected
  proximity_override_helper: !input helper_proximity_override
  presence_timeout: !input presence_timeout_minutes
  home_zone_distance: !input home_zone_distance
  room_sensors: !input room_presence_sensors
  room_name: !input room_name
  room_ble_name: !input room_name
  adjacent_rooms_input: !input adjacent_room_names
  # Parse adjacent room names into a list for BLE detection
  adjacent_room_list: >
    {% if adjacent_rooms_input and adjacent_rooms_input != '' %}
      {% set rooms = [] %}
      {% for room in adjacent_rooms_input.split(',') %}
        {% set rooms = rooms + [room.strip()] %}
      {% endfor %}
      {{ rooms }}
    {% else %}
      []
    {% endif %}
  smart_behavior: !input smart_mode_behavior
  presence_validation: !input presence_validation_mode
  temp_stability_enabled: !input temp_stability_enabled
  stability_tolerance: !input stability_tolerance
  stability_duration: !input stability_duration
  stability_behavior: !input stability_behavior
  extreme_override: !input extreme_temp_override
  extreme_high: !input extreme_high_temp
  extreme_low: !input extreme_low_temp
  # New v2.1+ features - Scheduling
  enable_scheduling: !input enable_scheduling
  morning_temp: !input morning_temp
  day_temp: !input day_temp
  evening_temp: !input evening_temp
  night_temp: !input night_temp
  enable_weekend_schedule: !input enable_weekend_schedule
  weekend_morning_temp: !input weekend_morning_temp
  weekend_day_temp: !input weekend_day_temp
  weekend_night_temp: !input weekend_night_temp
  # New v2.1+ features - Window Detection
  enable_window_detection: !input enable_window_detection
  window_sensors: !input window_sensors
  window_open_delay: !input window_open_delay
  window_close_delay: !input window_close_delay
  # New v2.1+ features - Weather Compensation
  enable_weather_compensation: !input enable_weather_compensation
  weather_entity: !input weather_entity
  outdoor_temp_sensor: !input outdoor_temp_sensor
  compensation_factor: !input compensation_factor
  max_compensation: !input max_compensation
  compensation_base_temp: !input compensation_base_temp
  # New v2.1+ helpers - Stability and Hysteresis
  helper_temp_stable_since: !input helper_temp_stable_since
  helper_last_transition: !input helper_last_transition
  hysteresis_tolerance: !input hysteresis_tolerance
  # Adaptive Control Mode variables
  adaptive_enabled: !input enable_adaptive_control
  adaptive_occupied_delay: !input adaptive_occupied_delay
  adaptive_vacant_delay: !input adaptive_vacant_delay
  adaptive_target_mode: !input adaptive_target_mode
  adaptive_override_timeout: !input adaptive_override_timeout
  adaptive_override_helper: !input adaptive_override_helper
  manual_mode_timeout: !input manual_mode_timeout

  # Convert timeout selection to hours for calculations
  manual_timeout_hours: >
    {% set timeout = manual_mode_timeout %}
    {% if timeout == 'never' %}
      0
    {% elif timeout == '1_hour' %}
      1
    {% elif timeout == '2_hours' %}
      2
    {% elif timeout == '4_hours' %}
      4
    {% elif timeout == '8_hours' %}
      8
    {% elif timeout == '12_hours' %}
      12
    {% elif timeout == '24_hours' %}
      24
    {% else %}
      4
    {% endif %}

trigger:
  - platform: state
    entity_id: !input climate_entities
    attribute: current_temperature
    id: temp_change
  
  - platform: state
    entity_id: !input temperature_sensor
    id: external_temp_change
  
  # Single time pattern trigger based on selected interval
  - platform: time_pattern
    minutes: "/1"
    id: periodic_check
  
  - platform: state
    entity_id: !input direction_sensor
    to: "towards"
    id: heading_home
  
  - platform: state
    entity_id: !input direction_sensor
    to: "arrived"
    id: arrived
  
  - platform: state
    entity_id: !input presence_persons
    id: presence_change
  
  - platform: state
    entity_id: !input presence_devices
    id: device_change

  # Fire immediately when control mode changes (Manual/Smart/Auto)
  - platform: state
    entity_id: !input helper_control_mode
    id: mode_change

condition: []

action:
  # Mode change detected - immediately clear helper BEFORE variables are calculated
  - if:
      - condition: template
        value_template: "{{ trigger.id == 'mode_change' and helper_mode not in [none, '', 'unavailable', 'unknown'] }}"
    then:
      - service: input_text.set_value
        target:
          entity_id: "{{ helper_mode }}"
        data:
          value: "off"
        continue_on_error: true

  # Store inputs as variables first
  - variables:
      check_interval: !input check_interval_minutes
      debug_enabled: !input enable_debug_logging
      proximity_sensor: >
        {% if proximity_sensor_input and proximity_sensor_input not in [none, '', 'unavailable', 'unknown'] and proximity_sensor_input is string and proximity_sensor_input != '' %}
          {{ proximity_sensor_input }}
        {% else %}
          none
        {% endif %}
      direction_sensor: >
        {% if direction_sensor_input and direction_sensor_input not in [none, '', 'unavailable', 'unknown'] and direction_sensor_input is string and direction_sensor_input != '' %}
          {{ direction_sensor_input }}
        {% else %}
          none
        {% endif %}
      # Dynamic adaptation variables
      previous_temp: >
        {% if helper_temp_history %}
          {{ states(helper_temp_history) | float(0) }}
        {% else %}
          0
        {% endif %}
      current_trend: >
        {% if helper_trend %}
          {{ states(helper_trend) | default('stable') }}
        {% else %}
          stable
        {% endif %}
      mode_start_time: >
        {% if helper_mode_time %}
          {{ states(helper_mode_time) | default(now()) | as_datetime }}
        {% else %}
          {{ now() }}
        {% endif %}
      # Calculate effectiveness automatically based on temperature progress
      current_effectiveness: >
        {% if helper_effectiveness and states(helper_effectiveness) != 'unavailable' %}
          {# Use manual helper if available #}
          {{ states(helper_effectiveness) | float(50) }}
        {% else %}
          {# Calculate effectiveness automatically with error handling #}
          {% if time_in_current_mode < 2 %}
            {# Too early to judge - assume moderate effectiveness #}
            50
          {% elif last_mode in ['cooling', 'heating', 'cooling_low', 'cooling_medium', 'cooling_high', 'heating_low', 'heating_medium', 'heating_high'] %}
            {% set target = target_temp %}
            {% set initial_distance = (previous_temp - target) | abs if previous_temp and previous_temp > 0 else distance_from_target %}
            {% set current_distance = distance_from_target | float(0) %}
            {% if initial_distance > 0 and initial_distance is number %}
              {# Calculate progress toward target (0-100%) with safety checks #}
              {% set progress = ((initial_distance - current_distance) / initial_distance) * 100 %}
              {% set time_factor = (time_in_current_mode | float(1)) / 10 %}  {# Normalize to 10 minutes #}
              {% if time_factor > 0 and time_factor is number %}
                {% set raw_effectiveness = (progress / time_factor) | round(1) %}
                {# Handle case where temperature hasn't changed yet but system is actively working #}
                {% if progress == 0 and time_in_current_mode < 10 and current_distance > 0.5 %}
                  {% set effectiveness = 40 %}  {# Moderate effectiveness while pursuing target #}
                {% else %}
                  {% set effectiveness = raw_effectiveness %}
                {% endif %}
                {# Cap between 0-100 and apply logic #}
                {% if calculated_trend == 'rising' and 'cooling' in last_mode %}
                  0  {# Cooling but getting hotter - completely ineffective #}
                {% elif calculated_trend == 'falling' and 'heating' in last_mode %}
                  0  {# Heating but getting colder - completely ineffective #}
                {% elif current_distance <= 0.5 %}
                  100  {# Very close to target - highly effective #}
                {% elif effectiveness > 100 %}
                  100
                {% elif effectiveness < 0 %}
                  10  {# Some minimum effectiveness #}
                {% else %}
                  {{ effectiveness | round(0) }}
                {% endif %}
              {% else %}
                50  {# Avoid division by zero or invalid time factor #}
              {% endif %}
            {% else %}
              75  {# Already at target or invalid distance calculation #}
            {% endif %}
          {% else %}
            50  {# Not in heating/cooling mode #}
          {% endif %}
        {% endif %}
      should_proceed: >
        {% if trigger.id == 'periodic_check' %}
          {% set mins = now().minute %}
          {% set interval = check_interval | int(1) %}
          {% if interval > 0 %}
            {{ (mins % interval) == 0 }}
          {% else %}
            true
          {% endif %}
        {% else %}
          true
        {% endif %}
  
  # Continue with automation if conditions are met
  - condition: template
    value_template: "{{ should_proceed }}"
  
  # Debug: Log automation trigger
  - if:
      - condition: template
        value_template: "{{ debug_enabled }}"
    then:
      - service: system_log.write
        data:
          message: >
            🏠 {{ room_name | upper }} CLIMATE CONTROL ACTIVATED
            {% if trigger.id == 'temp_change' %}📊 Temperature changed{% elif trigger.id == 'presence_change' %}👤 Someone arrived/left{% elif trigger.id == 'periodic_check' %}⏰ Routine check{% elif trigger.id == 'mode_change' %}🎮 MODE CHANGED: {{ trigger.from_state.state }} → {{ trigger.to_state.state }}{% else %}🔄 {{ trigger.id }}{% endif %}

            📋 Technical: {{ trigger.platform }}/{{ trigger.entity_id | default('timer') }}/{{ check_interval }}min
          level: warning
          logger: "blueprints.climate_control.{{ room_name | lower | replace(' ', '_') }}"

  # Mode change detected - immediately update helper and clear Manual Override
  - if:
      - condition: template
        value_template: "{{ trigger.id == 'mode_change' and helper_change not in [none, '', 'unavailable', 'unknown'] }}"
    then:
      # Reset Manual Override timer
      - service: input_datetime.set_datetime
        target:
          entity_id: "{{ helper_change }}"
        data:
          datetime: "{{ now() }}"
        continue_on_error: true


      - if:
          - condition: template
            value_template: "{{ debug_enabled }}"
        then:
          - service: system_log.write
            data:
              message: >
                🎮 {{ room_name | upper }} MODE CHANGE DETECTED: Cleared for immediate mode switch
                • Mode Changed: {{ trigger.from_state.state }} → {{ trigger.to_state.state }}
                • Helper cleared to "off" - {{ trigger.to_state.state }} mode will activate immediately
                • Manual Override disabled for mode changes
              level: warning
              logger: "blueprints.climate_control.{{ room_name | lower | replace(' ', '_') }}"
  
  - variables:
      current_temp: >
        {% if temp_sensor %}
          {{ states(temp_sensor) | float(25) }}
        {% elif use_avg %}
          {% set temps = namespace(values=[]) %}
          {% for entity in climate_list %}
            {% set temp_value = state_attr(entity, 'current_temperature') | float(0) %}
            {% if temp_value > 0 %}
              {% set temps.values = temps.values + [temp_value] %}
            {% endif %}
          {% endfor %}
          {% if temps.values | length > 0 %}
            {{ (temps.values | sum / temps.values | length) | round(1) }}
          {% else %}
            25
          {% endif %}
        {% else %}
          {{ state_attr(climate_list[0], 'current_temperature') | float(25) }}
        {% endif %}
      
      # Calculate base target temperature with scheduling
      scheduled_target_temp: >
        {% if enable_scheduling %}
          {% set hour = now().hour %}
          {% set is_weekend = now().weekday() >= 5 %}
          {% if is_weekend and enable_weekend_schedule %}
            {% if 6 <= hour < 10 %}
              {{ weekend_morning_temp }}
            {% elif 10 <= hour < 22 %}
              {{ weekend_day_temp }}
            {% else %}
              {{ weekend_night_temp }}
            {% endif %}
          {% else %}
            {% if 6 <= hour < 9 %}
              {{ morning_temp }}
            {% elif 9 <= hour < 18 %}
              {{ day_temp }}
            {% elif 18 <= hour < 22 %}
              {{ evening_temp }}
            {% else %}
              {{ night_temp }}
            {% endif %}
          {% endif %}
        {% else %}
          {{ target_temp }}
        {% endif %}
      
      # Apply weather compensation to target temperature
      weather_adjusted_target: >
        {% if enable_weather_compensation %}
          {% if outdoor_temp_sensor %}
            {% set outdoor = states(outdoor_temp_sensor) | float(25) %}
          {% else %}
            {% set outdoor = state_attr(weather_entity, 'temperature') | float(25) %}
          {% endif %}
          {% set compensation = ((outdoor - compensation_base_temp) * compensation_factor) | round(1) %}
          {% set compensation = [[-max_compensation, compensation] | max, max_compensation] | min %}
          {{ (scheduled_target_temp + compensation) | round(1) }}
        {% else %}
          {{ scheduled_target_temp }}
        {% endif %}
      
      # Calculate weather-adjusted comfort zones and thresholds
      comfort_min_temp: >
        {% set adjustment = weather_adjusted_target - target_temp %}
        {{ (base_comfort_min + adjustment) | round(1) }}
      
      comfort_max_temp: >
        {% set adjustment = weather_adjusted_target - target_temp %}
        {{ (base_comfort_max + adjustment) | round(1) }}
      
      cooling_target_temp: >
        {% set adjustment = weather_adjusted_target - target_temp %}
        {{ (base_cooling_target + adjustment) | round(1) }}
      
      heating_target_temp: >
        {% set adjustment = weather_adjusted_target - target_temp %}
        {{ (base_heating_target + adjustment) | round(1) }}
      
      cooling_low_temp: >
        {% set adjustment = weather_adjusted_target - target_temp %}
        {{ (base_cooling_low + adjustment) | round(1) }}
      
      cooling_medium_temp: >
        {% set adjustment = weather_adjusted_target - target_temp %}
        {{ (base_cooling_medium + adjustment) | round(1) }}
      
      cooling_high_temp: >
        {% set adjustment = weather_adjusted_target - target_temp %}
        {{ (base_cooling_high + adjustment) | round(1) }}
      
      heating_low_temp: >
        {% set adjustment = weather_adjusted_target - target_temp %}
        {{ (base_heating_low + adjustment) | round(1) }}
      
      heating_medium_temp: >
        {% set adjustment = weather_adjusted_target - target_temp %}
        {{ (base_heating_medium + adjustment) | round(1) }}
      
      heating_high_temp: >
        {% set adjustment = weather_adjusted_target - target_temp %}
        {{ (base_heating_high + adjustment) | round(1) }}
      
      # Check window sensors
      window_open: >
        {% if enable_window_detection %}
          {% set open = namespace(value=false) %}
          {% for sensor in window_sensors %}
            {% if is_state(sensor, 'on') %}
              {% set open.value = true %}
            {% endif %}
          {% endfor %}
          {{ open.value }}
        {% else %}
          false
        {% endif %}
      
      anyone_home: >
        {% set home = namespace(value=false) %}
        {% for person in persons %}
          {% if is_state(person, 'home') %}
            {% set home.value = true %}
          {% endif %}
        {% endfor %}
        {% if not home.value %}
          {% for device in devices %}
            {% if is_state(device, 'on') or is_state(device, 'PowerOn') %}
              {% set home.value = true %}
            {% endif %}
          {% endfor %}
        {% endif %}
        {{ home.value }}
      
      approaching_home: >
        {% if direction_sensor and proximity_sensor %}
          {{ is_state(direction_sensor, 'towards') and 
             states(proximity_sensor) | float(10000) < home_zone_distance }}
        {% else %}
          false
        {% endif %}
      
      time_since_change: >
        {% if helper_change and states(helper_change) not in ['unknown', 'unavailable', ''] %}
          {{ (as_timestamp(now()) - as_timestamp(states(helper_change))) / 60 }}
        {% else %}
          999
        {% endif %}
      
      # Dynamic adaptation calculations
      time_in_current_mode: >
        {% set mode_time_val = states(helper_mode_time) if helper_mode_time else 'unknown' %}
        {% if mode_time_val not in ['unknown', 'unavailable', '', None] %}
          {% set mode_timestamp = as_timestamp(mode_time_val) %}
          {% if mode_timestamp %}
            {{ ((as_timestamp(now()) - mode_timestamp) / 60) | round(1) | abs }}
          {% else %}
            0
          {% endif %}
        {% else %}
          0
        {% endif %}
      
      # Calculate actual temperature stability time (when temp last changed significantly)
      temp_stability_time: >
        {% if helper_temp_history and helper_temp_stable_since %}
          {% set temp_diff = (current_temp - previous_temp) | abs %}
          {% if temp_diff <= stability_tolerance %}
            {% set stable_since = states(helper_temp_stable_since) %}
            {% if stable_since not in ['unknown', 'unavailable', '', None] %}
              {% set stable_timestamp = as_timestamp(stable_since) %}
              {% if stable_timestamp %}
                {{ ((as_timestamp(now()) - stable_timestamp) / 60) | round(1) }}
              {% else %}
                0
              {% endif %}
            {% else %}
              {# Stable since not set, assume just became stable #}
              0
            {% endif %}
          {% else %}
            {# Temperature changed significantly, stability time is 0 #}
            0
          {% endif %}
        {% else %}
          0
        {% endif %}
      
      temp_change_rate: >
        {% if previous_temp > 0 and current_temp != previous_temp %}
          {% set interval_num = check_interval | int(1) %}
          {% if interval_num > 0 %}
            {{ ((current_temp - previous_temp) / interval_num) | round(2) }}
          {% else %}
            0
          {% endif %}
        {% else %}
          0
        {% endif %}
      
      calculated_trend: >
        {% if temp_change_rate > 0.2 %}
          rising
        {% elif temp_change_rate < -0.2 %}
          falling  
        {% else %}
          stable
        {% endif %}
      
      temp_stability_detected: >
        {% if temp_stability_enabled and helper_temp_history %}
          {% set temp_diff = (current_temp - previous_temp) | abs %}
          {% set time_threshold = stability_duration | float %}
          {# CRITICAL: Only consider stable if temp is stable AND within comfort zone AND close to target #}
          {% set in_comfort_zone = current_temp >= comfort_min_temp and current_temp <= comfort_max_temp %}
          {# NEW: Respect target-based hysteresis - must be close to actual target, not just in comfort zone #}
          {% set close_to_target = (current_temp - target_temp) | abs <= 0.5 %}
          {# NEW: Don't interrupt continue modes that are actively pursuing target #}
          {% set not_in_continue_mode = last_mode not in ['cooling_low', 'cooling_medium', 'cooling_high', 'heating_low', 'heating_medium', 'heating_high'] %}
          {% if temp_diff <= stability_tolerance and calculated_trend == 'stable' and in_comfort_zone and close_to_target and not_in_continue_mode %}
            {% if temp_stability_time >= time_threshold %}
              {% if last_mode not in ['off', 'stability_off', 'stability_eco', 'unavailable', 'unknown', none] %}
                true
              {% else %}
                false
              {% endif %}
            {% else %}
              false
            {% endif %}
          {% else %}
            false
          {% endif %}
        {% else %}
          false
        {% endif %}
      
      distance_from_target: >
        {% if last_mode in ['cooling', 'cooling_low', 'cooling_medium', 'cooling_high'] %}
          {{ (current_temp - target_temp) | abs }}
        {% elif last_mode in ['heating', 'heating_low', 'heating_medium', 'heating_high'] %}
          {{ (current_temp - target_temp) | abs }}
        {% elif current_temp > comfort_max_temp %}
          {{ (current_temp - target_temp) | abs }}
        {% elif current_temp < comfort_min_temp %}
          {{ (current_temp - target_temp) | abs }}
        {% else %}
          0
        {% endif %}
      
      # Hysteresis logic - prevent rapid switching at temperature boundaries
      last_transition: >
        {% if helper_last_transition %}
          {% set transition_state = states(helper_last_transition) | default('none') %}
          {{ 'none' if transition_state in ['unknown', 'unavailable', ''] else transition_state }}
        {% else %}
          none
        {% endif %}
      
      cooling_with_hysteresis: >
        {% if last_transition == 'heating' %}
          {# Coming from heating - need extra margin to switch to cooling #}
          {{ current_temp > (comfort_max_temp + hysteresis_tolerance) }}
        {% else %}
          {# Standard cooling threshold #}
          {{ current_temp > comfort_max_temp }}
        {% endif %}
      
      heating_with_hysteresis: >
        {% if last_transition == 'cooling' %}
          {# Coming from cooling - need extra margin to switch to heating #}
          {{ current_temp < (comfort_min_temp - hysteresis_tolerance) }}
        {% else %}
          {# Standard heating threshold #}
          {{ current_temp < comfort_min_temp }}
        {% endif %}
      
      # Enhanced escalation logic with multiple triggers and graduated response
      escalation_level: >
        {% if not dynamic_enabled %}
          0
        {% else %}
          {% set min_check_time = 3 %}  {# Minimum 3 minutes before any escalation #}
          {% set emergency_threshold = 4.0 %}  {# Degrees from target for emergency escalation #}
          {% set stall_threshold = 2.0 %}  {# Degrees for considering progress stalled #}
          
          {# Level 0: No escalation needed #}
          {% if time_in_current_mode < min_check_time %}
            0
          {% elif current_effectiveness >= 80 %}
            0  {# Working well, no escalation needed #}
          {% elif distance_from_target <= 1.0 %}
            0  {# Close to target, no escalation needed #}
          
          {# Level 4: EMERGENCY - Immediate maximum power #}
          {% elif distance_from_target > emergency_threshold and time_in_current_mode >= min_check_time %}
            4  {# Far from target - emergency escalation #}
          {% elif current_effectiveness <= 10 and time_in_current_mode >= 5 %}
            4  {# Completely ineffective after 5 minutes #}
          {% elif last_mode == 'cooling' and calculated_trend == 'rising' and current_temp > (comfort_max_temp + 1.0) %}
            4  {# Cooling but temperature rising above comfort zone #}
          {% elif last_mode == 'heating' and calculated_trend == 'falling' and current_temp < (comfort_min_temp - 1.0) %}
            4  {# Heating but temperature falling below comfort zone #}
          
          {# Level 3: HIGH - Strong escalation needed #}
          {% elif distance_from_target > 3.0 and time_in_current_mode >= 5 %}
            3  {# Still far from target after 5 minutes #}
          {% elif current_effectiveness <= 25 and time_in_current_mode >= 7 %}
            3  {# Poor effectiveness after reasonable time #}
          {% elif distance_from_target > stall_threshold and calculated_trend == 'stable' and time_in_current_mode >= 8 %}
            3  {# Progress has stalled #}
          
          {# Level 2: MEDIUM - Moderate escalation #}
          {% elif distance_from_target > 2.0 and time_in_current_mode >= 8 %}
            2  {# Moderate distance after longer time #}
          {% elif current_effectiveness <= 40 and time_in_current_mode >= 10 %}
            2  {# Below average effectiveness #}
          {% elif distance_from_target > 1.5 and time_in_current_mode >= effectiveness_check_mins %}
            2  {# Using original check time for moderate escalation #}
          
          {# Level 1: LOW - Minor escalation #}
          {% elif distance_from_target > 1.0 and time_in_current_mode >= (effectiveness_check_mins + 2) %}
            1  {# Slight distance after extended time #}
          {% elif current_effectiveness <= 60 and time_in_current_mode >= (effectiveness_check_mins + 5) %}
            1  {# Mediocre effectiveness after extra time #}
          
          {% else %}
            0  {# No escalation needed #}
          {% endif %}
        {% endif %}
      
      should_escalate: >
        {{ escalation_level > 0 }}
      
      # Smart mode calculations
      control_mode: >
        {% if control_mode_helper %}
          {{ states(control_mode_helper) | default('Auto') }}
        {% else %}
          Auto
        {% endif %}
      
      room_presence_detected: >
        {% set validation_mode = presence_validation | default('any') | lower %}
        {% set sensor_count = room_sensors | length %}
        
        {# If no sensors configured, always return false #}
        {% if sensor_count == 0 %}
          false
        {% else %}
          {# Categorize sensors and check their states #}
          {% set sensors_triggered = namespace(count=0) %}
          {% set ble_detected = namespace(value=false) %}
          {% set motion_detected = namespace(value=false) %}
          {% set other_detected = namespace(value=false) %}
          {% set motion_sensor_count = namespace(count=0) %}
          {% set ble_sensor_count = namespace(count=0) %}
          
          {% for sensor in room_sensors %}
            {% set sensor_state = states(sensor) | lower %}
            {% set is_triggered = false %}
            
            {# Check if this is a BLE/area sensor #}
            {% if sensor.startswith('sensor.') and room_ble_name %}
              {% set ble_sensor_count.count = ble_sensor_count.count + 1 %}
              {% if sensor_state == room_ble_name | lower %}
                {% set ble_detected.value = true %}
                {% set is_triggered = true %}
              {# Check if BLE detected in adjacent rooms #}
              {% elif adjacent_room_list | length > 0 %}
                {% for adjacent_room in adjacent_room_list %}
                  {% if sensor_state == adjacent_room | lower %}
                    {% set ble_detected.value = true %}
                    {% set is_triggered = true %}
                  {% endif %}
                {% endfor %}
              {% endif %}
            
            {# Check if this is a motion/binary sensor #}
            {% elif sensor.startswith('binary_sensor.') %}
              {% set motion_sensor_count.count = motion_sensor_count.count + 1 %}
              {% if is_state(sensor, 'on') or is_state(sensor, 'detected') or is_state(sensor, 'occupied') %}
                {% set motion_detected.value = true %}
                {% set is_triggered = true %}
              {% endif %}
            
            {# Check other sensor types #}
            {% elif is_state(sensor, 'on') or sensor_state in ['home', 'present', 'detected', 'occupied'] %}
              {% set other_detected.value = true %}
              {% set is_triggered = true %}
            {% endif %}
            
            {# Count triggered sensors #}
            {% if is_triggered %}
              {% set sensors_triggered.count = sensors_triggered.count + 1 %}
            {% endif %}
          {% endfor %}
          
          {# Apply validation mode logic #}
          {% if validation_mode == 'all' %}
            {# ALL mode - all sensors must be triggered #}
            {{ sensors_triggered.count == sensor_count }}
            
          {% elif validation_mode == 'majority' %}
            {# MAJORITY mode - more than half must be triggered #}
            {{ sensors_triggered.count > (sensor_count / 2) }}
            
          {% elif validation_mode == 'smart' %}
            {# SMART mode - intelligent BLE + motion validation #}
            {% if ble_sensor_count.count > 0 and motion_sensor_count.count > 0 %}
              {# Both BLE and motion sensors exist - require both #}
              {{ ble_detected.value and motion_detected.value }}
            {% elif ble_sensor_count.count > 0 %}
              {# Only BLE sensors - use BLE detection #}
              {{ ble_detected.value }}
            {% elif motion_sensor_count.count > 0 %}
              {# Only motion sensors - use motion detection #}
              {{ motion_detected.value }}
            {% else %}
              {# Other sensors only - any triggered #}
              {{ other_detected.value }}
            {% endif %}
            
          {% elif validation_mode == 'ble_plus' %}
            {# BLE_PLUS mode - require BLE + at least one other sensor #}
            {% if ble_sensor_count.count > 0 %}
              {{ ble_detected.value and (motion_detected.value or other_detected.value) }}
            {% else %}
              {# No BLE sensors - fallback to ANY mode #}
              {{ sensors_triggered.count > 0 }}
            {% endif %}
            
          {% else %}
            {# ANY mode (default) - any sensor triggered #}
            {{ sensors_triggered.count > 0 }}
          {% endif %}
        {% endif %}
      
      last_presence_time: >
        {% if presence_detected_helper and states(presence_detected_helper) not in ['unknown', 'unavailable', ''] %}
          {{ states(presence_detected_helper) | as_datetime }}
        {% else %}
          {{ now() }}
        {% endif %}
      
      minutes_since_presence: >
        {% if room_presence_detected %}
          0
        {% else %}
          {% set presence_time_val = states(presence_detected_helper) if presence_detected_helper else 'unknown' %}
          {% if presence_time_val not in ['unknown', 'unavailable', '', None] %}
            {% set presence_timestamp = as_timestamp(presence_time_val) %}
            {% if presence_timestamp %}
              {{ ((as_timestamp(now()) - presence_timestamp) / 60) | round(0) | abs }}
            {% else %}
              999
            {% endif %}
          {% else %}
            999
          {% endif %}
        {% endif %}
      
      current_distance: >
        {% if proximity_sensor %}
          {{ states(proximity_sensor) | float(99999) }}
        {% else %}
          99999
        {% endif %}
      
      proximity_zone: >
        {% if current_distance < home_zone_distance %}
          home
        {% else %}
          away
        {% endif %}
      
      smart_presence_active: >
        {% if control_mode == 'Manual' %}
          false
        {% elif room_presence_detected %}
          true
        {% elif minutes_since_presence < presence_timeout %}
          true
        {% elif proximity_zone == 'home' %}
          true
        {% elif anyone_home and control_mode == 'Auto' %}
          true
        {% else %}
          false
        {% endif %}
      
      extreme_temp_detected: >
        {% if not extreme_override %}
          false
        {% elif current_temp > extreme_high %}
          hot
        {% elif current_temp < extreme_low %}
          cold
        {% else %}
          false
        {% endif %}
      
      # Check if user manually changed AC settings (from HA or remote)
      manual_change_detected: >
        {% if helper_change and states(helper_change) not in ['unknown', 'unavailable', ''] %}
          {% set last_mode_state = states(helper_mode) | default('off') %}
          {% set actual_state = actual_ac_state %}
          {% set time_since_change = (as_timestamp(now()) - as_timestamp(states(helper_change))) / 60 %}
          {# Only detect manual changes if enough time has passed since last automation change #}
          {% if time_since_change < 2 %}
            false
          {# If AC state doesn't match what automation expects, user changed it manually #}
          {% elif last_mode_state == 'off' and actual_state == 'on' %}
            true
          {% elif last_mode_state in ['cooling', 'cooling_high', 'cooling_medium', 'cooling_low', 'heating', 'heating_high', 'heating_medium', 'heating_low', 'eco'] and actual_state == 'off' %}
            true
          {% else %}
            false
          {% endif %}
        {% else %}
          false
        {% endif %}

      # Manual Override - NEVER active when user explicitly switches modes
      manual_override_active: >
        {% if trigger.id == 'mode_change' %}
          false
        {% elif not manual_change_detected %}
          false
        {% elif adaptive_override_timeout <= 0 %}
          false
        {% elif control_mode == 'Manual' %}
          false
        {% else %}
          {% set last_change = states(helper_change) %}
          {% if last_change not in ['unknown', 'unavailable', ''] %}
            {% set time_since_manual = (as_timestamp(now()) - as_timestamp(last_change)) / 3600 %}
            {{ time_since_manual < adaptive_override_timeout }}
          {% else %}
            false
          {% endif %}
        {% endif %}

      # DEBUG: Manual change detection values
      debug_manual_detection: >
        DEBUG: manual_change_detected={{ manual_change_detected }}, helper_mode={{ states(helper_mode) | default('off') }}, actual_ac_state={{ actual_ac_state }}

      should_activate: >
        {% if control_mode == 'Manual' %}
          {# In manual mode, only activate for extreme temperatures with presence #}
          {% if extreme_temp_detected not in [false, 'false'] and (room_presence_detected or approaching_home) %}
            true
          {% else %}
            false
          {% endif %}
        {% elif manual_override_active %}
          {# User manually changed settings - respect the override timeout #}
          false
        {% elif control_mode == 'Smart' %}
          {% if not room_presence_detected and smart_behavior == 'off' and not extreme_temp_detected %}
            false
          {% else %}
            {{ smart_presence_active }}
          {% endif %}
        {% else %}
          {{ anyone_home or approaching_home }}
        {% endif %}
  
  # Detect manual AC changes and auto-switch to Manual mode
  - if:
      - condition: template
        value_template: "{{ control_mode_helper and control_mode != 'Manual' }}"
      - condition: template
        value_template: "{{ manual_change_detected }}"
    then:
      - service: system_log.write
        data:
          message: >
            🎮 {{ room_name | upper }} MANUAL OVERRIDE DETECTED
            • AC was manually changed (helper_mode: {{ states(helper_mode) | default('off') }}, actual: {{ actual_ac_state }})
            • Time since last automation change: {{ ((as_timestamp(now()) - as_timestamp(states(helper_change))) / 60) | round(1) if states(helper_change) not in ['unknown', 'unavailable', ''] else 'N/A' }}min
            • Switching to Manual mode to respect user control
            • Override protection: {{ adaptive_override_timeout }} hours
          level: warning
          logger: "blueprints.climate_control.{{ room_name | lower | replace(' ', '_') }}"

      # Switch to Manual mode
      - condition: template
        value_template: "{{ control_mode_helper not in [none, '', 'unavailable', 'unknown'] }}"
      - service: input_select.select_option
        target:
          entity_id: "{{ control_mode_helper }}"
        data:
          option: "Manual"

      # Update last change time for override protection
      - service: input_datetime.set_datetime
        target:
          entity_id: "{{ helper_change }}"
        data:
          datetime: "{{ now() }}"

      # Set last mode to manual
      - service: input_text.set_value
        target:
          entity_id: "{{ helper_mode }}"
        data:
          value: "manual"

  # Update presence helper only when room presence FIRST becomes detected (state change from False → True)
  - variables:
      # Check if we had recent presence activity (within presence timeout + 5 minutes buffer)
      had_recent_presence: >
        {% if presence_detected_helper and states(presence_detected_helper) not in ['unknown', 'unavailable', ''] %}
          {% set last_presence_timestamp = as_timestamp(states(presence_detected_helper)) %}
          {% set current_timestamp = as_timestamp(now()) %}
          {% set minutes_since_last = (current_timestamp - last_presence_timestamp) / 60 %}
          {{ minutes_since_last <= (presence_timeout + 5) }}
        {% else %}
          false
        {% endif %}
  
  # Only update timestamp if:
  # 1. Room presence is currently detected AND
  # 2. We haven't had recent presence activity (indicating this is a NEW session)
  - if:
      - condition: template
        value_template: "{{ room_presence_detected and presence_detected_helper not in [none, '', 'unavailable', 'unknown'] }}"
      - condition: template
        value_template: "{{ not had_recent_presence }}"
    then:
      - service: input_datetime.set_datetime
        target:
          entity_id: "{{ presence_detected_helper }}"
        data:
          datetime: "{{ now() }}"
  
  # Update temperature stability tracking - record when temp became stable
  - condition: template
    value_template: "{{ helper_temp_stable_since not in [none, '', 'unavailable', 'unknown'] and helper_temp_history not in [none, '', 'unavailable', 'unknown'] }}"
  - choose:
      - conditions:
          - condition: template
            value_template: >
              {% set temp_diff = (current_temp - previous_temp) | abs %}
              {% set was_stable = states(helper_temp_stable_since) not in ['unknown', 'unavailable', '', None] %}
              {{ temp_diff <= stability_tolerance and not was_stable }}
        sequence:
          # Temperature just became stable - record the time
          - service: input_datetime.set_datetime
            target:
              entity_id: "{{ helper_temp_stable_since }}"
            data:
              datetime: "{{ now() }}"
      
      - conditions:
          - condition: template
            value_template: >
              {% set temp_diff = (current_temp - previous_temp) | abs %}
              {{ temp_diff > stability_tolerance }}
        sequence:
          # Temperature is no longer stable - set to current time
          # This resets the stability timer without causing calculation issues
          - service: input_datetime.set_datetime
            target:
              entity_id: "{{ helper_temp_stable_since }}"
            data:
              datetime: "{{ now() }}"
  
  # Exit immediately if windows are open (unless disabled)
  - condition: template
    value_template: "{{ not window_open or not enable_window_detection }}"
  
  - variables:
      # Enhanced predictive de-escalation with smooth transitions
      deescalation_level: >
        {% if not dynamic_enabled %}
          0
        {% else %}
          {% set approaching_threshold = deescalation_threshold %}  {# Distance to start de-escalating #}
          {% set close_threshold = approaching_threshold / 2 %}    {# Very close to target #}
          {% set rate_threshold = 0.3 %}  {# °C/min change rate for predictions #}
          
          {# Level 0: No de-escalation #}
          {% if distance_from_target > approaching_threshold %}
            0  {# Still far from target #}
          {% elif calculated_trend == 'stable' or calculated_trend == '' %}
            0  {# No clear trend to predict #}
          
          {# Level 3: MAXIMUM de-escalation - Switch to eco/maintenance mode #}
          {% elif distance_from_target <= close_threshold and current_effectiveness >= 80 %}
            3  {# Very close and working well - switch to maintenance #}
          {% elif distance_from_target <= 0.5 %}
            3  {# Extremely close to target #}
          {% elif last_mode == 'cooling' and calculated_trend == 'falling' and (current_temp - cooling_target_temp) <= 1.0 %}
            3  {# Cooling and approaching target closely #}
          {% elif last_mode == 'heating' and calculated_trend == 'rising' and (heating_target_temp - current_temp) <= 1.0 %}
            3  {# Heating and approaching target closely #}
          
          {# Level 2: HIGH de-escalation - Reduce to medium power #}
          {% elif distance_from_target <= approaching_threshold and current_effectiveness >= 60 %}
            2  {# Approaching and working reasonably well #}
          {% elif distance_from_target <= 1.5 and calculated_trend != 'stable' %}
            2  {# Getting close with clear progress #}
          {% elif temp_change_rate | abs >= rate_threshold and distance_from_target <= 2.0 %}
            2  {# Fast progress toward target - predict overshoot #}
          
          {# Level 1: LOW de-escalation - Minor power reduction #}
          {% elif distance_from_target <= approaching_threshold and current_effectiveness >= 40 %}
            1  {# Approaching with moderate effectiveness #}
          {% elif distance_from_target <= 2.5 and time_in_current_mode >= 10 %}
            1  {# Moderate distance after sufficient time #}
          
          {% else %}
            0  {# Continue current power level #}
          {% endif %}
        {% endif %}
      
      should_deescalate: >
        {{ deescalation_level > 0 }}
  
  # Debug: Log dynamic escalation timing configuration
  - if:
      - condition: template
        value_template: "{{ debug_enabled and dynamic_enabled }}"
    then:
      - service: system_log.write
        data:
          message: >
            🔍 {{ room_name | upper }} DYNAMIC ESCALATION TIMING DEBUG

            📊 User Configuration:
                • Effectiveness Check Interval: {{ effectiveness_check_mins }} minutes
                • Runtime: {{ time_in_current_mode | round(1) }} min ({{ (time_in_current_mode - effectiveness_check_mins) | round(1) }} min {{ 'over' if time_in_current_mode >= effectiveness_check_mins else 'under' }} threshold)

            ⏰ Escalation Time Thresholds:
                • Level 2 (1.5°C distance): {{ effectiveness_check_mins }}min → {{ 'READY' if time_in_current_mode >= effectiveness_check_mins else 'WAITING' }}
                • Level 1 (1.0°C distance): {{ effectiveness_check_mins + 2 }}min → {{ 'READY' if time_in_current_mode >= (effectiveness_check_mins + 2) else 'WAITING' }}
                • Level 1 (≤60% effectiveness): {{ effectiveness_check_mins + 5 }}min → {{ 'READY' if time_in_current_mode >= (effectiveness_check_mins + 5) else 'WAITING' }}

            📈 Current Status:
                • Distance from target: {{ distance_from_target }}°C
                • Current effectiveness: {{ current_effectiveness }}%
                • Escalation level: {{ escalation_level }}/4
                • Should escalate: {{ should_escalate }}
          level: debug
          logger: "blueprints.climate_control.{{ room_name | lower | replace(' ', '_') }}.dynamic"

  # Debug: Log presence validation details
  - if:
      - condition: template
        value_template: "{{ debug_enabled and room_sensors | length > 0 }}"
    then:
      - service: system_log.write
        data:
          message: >
            🔍 {{ room_name | upper }} PRESENCE VALIDATION DEBUG
            
            📊 Sensor Analysis:
            {% set validation_mode = presence_validation | default('any') | lower %}
            {% set sensors_triggered = namespace(count=0, list=[]) %}
            {% set ble_detected = namespace(value=false, sensors=[]) %}
            {% set motion_detected = namespace(value=false, sensors=[]) %}
            {% set other_detected = namespace(value=false, sensors=[]) %}
            
            {% for sensor in room_sensors %}
              {% set sensor_state = states(sensor) | lower %}
              {% set is_triggered = false %}
              
              {% if sensor.startswith('sensor.') and room_ble_name %}
                {% set detected_room = '' %}
                {% if sensor_state == room_ble_name | lower %}
                  {% set ble_detected.value = true %}
                  {% set ble_detected.sensors = ble_detected.sensors + [sensor] %}
                  {% set is_triggered = true %}
                  {% set detected_room = room_ble_name %}
                {% elif adjacent_room_list | length > 0 %}
                  {% for adjacent_room in adjacent_room_list %}
                    {% if sensor_state == adjacent_room | lower %}
                      {% set ble_detected.value = true %}
                      {% set ble_detected.sensors = ble_detected.sensors + [sensor] %}
                      {% set is_triggered = true %}
                      {% set detected_room = adjacent_room ~ ' (adjacent)' %}
                    {% endif %}
                  {% endfor %}
                {% endif %}
                • BLE: {{ sensor }} = "{{ sensor_state }}" ({{ 'TRIGGERED - ' ~ detected_room if is_triggered else 'not detected' }})
              {% elif sensor.startswith('binary_sensor.') %}
                {% if is_state(sensor, 'on') or is_state(sensor, 'detected') or is_state(sensor, 'occupied') %}
                  {% set motion_detected.value = true %}
                  {% set motion_detected.sensors = motion_detected.sensors + [sensor] %}
                  {% set is_triggered = true %}
                {% endif %}
                • Motion: {{ sensor }} = {{ states(sensor) }} ({{ 'TRIGGERED' if is_triggered else 'clear' }})
              {% else %}
                {% if is_state(sensor, 'on') or sensor_state in ['home', 'present', 'detected', 'occupied'] %}
                  {% set other_detected.value = true %}
                  {% set other_detected.sensors = other_detected.sensors + [sensor] %}
                  {% set is_triggered = true %}
                {% endif %}
                • Other: {{ sensor }} = {{ states(sensor) }} ({{ 'TRIGGERED' if is_triggered else 'inactive' }})
              {% endif %}
              
              {% if is_triggered %}
                {% set sensors_triggered.count = sensors_triggered.count + 1 %}
                {% set sensors_triggered.list = sensors_triggered.list + [sensor] %}
              {% endif %}
            {% endfor %}
            
            🎯 Validation Summary:
                • Mode: {{ validation_mode | upper }}
                • Total Sensors: {{ room_sensors | length }}
                • Triggered: {{ sensors_triggered.count }} ({{ sensors_triggered.list | join(', ') if sensors_triggered.list else 'none' }})
            
            📊 Detection Results:
                • BLE Detected: {{ ble_detected.value }} ({{ ble_detected.sensors | join(', ') if ble_detected.sensors else 'none' }})
                • Motion Detected: {{ motion_detected.value }} ({{ motion_detected.sensors | join(', ') if motion_detected.sensors else 'none' }})
                • Other Detected: {{ other_detected.value }} ({{ other_detected.sensors | join(', ') if other_detected.sensors else 'none' }})
            
            ✅ Room Presence Result: {{ room_presence_detected }}
            {% if validation_mode == 'smart' and ble_detected.sensors | length > 0 and motion_detected.sensors | length == 0 %}
            ⚠️ BLE detected but no motion - likely false trigger from adjacent room
            {% endif %}
          level: warning
          logger: "blueprints.climate_control.{{ room_name | lower | replace(' ', '_') }}.presence"

  # Debug: Log current conditions
  - if:
      - condition: template
        value_template: "{{ debug_enabled }}"
    then:
      - service: system_log.write
        data:
          message: >
            📊 {{ room_name | upper }} CLIMATE CONTROL STATUS
            
            🌡️ Current Conditions:
                • Temperature: {{ current_temp }}°C {% if calculated_trend == 'rising' %}↗️ Rising{% elif calculated_trend == 'falling' %}↘️ Falling{% else %}➡️ Stable{% endif %}
                • People: {{ 'Someone home' if anyone_home else 'Nobody home' }}{% if approaching_home %} (approaching){% endif %}
                • Room Presence: {{ 'DETECTED' if room_presence_detected else 'Empty' }} (Mode: {{ presence_validation | default('any') | upper }})
                • Current Mode: {{ last_mode|title }} (running {{ time_since_change | round(0) }} min)
                • Swing: {{ state_attr(climate_list[0], 'swing_mode') | default('Unknown') | title }}
                • Control Mode: {{ control_mode }} (from: {{ control_mode_helper if control_mode_helper else 'NO HELPER' }})
            
            🎯 System Status:
                {% if temp_stability_enabled and temp_stability_detected and last_mode not in ['stability_off', 'stability_eco'] %}• Temperature stable for {{ temp_stability_time | round(0) }} min - ready for auto-off{% elif temp_stability_enabled and last_mode in ['cooling_low', 'cooling_medium', 'cooling_high', 'heating_low', 'heating_medium', 'heating_high'] %}• Continue mode active - pursuing {{ target_temp }}°C target ({{ (current_temp - target_temp) | round(1) }}°C to go){% elif last_mode in ['stability_off', 'stability_eco'] %}• AC off due to temperature stability{% elif current_temp < comfort_min_temp %}• Too cold - needs heating{% elif current_temp > comfort_max_temp %}• Too hot - needs cooling{% else %}• Perfect temperature{% endif %}
            
            📋 Technical Details:
                • Comfort Zone: {{ comfort_min_temp }}-{{ comfort_max_temp }}°C
                • Cooling Triggers: {{ cooling_low_temp }}°C (low), {{ cooling_medium_temp }}°C (medium), {{ cooling_high_temp }}°C (high)
                • Heating Triggers: {{ heating_low_temp }}°C (low), {{ heating_medium_temp }}°C (medium), {{ heating_high_temp }}°C (high)
            {% if temp_stability_enabled %}    • Stability: {{ 'ACTIVE - AC OFF' if last_mode in ['stability_off', 'stability_eco'] else 'DETECTED' if temp_stability_detected else 'Continue mode - pursuing target' if last_mode in ['cooling_low', 'cooling_medium', 'cooling_high', 'heating_low', 'heating_medium', 'heating_high'] else 'Monitoring - Outside comfort zone' if current_temp < comfort_min_temp or current_temp > comfort_max_temp else 'Monitoring' }} (±{{ stability_tolerance }}°C for {{ stability_duration }}min, target ±0.5°C){% endif %}
                • Trend: {{ temp_change_rate }}°C/min, Distance: {{ distance_from_target }}°C, Effectiveness: {{ current_effectiveness }}%
            {% if dynamic_enabled %}    • Dynamic: Escalation Level {{ escalation_level }}/4, De-escalation Level {{ deescalation_level }}/3 (Check Interval: {{ effectiveness_check_mins }}min){% endif %}
          level: warning
          logger: "blueprints.climate_control.{{ room_name | lower | replace(' ', '_') }}"
  
  # Update temperature history when starting new mode (not during continue mode)
  - condition: template
    value_template: >
      {{
        dynamic_enabled and
        helper_temp_history not in [none, '', 'unavailable', 'unknown'] and
        (
          states(helper_mode) | default('off') != last_mode or
          states(helper_mode) | default('off') == 'off' or
          last_mode == 'off'
        )
      }}
  - service: input_number.set_value
    target:
      entity_id: "{{ helper_temp_history }}"
    data:
      value: "{{ current_temp }}"

  # Update effectiveness score helper
  - condition: template
    value_template: "{{ dynamic_enabled and helper_effectiveness not in [none, '', 'unavailable', 'unknown'] }}"
  - service: input_number.set_value
    target:
      entity_id: "{{ helper_effectiveness }}"
    data:
      value: "{{ current_effectiveness }}"

  - condition: template
    value_template: "{{ dynamic_enabled and helper_trend not in [none, '', 'unavailable', 'unknown'] }}"  
  - service: input_text.set_value
    target:
      entity_id: "{{ helper_trend }}"
    data:
      value: "{{ calculated_trend }}"

  # Debug: Log the actual last_mode value to see what's happening
  - condition: template
    value_template: "{{ debug_enabled }}"
  - service: system_log.write
    data:
      message: >
        🔍 {{ room_name | upper }} DEBUG Part 1: Basic conditions
        
        📊 Core Variables:
            • last_mode (internal): "{{ last_mode }}"
            • actual_ac_state: "{{ actual_ac_state }}"
            • should_activate: {{ should_activate }}
            • current_temp: {{ current_temp }}°C
            • cooling_high: {{ cooling_high_temp }}°C
            • cooling_medium: {{ cooling_medium_temp }}°C
      level: warning
      logger: "blueprints.climate_control.{{ room_name | lower | replace(' ', '_') }}"
  
  - condition: template
    value_template: "{{ debug_enabled }}"
  - service: system_log.write
    data:
      message: >
        🔍 {{ room_name | upper }} DEBUG Part 2: Cooling checks
        
        🌡️ Temperature Logic:
            • last_transition: "{{ last_transition }}"
            • hysteresis: {{ hysteresis_tolerance }}°C
            • temp >= high: {{ current_temp >= cooling_high_temp }}
            • time_since_change: {{ time_since_change | round(1) }}min
            • runtime_min: {{ runtime_min }}min
            • time_check: {{ time_since_change > runtime_min }}
            • mode_check: {{ last_mode != 'cooling' }}
            • actual_ac_off: {{ actual_ac_state == 'off' }}
            • HIGH cooling conditions: {% if last_mode == 'cooling_high' %}{{ current_temp > target_temp }} (continue until target){% else %}{{ should_activate and current_temp >= cooling_high_temp and actual_ac_state == 'off' }} (start condition){% endif %}
      level: warning
      logger: "blueprints.climate_control.{{ room_name | lower | replace(' ', '_') }}"

  - condition: template
    value_template: "{{ debug_enabled }}"
  - service: system_log.write
    data:
      message: >
        🔍 {{ room_name | upper }} DEBUG Part 3: Heating & Cooling checks
        
        🔥 Heating Logic:
            • heating_low: {{ heating_low_temp }}°C
            • heating_medium: {{ heating_medium_temp }}°C
            • heating_high: {{ heating_high_temp }}°C
            • temp <= low: {{ current_temp <= heating_low_temp }}
            • temp <= medium: {{ current_temp <= heating_medium_temp }}
            • temp <= high: {{ current_temp <= heating_high_temp }}
            • time_check: {{ time_since_change > runtime_min }} ({{ time_since_change | round(1) }}min > {{ runtime_min }}min)
            • mode_check: {{ last_mode != 'heating' }} (last_mode: {{ last_mode }})
            • runtime_lockout: {{ 'ACTIVE - preventing activation' if time_since_change <= runtime_min and last_mode == 'heating' else 'inactive' }}
            • actual_ac_off: {{ actual_ac_state == 'off' }}
            • Heating Mode: {{ 'ENABLED' if enable_heating else 'DISABLED' }}
            • LOW heating conditions: {% if not enable_heating %}DISABLED{% elif last_mode == 'heating_low' %}{{ current_temp < target_temp and current_temp > heating_medium_temp }} (continue until target){% else %}{{ should_activate and enable_heating and current_temp <= heating_low_temp and current_temp > heating_medium_temp and (time_since_change > runtime_min or last_mode != 'heating_low') }} (start condition){% endif %}
            • MEDIUM heating conditions: {% if not enable_heating %}DISABLED{% elif last_mode == 'heating_medium' %}{{ current_temp < target_temp and current_temp > heating_high_temp }} (continue until target){% else %}{{ should_activate and enable_heating and current_temp <= heating_medium_temp and current_temp > heating_high_temp and (time_since_change > runtime_min or last_mode != 'heating_medium') }} (start condition){% endif %}
            • HIGH heating conditions: {% if not enable_heating %}DISABLED{% elif last_mode == 'heating_high' %}{{ current_temp < target_temp }} (continue until target){% else %}{{ should_activate and enable_heating and current_temp <= heating_high_temp and (time_since_change > runtime_min or last_mode != 'heating_high') }} (start condition){% endif %}
        
        ❄️ Cooling Logic:
            • cooling_low: {{ cooling_low_temp }}°C
            • cooling_medium: {{ cooling_medium_temp }}°C
            • cooling_high: {{ cooling_high_temp }}°C
            • temp >= low: {{ current_temp >= cooling_low_temp }}
            • temp >= medium: {{ current_temp >= cooling_medium_temp }}
            • temp >= high: {{ current_temp >= cooling_high_temp }}
            • time_check: {{ time_since_change > runtime_min }} ({{ time_since_change | round(1) }}min > {{ runtime_min }}min)
            • mode_check: {{ last_mode != 'cooling' }} (last_mode: {{ last_mode }})
            • runtime_lockout: {{ 'ACTIVE - preventing activation' if time_since_change <= runtime_min and last_mode == 'cooling' else 'inactive' }}
            • actual_ac_off: {{ actual_ac_state == 'off' }}
            • Cooling Mode: {{ 'ENABLED' if enable_cooling else 'DISABLED' }}
            • LOW cooling conditions: {% if not enable_cooling %}DISABLED{% elif last_mode == 'cooling_low' %}{{ current_temp > target_temp and current_temp < cooling_medium_temp }} (continue until target){% else %}{{ should_activate and enable_cooling and current_temp >= cooling_low_temp and current_temp < cooling_medium_temp and (time_since_change > runtime_min or last_mode != 'cooling_low') }} (start condition){% endif %}
            • MEDIUM cooling conditions: {% if not enable_cooling %}DISABLED{% elif last_mode == 'cooling_medium' %}{{ current_temp > target_temp and current_temp < cooling_high_temp }} (continue until target){% else %}{{ should_activate and enable_cooling and current_temp >= cooling_medium_temp and current_temp < cooling_high_temp and (time_since_change > runtime_min or last_mode != 'cooling_medium') }} (start condition){% endif %}
            • HIGH cooling conditions: {% if not enable_cooling %}DISABLED{% elif last_mode == 'cooling_high' %}{{ current_temp > target_temp }} (continue until target){% else %}{{ should_activate and enable_cooling and current_temp >= cooling_high_temp and actual_ac_state == 'off' }} (start condition){% endif %}
      level: warning
      logger: "blueprints.climate_control.{{ room_name | lower | replace(' ', '_') }}"

  # ADAPTIVE CONTROL REACTIVATION: Clear adaptive_off when room is occupied and heating/cooling needed
  - if:
      - condition: template
        value_template: "{{ last_mode == 'adaptive_off' and adaptive_enabled and control_mode == 'Smart' }}"
      - condition: template
        value_template: "{{ room_presence_detected }}"
      - condition: template
        value_template: >
          {% if presence_detected_helper and states(presence_detected_helper) not in ['unknown', 'unavailable', ''] %}
            {% set presence_time = (as_timestamp(now()) - as_timestamp(states(presence_detected_helper))) / 60 %}
            {{ presence_time >= adaptive_occupied_delay }}
          {% else %}
            false
          {% endif %}
      - condition: template
        value_template: "{{ current_temp <= heating_medium_temp or current_temp >= cooling_medium_temp }}"
    then:
      - service: system_log.write
        data:
          message: >
            🔄 {{ room_name | upper }} ADAPTIVE REACTIVATION
            
            ✅ Conditions Met:
                • Was in adaptive_off mode
                • Room occupied for {{ ((as_timestamp(now()) - as_timestamp(states(presence_detected_helper))) / 60) | round(1) }}+ minutes
                • Temperature {{ current_temp }}°C requires action ({{ 'heating' if current_temp <= heating_medium_temp else 'cooling' }})
            
            🎯 Resuming normal Smart mode climate control
          level: warning
          logger: "blueprints.climate_control.{{ room_name | lower | replace(' ', '_') }}"
      
      # Clear adaptive_off mode - allow normal logic to proceed
      - service: input_text.set_value
        target:
          entity_id: !input helper_last_mode
        data:
          value: "off"
      
      # Reset mode start time to allow immediate evaluation in next cycle (only if mode changed)
      - condition: template
        value_template: "{{ helper_mode_time not in [none, '', 'unavailable', 'unknown'] and last_mode != 'off' }}"
      - service: input_datetime.set_datetime
        target:
          entity_id: "{{ helper_mode_time }}"
        data:
          datetime: "{{ now() }}"

  - choose:
      # Handle Manual mode - True manual control with optional timeout
      - conditions:
          - condition: template
            value_template: "{{ control_mode == 'Manual' }}"
        sequence:
          # Check if Manual mode timeout has been reached
          - if:
              - condition: template
                value_template: "{{ manual_timeout_hours > 0 }}"
              - condition: template
                value_template: >
                  {% if helper_change and states(helper_change) not in ['unknown', 'unavailable', ''] %}
                    {% set time_in_manual = (as_timestamp(now()) - as_timestamp(states(helper_change))) / 3600 %}
                    {{ time_in_manual >= manual_timeout_hours }}
                  {% else %}
                    false
                  {% endif %}
            then:
              - service: system_log.write
                data:
                  message: >
                    ⏰ {{ room_name | upper }} MANUAL MODE TIMEOUT: Returning to automation
                    • Manual timeout reached: {{ manual_timeout_hours }} hours
                    • Time in manual: {{ ((as_timestamp(now()) - as_timestamp(states(helper_change))) / 3600) | round(1) }}h
                    • Switching back to Smart mode - automation will resume
                    • AC will continue running under normal automation control
                  level: warning
                  logger: "blueprints.climate_control.{{ room_name | lower | replace(' ', '_') }}"

              # Switch back to Smart mode (don't turn off AC)
              - condition: template
                value_template: "{{ control_mode_helper not in [none, '', 'unavailable', 'unknown'] }}"
              - service: input_select.select_option
                target:
                  entity_id: "{{ control_mode_helper }}"
                data:
                  option: "Smart"
                continue_on_error: true

              - service: input_text.set_value
                target:
                  entity_id: !input helper_last_mode
                data:
                  value: "manual_timeout_return_to_smart"
                continue_on_error: true

              - condition: template
                value_template: "{{ enable_notifications }}"
              - service: !input notification_service
                data:
                  title: "{{ room_name }} Climate Control"
                  message: "Manual timeout ({{ manual_timeout_hours }}h) - switched back to Smart mode"
              - stop: "Manual mode timeout - returned to Smart mode"
            else:
              # Manual mode continues - complete user control
              - condition: template
                value_template: "{{ debug_enabled }}"
              - service: system_log.write
                data:
                  message: >
                    🎮 {{ room_name | upper }} MANUAL MODE: Full user control active
                    • Temperature: {{ current_temp }}°C
                    • Timeout setting: {{ manual_mode_timeout }} ({{ manual_timeout_hours }}h)
                    {% if manual_timeout_hours > 0 %}
                    • Time remaining: {{ (manual_timeout_hours - ((as_timestamp(now()) - as_timestamp(states(helper_change))) / 3600)) | round(1) }}h
                    {% else %}
                    • No timeout - manual until you change it
                    {% endif %}
                    • FULL CONTROL: Set any temp/fan remotely - NO automation interference
                    • Perfect for pre-conditioning rooms before arrival!
                  level: debug
                  logger: "blueprints.climate_control.{{ room_name | lower | replace(' ', '_') }}"
              - stop: "Manual mode active - user has complete control"

      # Skip if manual override is active (user changed AC while in Auto/Smart) - NEVER for mode changes
      - conditions:
          - condition: template
            value_template: "{{ manual_override_active and manual_change_detected }}"
        sequence:
          - condition: template
            value_template: "{{ debug_enabled }}"
          - service: system_log.write
            data:
              message: >
                🔒 {{ room_name | upper }} MANUAL OVERRIDE: Respecting user changes
                • Manual change detected: {{ manual_change_detected }}
                • Last mode state: "{{ states(helper_mode) | default('off') }}"
                • Actual AC state: "{{ actual_ac_state }}"
                • Override timeout: {{ adaptive_override_timeout }} hours
                • Time remaining: {{ (adaptive_override_timeout - ((as_timestamp(now()) - as_timestamp(states(helper_change))) / 3600)) | round(1) }} hours
                • AC will maintain user settings until timeout expires
              level: warning
              logger: "blueprints.climate_control.{{ room_name | lower | replace(' ', '_') }}"

          # STOP HERE - Respect user's manual changes
          - stop: "Manual override active - respecting user changes"

      # HIGH HEATING: Smart hysteresis with direct helper reads
      - conditions:
          - condition: template
            value_template: >
              {% set current_mode = states(helper_mode) | default('off') %}
              {% if current_mode == 'heating_high' %}
                {# Already heating high - continue until target reached #}
                {{ current_temp < target_temp }}
              {% else %}
                {# Not heating high - start if threshold exceeded and conditions met #}
                {{ should_activate and enable_heating and current_temp <= heating_high_temp }}
              {% endif %}
        sequence:
          - service: system_log.write
            data:
              message: >
                {% set current_mode = states(helper_mode) | default('off') %}
                🔥 {{ room_name | upper }} HIGH HEATING ACTIVATED
                {% if current_mode == 'heating_high' %}
                📊 CONTINUE MODE: Already heating high, continuing until target
                • Current: {{ current_temp }}°C → Target: {{ target_temp }}°C ({{ (target_temp - current_temp) | round(1) }}°C to go)
                {% else %}
                🚀 START MODE: Starting HIGH heating 
                • Trigger: {{ current_temp }}°C ≤ {{ heating_high_temp }}°C (threshold)
                • Target: {{ target_temp }}°C ({{ (target_temp - current_temp) | round(1) }}°C to heat)
                {% endif %}
                • Last Mode: {{ current_mode }}
              level: warning
              logger: "blueprints.climate_control.{{ room_name | lower | replace(' ', '_') }}"
          - service: climate.set_hvac_mode
            target:
              entity_id: !input climate_entities
            data:
              hvac_mode: "heat"
          - service: climate.set_temperature
            target:
              entity_id: !input climate_entities
            data:
              temperature: "{{ target_temp }}"

          # HIGH heating always uses maximum fan speed (no escalation beyond this tier)
          - repeat:
              for_each: "{{ climate_list }}"
              sequence:
                - variables:
                    current_entity: "{{ repeat.item }}"
                    available_fans: "{{ state_attr(repeat.item, 'fan_modes') | list }}"

                    # HIGH heating maximum fan selection
                    selected_fan_mode: >
                      {% set fans = state_attr(repeat.item, 'fan_modes') | list %}
                      {% set desired_max_fan = fan_speed_max %}
                      {% if desired_max_fan in fans %}
                        {{ desired_max_fan }}
                      {% elif '5' in fans %}
                        5
                      {% elif 'Level 5' in fans %}
                        Level 5
                      {% elif 'turbo' in fans %}
                        turbo
                      {% elif 'Turbo' in fans %}
                        Turbo
                      {% elif 'max' in fans %}
                        max
                      {% elif 'Max' in fans %}
                        Max
                      {% elif 'high' in fans %}
                        high
                      {% elif 'High' in fans %}
                        High
                      {% elif 'Auto' in fans %}
                        Auto
                      {% elif 'auto' in fans %}
                        auto
                      {% else %}
                        {{ fans[-1] if fans else 'Auto' }}
                      {% endif %}

                - service: climate.set_fan_mode
                  target:
                    entity_id: "{{ current_entity }}"
                  data:
                    fan_mode: "{{ selected_fan_mode }}"

                - condition: template
                  value_template: "{{ debug_enabled }}"
                - service: system_log.write
                  data:
                    message: >
                      🔍 HIGH HEAT MAXIMUM FAN: {{ repeat.item }}
                      • Escalation Level: {{ escalation_level }}/4 (max tier - Level 5 always)
                      • Fan Selection: {{ selected_fan_mode }} (maximum power)
                      • Effectiveness: {{ current_effectiveness }}% after {{ time_in_current_mode | round(1) }}min
                      • Distance from Target: {{ distance_from_target | round(1) }}°C
                      • Tier Reason: HIGH tier = emergency response, no escalation beyond Level 5
                      • Available Fans: {{ available_fans | join(', ') }}
                    level: debug
                    logger: "blueprints.climate_control.{{ room_name | lower | replace(' ', '_') }}"
          # Only update helper entities when starting new mode (not continuing)
          - condition: template
            value_template: "{{ states(helper_mode) | default('off') != 'heating_high' }}"
          - service: input_text.set_value
            target:
              entity_id: !input helper_last_mode
            data:
              value: "heating_high"
          - service: input_datetime.set_datetime
            target:
              entity_id: !input helper_last_change
            data:
              datetime: "{{ now() }}"

      # MEDIUM HEATING: Smart hysteresis with direct helper reads
      - conditions:
          - condition: template
            value_template: >
              {% set current_mode = states(helper_mode) | default('off') %}
              {% if current_mode == 'heating_medium' %}
                {# Already heating medium - continue until target reached or escalation needed #}
                {{ current_temp < target_temp and current_temp > heating_high_temp }}
              {% else %}
                {# Not heating medium - start if in medium range and conditions met #}
                {{ should_activate and enable_heating and current_temp <= heating_medium_temp and current_temp > heating_high_temp }}
              {% endif %}
        sequence:
          - service: system_log.write
            data:
              message: >
                {% set current_mode = states(helper_mode) | default('off') %}
                🔥 {{ room_name | upper }} MEDIUM HEATING ACTIVATED
                {% if current_mode == 'heating_medium' %}
                📊 CONTINUE MODE: Already heating medium, continuing until target
                • Current: {{ current_temp }}°C → Target: {{ target_temp }}°C ({{ (target_temp - current_temp) | round(1) }}°C to go)
                {% else %}
                🚀 START MODE: Starting MEDIUM heating
                • Trigger: {{ current_temp }}°C ≤ {{ heating_medium_temp }}°C (threshold)
                • Target: {{ target_temp }}°C ({{ (target_temp - current_temp) | round(1) }}°C to heat)
                {% endif %}
                • Last Mode: {{ current_mode }}
              level: warning
              logger: "blueprints.climate_control.{{ room_name | lower | replace(' ', '_') }}"
          - service: climate.set_hvac_mode
            target:
              entity_id: !input climate_entities
            data:
              hvac_mode: "heat"
          - service: climate.set_temperature
            target:
              entity_id: !input climate_entities
            data:
              temperature: "{{ target_temp }}"

          # Progressive fan speed for MEDIUM heating based on escalation level
          - repeat:
              for_each: "{{ climate_list }}"
              sequence:
                - variables:
                    current_entity: "{{ repeat.item }}"
                    available_fans: "{{ state_attr(repeat.item, 'fan_modes') | list }}"

                    # MEDIUM heating progressive fan selection (Level 3 → Level 4 → Level 5)
                    selected_fan_mode: >
                      {% set fans = state_attr(repeat.item, 'fan_modes') | list %}
                      {% if escalation_level == 0 %}
                        {# Level 0: Start moderate - Level 3 equivalent #}
                        {% set desired_medium_fan = fan_speed_medium %}
                        {% if desired_medium_fan in fans %}
                          {{ desired_medium_fan }}
                        {% elif '3' in fans %}
                          3
                        {% elif 'Level 3' in fans %}
                          Level 3
                        {% elif 'medium' in fans %}
                          medium
                        {% elif 'Medium' in fans %}
                          Medium
                        {% elif 'Auto' in fans %}
                          Auto
                        {% elif 'auto' in fans %}
                          auto
                        {% else %}
                          {{ fans[0] if fans else 'Auto' }}
                        {% endif %}
                      {% elif escalation_level == 1 %}
                        {# Level 1: Higher escalation - Level 4 equivalent #}
                        {% if '4' in fans %}
                          4
                        {% elif 'Level 4' in fans %}
                          Level 4
                        {% elif 'high' in fans %}
                          high
                        {% elif 'High' in fans %}
                          High
                        {% elif '5' in fans %}
                          5
                        {% elif 'Level 5' in fans %}
                          Level 5
                        {% elif 'auto' in fans %}
                          auto
                        {% elif 'Auto' in fans %}
                          Auto
                        {% else %}
                          {{ fans[-2] if fans|length > 1 else fans[-1] }}
                        {% endif %}
                      {% else %}
                        {# Level 2+: Maximum for MEDIUM tier - Level 5 equivalent #}
                        {% set desired_max_fan = fan_speed_max %}
                        {% if desired_max_fan in fans %}
                          {{ desired_max_fan }}
                        {% elif '5' in fans %}
                          5
                        {% elif 'Level 5' in fans %}
                          Level 5
                        {% elif 'high' in fans %}
                          high
                        {% elif 'High' in fans %}
                          High
                        {% elif 'turbo' in fans %}
                          turbo
                        {% elif 'Turbo' in fans %}
                          Turbo
                        {% elif 'max' in fans %}
                          max
                        {% elif 'Max' in fans %}
                          Max
                        {% else %}
                          {{ fans[-1] if fans else 'Auto' }}
                        {% endif %}
                      {% endif %}

                - service: climate.set_fan_mode
                  target:
                    entity_id: "{{ current_entity }}"
                  data:
                    fan_mode: "{{ selected_fan_mode }}"

                - condition: template
                  value_template: "{{ debug_enabled }}"
                - service: system_log.write
                  data:
                    message: >
                      🔍 MEDIUM HEAT ADAPTIVE FAN: {{ repeat.item }}
                      • Escalation Level: {{ escalation_level }}/4
                      • Fan Selection: {{ selected_fan_mode }}
                      • Effectiveness: {{ current_effectiveness }}% after {{ time_in_current_mode | round(1) }}min
                      • Distance from Target: {{ distance_from_target | round(1) }}°C
                      • Escalation Reason: {% if escalation_level == 0 %}Working well - no escalation needed{% elif escalation_level == 1 %}Moderate effectiveness - escalate to Level 4{% elif escalation_level == 2 %}Poor effectiveness or distance - escalate to Level 5{% else %}Emergency - maximum escalation{% endif %}
                      • Available Fans: {{ available_fans | join(', ') }}
                    level: debug
                    logger: "blueprints.climate_control.{{ room_name | lower | replace(' ', '_') }}"
          # Only update helper entities when starting new mode (not continuing)
          - condition: template
            value_template: "{{ states(helper_mode) | default('off') != 'heating_medium' }}"
          - service: input_text.set_value
            target:
              entity_id: !input helper_last_mode
            data:
              value: "heating_medium"
          - service: input_datetime.set_datetime
            target:
              entity_id: !input helper_last_change
            data:
              datetime: "{{ now() }}"

      # LOW HEATING: Smart hysteresis with direct helper reads
      - conditions:
          - condition: template
            value_template: >
              {% set current_mode = states(helper_mode) | default('off') %}
              {% if current_mode == 'heating_low' %}
                {# Already heating low - continue until target reached or escalation needed #}
                {{ current_temp < target_temp and current_temp > heating_medium_temp }}
              {% else %}
                {# Not heating low - start if in low range and conditions met #}
                {{ should_activate and enable_heating and current_temp <= heating_low_temp and current_temp > heating_medium_temp }}
              {% endif %}
        sequence:
          - service: system_log.write
            data:
              message: >
                {% set current_mode = states(helper_mode) | default('off') %}
                🔥 {{ room_name | upper }} LOW HEATING ACTIVATED
                {% if current_mode == 'heating_low' %}
                📊 CONTINUE MODE: Already heating low, continuing until target
                • Current: {{ current_temp }}°C → Target: {{ target_temp }}°C ({{ (target_temp - current_temp) | round(1) }}°C to go)
                {% else %}
                🚀 START MODE: Starting LOW heating
                • Trigger: {{ current_temp }}°C ≤ {{ heating_low_temp }}°C (threshold)
                • Target: {{ target_temp }}°C ({{ (target_temp - current_temp) | round(1) }}°C to heat)
                {% endif %}
                • Last Mode: {{ current_mode }}
              level: warning
              logger: "blueprints.climate_control.{{ room_name | lower | replace(' ', '_') }}"
          - service: climate.set_hvac_mode
            target:
              entity_id: !input climate_entities
            data:
              hvac_mode: "heat"
          - service: climate.set_temperature
            target:
              entity_id: !input climate_entities
            data:
              temperature: "{{ target_temp }}"

          # Progressive fan speed for LOW heating based on escalation level
          - repeat:
              for_each: "{{ climate_list }}"
              sequence:
                - variables:
                    current_entity: "{{ repeat.item }}"
                    available_fans: "{{ state_attr(repeat.item, 'fan_modes') | list }}"

                    # LOW heating progressive fan selection (Level 1 → Level 2 → Level 3)
                    selected_fan_mode: >
                      {% set fans = state_attr(repeat.item, 'fan_modes') | list %}
                      {% if escalation_level == 0 %}
                        {# Level 0: Start gentle - Level 1 equivalent #}
                        {% if 'low' in fans %}
                          low
                        {% elif 'Low' in fans %}
                          Low
                        {% elif '1' in fans %}
                          1
                        {% elif 'Level 1' in fans %}
                          Level 1
                        {% elif 'quiet' in fans %}
                          quiet
                        {% elif 'Quiet' in fans %}
                          Quiet
                        {% elif 'Auto' in fans %}
                          Auto
                        {% elif 'auto' in fans %}
                          auto
                        {% else %}
                          {{ fans[0] if fans else 'Auto' }}
                        {% endif %}
                      {% elif escalation_level == 1 %}
                        {# Level 1: Moderate escalation - Level 2 equivalent #}
                        {% if '2' in fans %}
                          2
                        {% elif 'Level 2' in fans %}
                          Level 2
                        {% elif 'medium' in fans %}
                          medium
                        {% elif 'Medium' in fans %}
                          Medium
                        {% elif '3' in fans %}
                          3
                        {% elif 'Level 3' in fans %}
                          Level 3
                        {% elif 'auto' in fans %}
                          auto
                        {% elif 'Auto' in fans %}
                          Auto
                        {% else %}
                          {{ fans[1] if fans|length > 1 else fans[0] }}
                        {% endif %}
                      {% else %}
                        {# Level 2+: Maximum for LOW tier - Level 3 equivalent #}
                        {% if '3' in fans %}
                          3
                        {% elif 'Level 3' in fans %}
                          Level 3
                        {% elif 'medium' in fans %}
                          medium
                        {% elif 'Medium' in fans %}
                          Medium
                        {% elif 'auto' in fans %}
                          auto
                        {% elif 'Auto' in fans %}
                          Auto
                        {% else %}
                          {{ fans[-1] if fans else 'Auto' }}
                        {% endif %}
                      {% endif %}

                - service: climate.set_fan_mode
                  target:
                    entity_id: "{{ current_entity }}"
                  data:
                    fan_mode: "{{ selected_fan_mode }}"

                - condition: template
                  value_template: "{{ debug_enabled }}"
                - service: system_log.write
                  data:
                    message: >
                      🔍 LOW HEAT ADAPTIVE FAN: {{ repeat.item }}
                      • Escalation Level: {{ escalation_level }}/4
                      • Fan Selection: {{ selected_fan_mode }}
                      • Effectiveness: {{ current_effectiveness }}% after {{ time_in_current_mode | round(1) }}min
                      • Distance from Target: {{ distance_from_target | round(1) }}°C
                      • Escalation Reason: {% if escalation_level == 0 %}Working well - no escalation needed{% elif escalation_level == 1 %}Moderate effectiveness - minor escalation{% elif escalation_level == 2 %}Poor effectiveness or distance - moderate escalation{% elif escalation_level == 3 %}Stalled progress - high escalation{% elif escalation_level == 4 %}Emergency - maximum escalation{% else %}Unknown level{% endif %}
                      • Available Fans: {{ available_fans | join(', ') }}
                    level: debug
                    logger: "blueprints.climate_control.{{ room_name | lower | replace(' ', '_') }}"
          # Only update helper entities when starting new mode (not continuing)
          - condition: template
            value_template: "{{ states(helper_mode) | default('off') != 'heating_low' }}"
          - service: input_text.set_value
            target:
              entity_id: !input helper_last_mode
            data:
              value: "heating_low"
          - service: input_datetime.set_datetime
            target:
              entity_id: !input helper_last_change
            data:
              datetime: "{{ now() }}"

      # HIGH COOLING: Smart hysteresis with direct helper reads
      - conditions:
          - condition: template
            value_template: >
              {% set current_mode = states(helper_mode) | default('off') %}
              {% if current_mode == 'cooling_high' %}
                {# Already cooling high - continue until target reached #}
                {{ current_temp > target_temp }}
              {% else %}
                {# Not cooling high - start if threshold exceeded and conditions met #}
                {{ should_activate and enable_cooling and current_temp >= cooling_high_temp }}
              {% endif %}
        sequence:
          - service: system_log.write
            data:
              message: "❄️ {{ room_name | upper }} HIGH COOLING ACTIVATED - Temperature {{ current_temp }}°C above {{ cooling_high_temp }}°C (high threshold)"
              level: warning
              logger: "blueprints.climate_control.{{ room_name | lower | replace(' ', '_') }}"
          - service: climate.set_hvac_mode
            target:
              entity_id: !input climate_entities
            data:
              hvac_mode: "cool"
          - service: climate.set_temperature
            target:
              entity_id: !input climate_entities
            data:
              temperature: "{{ target_temp }}"

          # HIGH cooling always uses maximum fan speed (no escalation beyond this tier)
          - repeat:
              for_each: "{{ climate_list }}"
              sequence:
                - variables:
                    current_entity: "{{ repeat.item }}"
                    available_fans: "{{ state_attr(repeat.item, 'fan_modes') | list }}"

                    # HIGH cooling maximum fan selection
                    selected_fan_mode: >
                      {% set fans = state_attr(repeat.item, 'fan_modes') | list %}
                      {% set desired_max_fan = fan_speed_max %}
                      {% if desired_max_fan in fans %}
                        {{ desired_max_fan }}
                      {% elif '5' in fans %}
                        5
                      {% elif 'Level 5' in fans %}
                        Level 5
                      {% elif 'turbo' in fans %}
                        turbo
                      {% elif 'Turbo' in fans %}
                        Turbo
                      {% elif 'max' in fans %}
                        max
                      {% elif 'Max' in fans %}
                        Max
                      {% elif 'high' in fans %}
                        high
                      {% elif 'High' in fans %}
                        High
                      {% elif 'Auto' in fans %}
                        Auto
                      {% elif 'auto' in fans %}
                        auto
                      {% else %}
                        {{ fans[-1] if fans else 'Auto' }}
                      {% endif %}

                - service: climate.set_fan_mode
                  target:
                    entity_id: "{{ current_entity }}"
                  data:
                    fan_mode: "{{ selected_fan_mode }}"

                - condition: template
                  value_template: "{{ debug_enabled }}"
                - service: system_log.write
                  data:
                    message: >
                      🔍 HIGH COOL MAXIMUM FAN: {{ repeat.item }}
                      • Escalation Level: {{ escalation_level }}/4 (max tier - Level 5 always)
                      • Fan Selection: {{ selected_fan_mode }} (maximum power)
                      • Effectiveness: {{ current_effectiveness }}% after {{ time_in_current_mode | round(1) }}min
                      • Distance from Target: {{ distance_from_target | round(1) }}°C
                      • Tier Reason: HIGH tier = emergency response, no escalation beyond Level 5
                      • Available Fans: {{ available_fans | join(', ') }}
                    level: debug
                    logger: "blueprints.climate_control.{{ room_name | lower | replace(' ', '_') }}"
          # Only update helper entities when starting new mode (not continuing)
          - condition: template
            value_template: "{{ states(helper_mode) | default('off') != 'cooling_high' }}"
          - service: input_text.set_value
            target:
              entity_id: !input helper_last_mode
            data:
              value: "cooling_high"
          - service: input_datetime.set_datetime
            target:
              entity_id: !input helper_last_change
            data:
              datetime: "{{ now() }}"

      # MEDIUM COOLING: Smart hysteresis with direct helper reads
      - conditions:
          - condition: template
            value_template: >
              {% set current_mode = states(helper_mode) | default('off') %}
              {% if current_mode == 'cooling_medium' %}
                {# Already cooling medium - continue until target reached or escalation needed #}
                {{ current_temp > target_temp and current_temp < cooling_high_temp }}
              {% else %}
                {# Not cooling medium - start if in medium range and conditions met #}
                {{ should_activate and enable_cooling and current_temp >= cooling_medium_temp and current_temp < cooling_high_temp }}
              {% endif %}
        sequence:
          - service: system_log.write
            data:
              message: >
                {% set current_mode = states(helper_mode) | default('off') %}
                ❄️ {{ room_name | upper }} MEDIUM COOLING ACTIVATED
                {% if current_mode == 'cooling_medium' %}
                📊 CONTINUE MODE: Already cooling medium, continuing until target
                • Current: {{ current_temp }}°C → Target: {{ target_temp }}°C ({{ (current_temp - target_temp) | round(1) }}°C to go)
                {% else %}
                🚀 START MODE: Starting MEDIUM cooling
                • Trigger: {{ current_temp }}°C ≥ {{ cooling_medium_temp }}°C (threshold)
                • Target: {{ target_temp }}°C ({{ (current_temp - target_temp) | round(1) }}°C to cool)
                {% endif %}
                • Last Mode: {{ current_mode }}
              level: warning
              logger: "blueprints.climate_control.{{ room_name | lower | replace(' ', '_') }}"
          - service: climate.set_hvac_mode
            target:
              entity_id: !input climate_entities
            data:
              hvac_mode: "cool"
          - service: climate.set_temperature
            target:
              entity_id: !input climate_entities
            data:
              temperature: "{{ target_temp }}"

          # Progressive fan speed for MEDIUM cooling based on escalation level
          - repeat:
              for_each: "{{ climate_list }}"
              sequence:
                - variables:
                    current_entity: "{{ repeat.item }}"
                    available_fans: "{{ state_attr(repeat.item, 'fan_modes') | list }}"

                    # MEDIUM cooling progressive fan selection (Level 3 → Level 4 → Level 5)
                    selected_fan_mode: >
                      {% set fans = state_attr(repeat.item, 'fan_modes') | list %}
                      {% if escalation_level == 0 %}
                        {# Level 0: Start moderate - Level 3 equivalent #}
                        {% set desired_medium_fan = fan_speed_medium %}
                        {% if desired_medium_fan in fans %}
                          {{ desired_medium_fan }}
                        {% elif '3' in fans %}
                          3
                        {% elif 'Level 3' in fans %}
                          Level 3
                        {% elif 'medium' in fans %}
                          medium
                        {% elif 'Medium' in fans %}
                          Medium
                        {% elif 'Auto' in fans %}
                          Auto
                        {% elif 'auto' in fans %}
                          auto
                        {% else %}
                          {{ fans[0] if fans else 'Auto' }}
                        {% endif %}
                      {% elif escalation_level == 1 %}
                        {# Level 1: Higher escalation - Level 4 equivalent #}
                        {% if '4' in fans %}
                          4
                        {% elif 'Level 4' in fans %}
                          Level 4
                        {% elif 'high' in fans %}
                          high
                        {% elif 'High' in fans %}
                          High
                        {% elif '5' in fans %}
                          5
                        {% elif 'Level 5' in fans %}
                          Level 5
                        {% elif 'auto' in fans %}
                          auto
                        {% elif 'Auto' in fans %}
                          Auto
                        {% else %}
                          {{ fans[-2] if fans|length > 1 else fans[-1] }}
                        {% endif %}
                      {% else %}
                        {# Level 2+: Maximum for MEDIUM tier - Level 5 equivalent #}
                        {% set desired_max_fan = fan_speed_max %}
                        {% if desired_max_fan in fans %}
                          {{ desired_max_fan }}
                        {% elif '5' in fans %}
                          5
                        {% elif 'Level 5' in fans %}
                          Level 5
                        {% elif 'high' in fans %}
                          high
                        {% elif 'High' in fans %}
                          High
                        {% elif 'turbo' in fans %}
                          turbo
                        {% elif 'Turbo' in fans %}
                          Turbo
                        {% elif 'max' in fans %}
                          max
                        {% elif 'Max' in fans %}
                          Max
                        {% else %}
                          {{ fans[-1] if fans else 'Auto' }}
                        {% endif %}
                      {% endif %}

                - service: climate.set_fan_mode
                  target:
                    entity_id: "{{ current_entity }}"
                  data:
                    fan_mode: "{{ selected_fan_mode }}"

                - condition: template
                  value_template: "{{ debug_enabled }}"
                - service: system_log.write
                  data:
                    message: >
                      🔍 MEDIUM COOL ADAPTIVE FAN: {{ repeat.item }}
                      • Escalation Level: {{ escalation_level }}/4
                      • Fan Selection: {{ selected_fan_mode }}
                      • Effectiveness: {{ current_effectiveness }}% after {{ time_in_current_mode | round(1) }}min
                      • Distance from Target: {{ distance_from_target | round(1) }}°C
                      • Escalation Reason: {% if escalation_level == 0 %}Working well - no escalation needed{% elif escalation_level == 1 %}Moderate effectiveness - escalate to Level 4{% elif escalation_level == 2 %}Poor effectiveness or distance - escalate to Level 5{% else %}Emergency - maximum escalation{% endif %}
                      • Available Fans: {{ available_fans | join(', ') }}
                    level: debug
                    logger: "blueprints.climate_control.{{ room_name | lower | replace(' ', '_') }}"
          # Only update helper entities when starting new mode (not continuing)
          - condition: template
            value_template: "{{ states(helper_mode) | default('off') != 'cooling_medium' }}"
          - service: input_text.set_value
            target:
              entity_id: !input helper_last_mode
            data:
              value: "cooling_medium"
          - service: input_datetime.set_datetime
            target:
              entity_id: !input helper_last_change
            data:
              datetime: "{{ now() }}"

      # LOW COOLING: Smart hysteresis with direct helper reads
      - conditions:
          - condition: template
            value_template: >
              {% set current_mode = states(helper_mode) | default('off') %}
              {% if current_mode == 'cooling_low' %}
                {# Already cooling low - continue until target reached or escalation needed #}
                {{ current_temp > target_temp and current_temp < cooling_medium_temp }}
              {% else %}
                {# Not cooling low - start if in low range and conditions met #}
                {{ should_activate and enable_cooling and current_temp >= cooling_low_temp and current_temp < cooling_medium_temp }}
              {% endif %}
        sequence:
          - service: system_log.write
            data:
              message: >
                {% set current_mode = states(helper_mode) | default('off') %}
                ❄️ {{ room_name | upper }} LOW COOLING ACTIVATED
                {% if current_mode == 'cooling_low' %}
                📊 CONTINUE MODE: Already cooling low, continuing until target
                • Current: {{ current_temp }}°C → Target: {{ target_temp }}°C ({{ (current_temp - target_temp) | round(1) }}°C to go)
                {% else %}
                🚀 START MODE: Starting LOW cooling
                • Trigger: {{ current_temp }}°C ≥ {{ cooling_low_temp }}°C (threshold)
                • Target: {{ target_temp }}°C ({{ (current_temp - target_temp) | round(1) }}°C to cool)
                {% endif %}
                • Last Mode: {{ current_mode }}
              level: warning
              logger: "blueprints.climate_control.{{ room_name | lower | replace(' ', '_') }}"
          - service: climate.set_hvac_mode
            target:
              entity_id: !input climate_entities
            data:
              hvac_mode: "cool"
          - service: climate.set_temperature
            target:
              entity_id: !input climate_entities
            data:
              temperature: "{{ target_temp }}"

          # Progressive fan speed for LOW cooling based on escalation level
          - repeat:
              for_each: "{{ climate_list }}"
              sequence:
                - variables:
                    current_entity: "{{ repeat.item }}"
                    available_fans: "{{ state_attr(repeat.item, 'fan_modes') | list }}"

                    # LOW cooling progressive fan selection (Level 1 → Level 2 → Level 3)
                    selected_fan_mode: >
                      {% set fans = state_attr(repeat.item, 'fan_modes') | list %}
                      {% if escalation_level == 0 %}
                        {# Level 0: Start gentle - Level 1 equivalent #}
                        {% if 'low' in fans %}
                          low
                        {% elif 'Low' in fans %}
                          Low
                        {% elif '1' in fans %}
                          1
                        {% elif 'Level 1' in fans %}
                          Level 1
                        {% elif 'quiet' in fans %}
                          quiet
                        {% elif 'Quiet' in fans %}
                          Quiet
                        {% elif 'Auto' in fans %}
                          Auto
                        {% elif 'auto' in fans %}
                          auto
                        {% else %}
                          {{ fans[0] if fans else 'Auto' }}
                        {% endif %}
                      {% elif escalation_level == 1 %}
                        {# Level 1: Moderate escalation - Level 2 equivalent #}
                        {% if '2' in fans %}
                          2
                        {% elif 'Level 2' in fans %}
                          Level 2
                        {% elif 'medium' in fans %}
                          medium
                        {% elif 'Medium' in fans %}
                          Medium
                        {% elif '3' in fans %}
                          3
                        {% elif 'Level 3' in fans %}
                          Level 3
                        {% elif 'auto' in fans %}
                          auto
                        {% elif 'Auto' in fans %}
                          Auto
                        {% else %}
                          {{ fans[1] if fans|length > 1 else fans[0] }}
                        {% endif %}
                      {% else %}
                        {# Level 2+: Maximum for LOW tier - Level 3 equivalent #}
                        {% if '3' in fans %}
                          3
                        {% elif 'Level 3' in fans %}
                          Level 3
                        {% elif 'medium' in fans %}
                          medium
                        {% elif 'Medium' in fans %}
                          Medium
                        {% elif 'auto' in fans %}
                          auto
                        {% elif 'Auto' in fans %}
                          Auto
                        {% else %}
                          {{ fans[-1] if fans else 'Auto' }}
                        {% endif %}
                      {% endif %}

                - service: climate.set_fan_mode
                  target:
                    entity_id: "{{ current_entity }}"
                  data:
                    fan_mode: "{{ selected_fan_mode }}"

                - condition: template
                  value_template: "{{ debug_enabled }}"
                - service: system_log.write
                  data:
                    message: >
                      🔍 LOW COOL ADAPTIVE FAN: {{ repeat.item }}
                      • Escalation Level: {{ escalation_level }}/4
                      • Fan Selection: {{ selected_fan_mode }}
                      • Effectiveness: {{ current_effectiveness }}% after {{ time_in_current_mode | round(1) }}min
                      • Distance from Target: {{ distance_from_target | round(1) }}°C
                      • Escalation Reason: {% if escalation_level == 0 %}Working well - no escalation needed{% elif escalation_level == 1 %}Moderate effectiveness - minor escalation{% elif escalation_level == 2 %}Poor effectiveness or distance - moderate escalation{% elif escalation_level == 3 %}Stalled progress - high escalation{% elif escalation_level == 4 %}Emergency - maximum escalation{% else %}Unknown level{% endif %}
                      • Available Fans: {{ available_fans | join(', ') }}
                    level: debug
                    logger: "blueprints.climate_control.{{ room_name | lower | replace(' ', '_') }}"
          # Only update helper entities when starting new mode (not continuing)
          - condition: template
            value_template: "{{ states(helper_mode) | default('off') != 'cooling_low' }}"
          - service: input_text.set_value
            target:
              entity_id: !input helper_last_mode
            data:
              value: "cooling_low"
          - service: input_datetime.set_datetime
            target:
              entity_id: !input helper_last_change
            data:
              datetime: "{{ now() }}"

      # PRIORITY: Check for high temperature first when AC is off
      - conditions:
          - condition: template
            value_template: "{{ current_temp >= cooling_high_temp and should_activate and actual_ac_state == 'off' }}"
        sequence:
          - service: system_log.write
            data:
              message: "🔥 {{ room_name | upper }} HIGH COOLING ACTIVATED - Temperature {{ current_temp }}°C exceeds {{ cooling_high_temp }}°C"
              level: warning
              logger: "blueprints.climate_control.{{ room_name | lower | replace(' ', '_') }}"
          
          - repeat:
              for_each: "{{ climate_list }}"
              sequence:
                - variables:
                    current_entity: "{{ repeat.item }}"
                    available_modes: "{{ state_attr(repeat.item, 'hvac_modes') | list }}"
                - service: climate.set_hvac_mode
                  target:
                    entity_id: "{{ current_entity }}"
                  data:
                    hvac_mode: >
                      {% if 'cool' in available_modes %}
                        cool
                      {% elif 'Cool' in available_modes %}
                        Cool
                      {% elif 'auto' in available_modes %}
                        auto
                      {% elif 'Auto' in available_modes %}
                        Auto
                      {% elif 'heat_cool' in available_modes %}
                        heat_cool
                      {% elif 'Heat/Cool' in available_modes %}
                        Heat/Cool
                      {% else %}
                        {{ available_modes[0] if available_modes else 'off' }}
                      {% endif %}
          
          - service: climate.set_temperature
            target:
              entity_id: !input climate_entities
            data:
              temperature: "{{ cooling_target_temp }}"
            continue_on_error: true
          
          - repeat:
              for_each: "{{ climate_list }}"
              sequence:
                - variables:
                    current_entity: "{{ repeat.item }}"
                    available_fans: "{{ state_attr(repeat.item, 'fan_modes') | list }}"
                    desired_max_fan: "{{ fan_speed_max }}"
                    
                    # Smart fan mode selection - finds the best match for "maximum" speed
                    selected_fan_mode: >
                      {% set fans = state_attr(repeat.item, 'fan_modes') | list %}
                      {% set desired = fan_speed_max %}
                      
                      {# First try the exact user selection #}
                      {% if desired in fans %}
                        {{ desired }}
                      {# Then try maximum speed equivalents in order of preference #}
                      {% elif 'high' in fans %}
                        high
                      {% elif 'High' in fans %}
                        High
                      {% elif '5' in fans %}
                        5
                      {% elif 'Level 5' in fans %}
                        Level 5
                      {% elif 'turbo' in fans %}
                        turbo
                      {% elif 'Turbo' in fans %}
                        Turbo
                      {% elif 'max' in fans %}
                        max
                      {% elif 'Max' in fans %}
                        Max
                      {# Fallback to highest available numeric #}
                      {% elif '4' in fans %}
                        4
                      {% elif 'Level 4' in fans %}
                        Level 4
                      {% elif '3' in fans %}
                        3
                      {% elif 'Level 3' in fans %}
                        Level 3
                      {# Last resort - Auto or highest available #}
                      {% elif 'Auto' in fans %}
                        Auto
                      {% elif 'auto' in fans %}
                        auto
                      {% else %}
                        {{ fans[-1] if fans else 'Auto' }}
                      {% endif %}
                
                - service: climate.set_fan_mode
                  target:
                    entity_id: "{{ current_entity }}"
                  data:
                    fan_mode: "{{ selected_fan_mode }}"
                
                - condition: template
                  value_template: "{{ debug_enabled }}"
                - service: system_log.write
                  data:
                    message: >
                      🔍 {{ room_name | upper }} SMART FAN: {{ repeat.item }}
                      • Desired: {{ desired_max_fan }}
                      • Available: {{ available_fans | join(', ') }}
                      • Selected: {{ selected_fan_mode }}
                      • Reason: {{ 'Exact match' if desired_max_fan in available_fans else 'Best equivalent for maximum cooling' }}
                    level: debug
                    logger: "blueprints.climate_control.{{ room_name | lower | replace(' ', '_') }}"
          
          - service: input_text.set_value
            target:
              entity_id: !input helper_last_mode
            data:
              value: "cooling"

          # Update mode start time for effectiveness tracking (only if mode changed)
          - condition: template
            value_template: "{{ dynamic_enabled and helper_mode_time not in [none, '', 'unavailable', 'unknown', []] and helper_mode_time is string and helper_mode_time != '' and last_mode != 'cooling' }}"
          - service: input_datetime.set_datetime
            target:
              entity_id: "{{ helper_mode_time }}"
            data:
              datetime: "{{ now() }}"
            continue_on_error: true

      # CRITICAL: Clear stability mode when temperature exits comfort zone
      # This MUST be the FIRST condition to prevent getting stuck
      - conditions:
          - condition: template
            value_template: "{{ last_mode in ['stability_off', 'stability_eco'] }}"
          - condition: template
            value_template: "{{ current_temp < comfort_min_temp or current_temp > comfort_max_temp }}"
        sequence:
          # Always log this critical reset
          - service: system_log.write
            data:
              message: >
                🔄 Stability Mode Reset: Temperature outside comfort zone!
                • Current: {{ current_temp }}°C
                • Comfort Zone: {{ comfort_min_temp }}-{{ comfort_max_temp }}°C
                • Was in mode: {{ last_mode }}
                • Clearing stability mode to allow normal operation
              level: warning
              logger: "blueprints.climate_control.{{ room_name | lower | replace(' ', '_') }}"
          
          # Don't set to "off" - set directly to cooling/heating to bypass checks
          - service: input_text.set_value
            target:
              entity_id: !input helper_last_mode
            data:
              value: >
                {% if current_temp >= cooling_medium_temp %}
                  cooling
                {% elif current_temp <= heating_medium_temp %}
                  heating
                {% else %}
                  off
                {% endif %}

          # Update mode start time for effectiveness tracking (only if mode changed)
          - condition: template
            value_template: "{{ dynamic_enabled and helper_mode_time not in [none, '', 'unavailable', 'unknown', []] and helper_mode_time is string and helper_mode_time != '' and last_mode not in ['cooling', 'heating', 'off'] }}"
          - service: input_datetime.set_datetime
            target:
              entity_id: "{{ helper_mode_time }}"
            data:
              datetime: "{{ now() }}"
            continue_on_error: true
          
          # Clear the stability timestamp to reset the feature
          # Don't use 1970 as it causes calculation issues - use current time instead
          - choose:
              - conditions:
                  - condition: template
                    value_template: "{{ helper_temp_stable_since not in [none, '', 'unavailable', 'unknown'] }}"
                sequence:
                  - service: input_datetime.set_datetime
                    target:
                      entity_id: "{{ helper_temp_stable_since }}"
                    data:
                      datetime: "{{ now() }}"
          
          # Now activate cooling/heating as needed since we're outside comfort zone or at trigger point
          - choose:
              # Need cooling
              - conditions:
                  - condition: template
                    value_template: "{{ current_temp >= cooling_medium_temp }}"
                sequence:
                  - repeat:
                      for_each: "{{ climate_list }}"
                      sequence:
                        - variables:
                            current_entity: "{{ repeat.item }}"
                            available_modes: "{{ state_attr(repeat.item, 'hvac_modes') | list }}"
                            
                            # Smart cooling mode selection
                            selected_hvac_mode: >
                              {% set modes = state_attr(repeat.item, 'hvac_modes') | list %}
                              {# For cooling, try cooling-capable modes #}
                              {% if 'cool' in modes %}
                                cool
                              {% elif 'Cool' in modes %}
                                Cool
                              {% elif 'auto' in modes %}
                                auto
                              {% elif 'Auto' in modes %}
                                Auto
                              {% elif 'heat_cool' in modes %}
                                heat_cool
                              {% elif 'Heat/Cool' in modes %}
                                Heat/Cool
                              {% else %}
                                {{ modes[0] if modes else 'off' }}
                              {% endif %}
                        
                        - service: climate.set_hvac_mode
                          target:
                            entity_id: "{{ current_entity }}"
                          data:
                            hvac_mode: "{{ selected_hvac_mode }}"
                        
                        - condition: template
                          value_template: "{{ debug_enabled }}"
                        - service: system_log.write
                          data:
                            message: >
                              🔍 SMART COOL MODE: {{ repeat.item }}
                              • Available: {{ available_modes | join(', ') }}
                              • Selected: {{ selected_hvac_mode }}
                              • Reason: Best cooling mode for this AC unit
                            level: debug
                            logger: "blueprints.climate_control.{{ room_name | lower | replace(' ', '_') }}"
                  
                  - service: climate.set_temperature
                    target:
                      entity_id: !input climate_entities
                    data:
                      temperature: "{{ cooling_target_temp }}"
                  
                  - repeat:
                      for_each: "{{ climate_list }}"
                      sequence:
                        - variables:
                            current_entity: "{{ repeat.item }}"
                            available_fans: "{{ state_attr(repeat.item, 'fan_modes') | list }}"
                            desired_max_fan: !input fan_speed_max
                        - service: climate.set_fan_mode
                          target:
                            entity_id: "{{ current_entity }}"
                          data:
                            fan_mode: >
                              {% if desired_max_fan in available_fans %}
                                {{ desired_max_fan }}
                              {% elif '5' in available_fans %}
                                5
                              {% elif 'Level 5' in available_fans %}
                                Level 5
                              {% elif 'high' in available_fans %}
                                high
                              {% elif 'Auto' in available_fans %}
                                Auto
                              {% elif 'auto' in available_fans %}
                                auto
                              {% else %}
                                {{ available_fans[-1] if available_fans else 'Auto' }}
                              {% endif %}
                  
                  - service: input_text.set_value
                    target:
                      entity_id: !input helper_last_mode
                    data:
                      value: "cooling"

                  # Update mode start time for effectiveness tracking (only if mode changed)
                  - condition: template
                    value_template: "{{ dynamic_enabled and helper_mode_time not in [none, '', 'unavailable', 'unknown', []] and helper_mode_time is string and helper_mode_time != '' and last_mode != 'cooling' }}"
                  - service: input_datetime.set_datetime
                    target:
                      entity_id: "{{ helper_mode_time }}"
                    data:
                      datetime: "{{ now() }}"

                  - service: system_log.write
                    data:
                      message: >
                        ❄️ Cooling activated after stability reset
                        • Temperature: {{ current_temp }}°C
                        • Target: {{ cooling_target_temp }}°C
                        • Mode: {{ 'High' if current_temp >= cooling_high_temp else 'Medium' }}
                      level: warning
                      logger: "blueprints.climate_control.{{ room_name | lower | replace(' ', '_') }}"
              
              # Need heating
              - conditions:
                  - condition: template
                    value_template: "{{ should_activate and current_temp <= heating_medium_temp and current_temp > heating_high_temp and (time_since_change > runtime_min or last_mode != 'heating_medium') }}"
                sequence:
                  - repeat:
                      for_each: "{{ climate_list }}"
                      sequence:
                        - variables:
                            current_entity: "{{ repeat.item }}"
                            available_modes: "{{ state_attr(repeat.item, 'hvac_modes') | list }}"
                            
                            # Smart heating mode selection
                            selected_hvac_mode: >
                              {% set modes = state_attr(repeat.item, 'hvac_modes') | list %}
                              {# For heating, try heating-capable modes #}
                              {% if 'heat' in modes %}
                                heat
                              {% elif 'Heat' in modes %}
                                Heat
                              {% elif 'auto' in modes %}
                                auto
                              {% elif 'Auto' in modes %}
                                Auto
                              {% elif 'heat_cool' in modes %}
                                heat_cool
                              {% elif 'Heat/Cool' in modes %}
                                Heat/Cool
                              {% else %}
                                {{ modes[0] if modes else 'off' }}
                              {% endif %}
                        
                        - service: climate.set_hvac_mode
                          target:
                            entity_id: "{{ current_entity }}"
                          data:
                            hvac_mode: "{{ selected_hvac_mode }}"
                        
                        - condition: template
                          value_template: "{{ debug_enabled }}"
                        - service: system_log.write
                          data:
                            message: >
                              🔍 SMART HEAT MODE: {{ repeat.item }}
                              • Available: {{ available_modes | join(', ') }}
                              • Selected: {{ selected_hvac_mode }}
                              • Reason: Best heating mode for this AC unit
                            level: debug
                            logger: "blueprints.climate_control.{{ room_name | lower | replace(' ', '_') }}"
                  
                  - service: climate.set_temperature
                    target:
                      entity_id: !input climate_entities
                    data:
                      temperature: "{{ heating_target_temp }}"
                  
                  - repeat:
                      for_each: "{{ climate_list }}"
                      sequence:
                        - variables:
                            current_entity: "{{ repeat.item }}"
                            available_fans: "{{ state_attr(repeat.item, 'fan_modes') | list }}"
                            desired_max_fan: !input fan_speed_max
                        - service: climate.set_fan_mode
                          target:
                            entity_id: "{{ current_entity }}"
                          data:
                            fan_mode: >
                              {% if desired_max_fan in available_fans %}
                                {{ desired_max_fan }}
                              {% elif '5' in available_fans %}
                                5
                              {% elif 'Level 5' in available_fans %}
                                Level 5
                              {% elif 'high' in available_fans %}
                                high
                              {% elif 'Auto' in available_fans %}
                                Auto
                              {% elif 'auto' in available_fans %}
                                auto
                              {% else %}
                                {{ available_fans[-1] if available_fans else 'Auto' }}
                              {% endif %}
                  
                  - service: input_text.set_value
                    target:
                      entity_id: !input helper_last_mode
                    data:
                      value: "heating"

                  # Update mode start time for effectiveness tracking (only if mode changed)
                  - condition: template
                    value_template: "{{ dynamic_enabled and helper_mode_time not in [none, '', 'unavailable', 'unknown', []] and helper_mode_time is string and helper_mode_time != '' and last_mode != 'heating' }}"
                  - service: input_datetime.set_datetime
                    target:
                      entity_id: "{{ helper_mode_time }}"
                    data:
                      datetime: "{{ now() }}"

                  - service: system_log.write
                    data:
                      message: >
                        🔥 Heating activated after stability reset
                        • Temperature: {{ current_temp }}°C
                        • Target: {{ heating_target_temp }}°C
                        • Mode: {{ 'High' if current_temp <= heating_low_temp else 'Medium' }}
                      level: warning
                      logger: "blueprints.climate_control.{{ room_name | lower | replace(' ', '_') }}"

      # Pre-conditioning when approaching
      - conditions:
          - condition: template
            value_template: "{{ enable_pre_conditioning }}"
          - condition: template
            value_template: "{{ should_activate }}"
          - condition: template
            value_template: "{{ approaching_home }}"
          - condition: template
            value_template: "{{ current_temp > cooling_medium_temp or current_temp < heating_medium_temp }}"
        sequence:
          - condition: template
            value_template: "{{ debug_enabled }}"
          - service: system_log.write
            data:
              message: >
                🔍 {{ room_name | upper }} CLIMATE DEBUG: Pre-conditioning activated
                Reason: Approaching home
                {% if proximity_sensor %}Distance: {{ states(proximity_sensor) }}m{% endif %}
                Temperature: {{ current_temp }}°C needs adjustment
              level: warning
              logger: "blueprints.climate_control.{{ room_name | lower | replace(' ', '_') }}"
          - service: climate.set_hvac_mode
            target:
              entity_id: !input climate_entities
            data:
              hvac_mode: >
                {% if current_temp > comfort_max_temp %}
                  cool
                {% elif current_temp < comfort_min_temp %}
                  heat
                {% else %}
                  heat_cool
                {% endif %}
          
          - service: climate.set_temperature
            target:
              entity_id: !input climate_entities
            data:
              temperature: "{{ weather_adjusted_target }}"

          - service: system_log.write
            data:
              message: >
                🌡️ {{ room_name | upper }} TEMP SET: Pre-conditioning
                Set AC to: {{ weather_adjusted_target | round(0) }}°C (rounded for AC compatibility)
                Actual value: {{ weather_adjusted_target | round(1) }}°C
                Reason: Pre-conditioning target temperature (approaching home)
                Current: {{ current_temp | round(1) }}°C → Target: {{ weather_adjusted_target | round(1) }}°C
              level: warning
              logger: "blueprints.climate_control.{{ room_name | lower | replace(' ', '_') }}"
          
          - service: climate.set_fan_mode
            target:
              entity_id: !input climate_entities
            data:
              fan_mode: >
                {% set available_fans = state_attr(climate_list[0], 'fan_modes') | list %}
                {% set desired_medium_fan = fan_speed_medium %}
                {% if desired_medium_fan in available_fans %}
                  {{ desired_medium_fan }}
                {% elif '3' in available_fans %}
                  3
                {% elif 'Level 3' in available_fans %}
                  Level 3
                {% elif 'medium' in available_fans %}
                  medium
                {% elif 'Auto' in available_fans %}
                  Auto
                {% elif 'auto' in available_fans %}
                  auto
                {% else %}
                  {{ available_fans[-1] if available_fans else 'Auto' }}
                {% endif %}
          
          - condition: template
            value_template: "{{ enable_notifications }}"
          
          - service: !input notification_service
            data:
              title: "Climate Pre-conditioning"
              message: >
                Pre-conditioning started - approaching home.
                Current: {{ current_temp }}°C
      
      # Low cooling - Gentle cooling mode when temperature just exceeds comfort zone
      # Activates at cooling_low_temp (e.g., 24.1°C) but continues until target reached
      # Uses low fan speed for maximum efficiency with minimal power consumption
      # NEW: Prevents short cycling by using target-based hysteresis logic
      # IMPORTANT: Only for pre-conditioning - not when already home in Smart mode
      - conditions:
          - condition: template
            value_template: "{{ should_activate and approaching_home }}"
          - condition: template
            value_template: >
              {% if last_mode == 'cooling' %}
                {# Already cooling - continue until target reached or moved to higher tier #}
                {{ current_temp > target_temp and current_temp < cooling_medium_temp }}
              {% else %}
                {# Not cooling - start if low threshold exceeded and below medium #}
                {{ current_temp >= cooling_low_temp and current_temp < cooling_medium_temp }}
              {% endif %}
          - condition: template
            value_template: "{{ time_since_change > runtime_min or last_mode != 'cooling' }}"
        sequence:
          - service: system_log.write
            data:
              message: >
                {% set current_mode = states(helper_mode) | default('off') %}
                🌡️ {{ room_name | upper }} LOW COOLING ACTIVATED
                {% if current_mode == 'cooling_low' %}
                📊 CONTINUE MODE: Already cooling low, continuing until target
                • Current: {{ current_temp }}°C → Target: {{ target_temp }}°C ({{ (current_temp - target_temp) | round(1) }}°C to go)
                {% else %}
                🚀 START MODE: Starting LOW cooling
                • Trigger: {{ current_temp }}°C ≥ {{ cooling_low_temp }}°C (threshold)
                • Target: {{ target_temp }}°C ({{ (current_temp - target_temp) | round(1) }}°C to cool)
                {% endif %}
                • Last Mode: {{ current_mode }}
              level: warning
              logger: "blueprints.climate_control.{{ room_name | lower | replace(' ', '_') }}"
          
          - repeat:
              for_each: "{{ climate_list }}"
              sequence:
                - variables:
                    current_entity: "{{ repeat.item }}"
                    available_modes: "{{ state_attr(repeat.item, 'hvac_modes') | list }}"
                    
                    # Smart cooling mode selection
                    selected_hvac_mode: >
                      {% set modes = state_attr(repeat.item, 'hvac_modes') | list %}
                      {# For cooling, try cooling-capable modes #}
                      {% if 'cool' in modes %}
                        cool
                      {% elif 'Cool' in modes %}
                        Cool
                      {% elif 'auto' in modes %}
                        auto
                      {% elif 'Auto' in modes %}
                        Auto
                      {% elif 'heat_cool' in modes %}
                        heat_cool
                      {% elif 'Heat/Cool' in modes %}
                        Heat/Cool
                      {% else %}
                        {{ modes[0] if modes else 'off' }}
                      {% endif %}
                
                - service: climate.set_hvac_mode
                  target:
                    entity_id: "{{ current_entity }}"
                  data:
                    hvac_mode: "{{ selected_hvac_mode }}"
                
                - condition: template
                  value_template: "{{ debug_enabled }}"
                - service: system_log.write
                  data:
                    message: >
                      🔍 SMART LOW COOL MODE: {{ repeat.item }}
                      • Available: {{ available_modes | join(', ') }}
                      • Selected: {{ selected_hvac_mode }}
                      • Reason: Low cooling mode for gentle temperature control
                    level: debug
                    logger: "blueprints.climate_control.{{ room_name | lower | replace(' ', '_') }}"
          
          - service: climate.set_temperature
            target:
              entity_id: !input climate_entities
            data:
              temperature: "{{ cooling_target_temp }}"
            continue_on_error: true
          
          # Use low fan speed for gentle cooling
          - repeat:
              for_each: "{{ climate_list }}"
              sequence:
                - variables:
                    current_entity: "{{ repeat.item }}"
                    available_fans: "{{ state_attr(repeat.item, 'fan_modes') | list }}"
                    
                    # Smart low fan mode selection
                    selected_fan_mode: >
                      {% set fans = state_attr(repeat.item, 'fan_modes') | list %}
                      
                      {# Try low speed equivalents in order of preference #}
                      {% if 'low' in fans %}
                        low
                      {% elif 'Low' in fans %}
                        Low
                      {% elif '1' in fans %}
                        1
                      {% elif 'Level 1' in fans %}
                        Level 1
                      {% elif 'quiet' in fans %}
                        quiet
                      {% elif 'Quiet' in fans %}
                        Quiet
                      {% elif 'eco' in fans %}
                        eco
                      {% elif 'Eco' in fans %}
                        Eco
                      {% elif 'auto' in fans %}
                        auto
                      {% elif 'Auto' in fans %}
                        Auto
                      {# Fallback to available options #}
                      {% elif '2' in fans %}
                        2
                      {% elif 'Level 2' in fans %}
                        Level 2
                      {% else %}
                        {{ fans[0] if fans else 'Auto' }}
                      {% endif %}
                
                - service: climate.set_fan_mode
                  target:
                    entity_id: "{{ current_entity }}"
                  data:
                    fan_mode: "{{ selected_fan_mode }}"
                
                - condition: template
                  value_template: "{{ debug_enabled }}"
                - service: system_log.write
                  data:
                    message: >
                      🔍 SMART LOW FAN: {{ repeat.item }}
                      • Available: {{ available_fans | join(', ') }}
                      • Selected: {{ selected_fan_mode }}
                      • Reason: Low fan speed for gentle cooling
                    level: debug
                    logger: "blueprints.climate_control.{{ room_name | lower | replace(' ', '_') }}"
          
          - service: input_text.set_value
            target:
              entity_id: !input helper_last_mode
            data:
              value: "cooling"

          # Update mode start time for effectiveness tracking (only if mode changed)
          - condition: template
            value_template: "{{ dynamic_enabled and helper_mode_time not in [none, '', 'unavailable', 'unknown', []] and helper_mode_time is string and helper_mode_time != '' and last_mode != 'cooling' }}"
          - service: input_datetime.set_datetime
            target:
              entity_id: "{{ helper_mode_time }}"
            data:
              datetime: "{{ now() }}"
            continue_on_error: true

      # Medium cooling - Normal cooling mode when temperature exceeds comfort zone
      # Activates at cooling_medium_temp (e.g., 25°C) but continues until target reached
      # Uses medium fan speed for efficient cooling without excessive power consumption
      # NEW: Prevents short cycling by using target-based hysteresis logic
      - conditions:
          - condition: template
            value_template: "{{ should_activate }}"
          - condition: template
            value_template: >
              {% if last_mode == 'cooling' %}
                {# Already cooling - continue until target reached or moved to higher tier #}
                {{ current_temp > target_temp and current_temp < cooling_high_temp }}
              {% else %}
                {# Not cooling - start if medium threshold exceeded and below high #}
                {{ current_temp >= cooling_medium_temp and current_temp < cooling_high_temp }}
              {% endif %}
          - condition: template
            value_template: "{{ time_since_change > runtime_min or last_mode != 'cooling' }}"
        sequence:
          - service: system_log.write
            data:
              message: >
                {% set current_mode = states(helper_mode) | default('off') %}
                🌡️ {{ room_name | upper }} MEDIUM COOLING ACTIVATED
                {% if current_mode == 'cooling_medium' %}
                📊 CONTINUE MODE: Already cooling medium, continuing until target
                • Current: {{ current_temp }}°C → Target: {{ target_temp }}°C ({{ (current_temp - target_temp) | round(1) }}°C to go)
                {% else %}
                🚀 START MODE: Starting MEDIUM cooling
                • Trigger: {{ current_temp }}°C ≥ {{ cooling_medium_temp }}°C (threshold)
                • Target: {{ target_temp }}°C ({{ (current_temp - target_temp) | round(1) }}°C to cool)
                {% endif %}
                • Last Mode: {{ current_mode }}
              level: warning
              logger: "blueprints.climate_control.{{ room_name | lower | replace(' ', '_') }}"
          
          - repeat:
              for_each: "{{ climate_list }}"
              sequence:
                - variables:
                    current_entity: "{{ repeat.item }}"
                    available_modes: "{{ state_attr(repeat.item, 'hvac_modes') | list }}"
                    
                    # Smart cooling mode selection
                    selected_hvac_mode: >
                      {% set modes = state_attr(repeat.item, 'hvac_modes') | list %}
                      {# For cooling, try cooling-capable modes #}
                      {% if 'cool' in modes %}
                        cool
                      {% elif 'Cool' in modes %}
                        Cool
                      {% elif 'auto' in modes %}
                        auto
                      {% elif 'Auto' in modes %}
                        Auto
                      {% elif 'heat_cool' in modes %}
                        heat_cool
                      {% elif 'Heat/Cool' in modes %}
                        Heat/Cool
                      {% else %}
                        {{ modes[0] if modes else 'off' }}
                      {% endif %}
                
                - service: climate.set_hvac_mode
                  target:
                    entity_id: "{{ current_entity }}"
                  data:
                    hvac_mode: "{{ selected_hvac_mode }}"
                
                - condition: template
                  value_template: "{{ debug_enabled }}"
                - service: system_log.write
                  data:
                    message: >
                      🔍 SMART MEDIUM COOL MODE: {{ repeat.item }}
                      • Available: {{ available_modes | join(', ') }}
                      • Selected: {{ selected_hvac_mode }}
                      • Reason: Medium cooling mode for balanced temperature control
                    level: debug
                    logger: "blueprints.climate_control.{{ room_name | lower | replace(' ', '_') }}"
          
          - service: climate.set_temperature
            target:
              entity_id: !input climate_entities
            data:
              temperature: "{{ cooling_target_temp }}"
            continue_on_error: true
          
          # Use medium fan speed for balanced cooling
          - repeat:
              for_each: "{{ climate_list }}"
              sequence:
                - variables:
                    current_entity: "{{ repeat.item }}"
                    available_fans: "{{ state_attr(repeat.item, 'fan_modes') | list }}"
                    desired_medium_fan: "{{ fan_speed_medium }}"
                    
                    # Smart medium fan mode selection
                    selected_fan_mode: >
                      {% set fans = state_attr(repeat.item, 'fan_modes') | list %}
                      {% set desired = fan_speed_medium %}
                      
                      {# First try the exact user selection #}
                      {% if desired in fans %}
                        {{ desired }}
                      {# Then try medium speed equivalents in order of preference #}
                      {% elif 'medium' in fans %}
                        medium
                      {% elif 'Medium' in fans %}
                        Medium
                      {% elif '3' in fans %}
                        3
                      {% elif 'Level 3' in fans %}
                        Level 3
                      {% elif 'auto' in fans %}
                        auto
                      {% elif 'Auto' in fans %}
                        Auto
                      {# Fallback to available options #}
                      {% elif '2' in fans %}
                        2
                      {% elif 'Level 2' in fans %}
                        Level 2
                      {% elif 'low' in fans %}
                        low
                      {% elif 'high' in fans %}
                        high
                      {% else %}
                        {{ fans[0] if fans else 'Auto' }}
                      {% endif %}
                
                - service: climate.set_fan_mode
                  target:
                    entity_id: "{{ current_entity }}"
                  data:
                    fan_mode: "{{ selected_fan_mode }}"
                
                - condition: template
                  value_template: "{{ debug_enabled }}"
                - service: system_log.write
                  data:
                    message: >
                      🔍 SMART MEDIUM FAN: {{ repeat.item }}
                      • Desired: {{ desired_medium_fan }}
                      • Available: {{ available_fans | join(', ') }}
                      • Selected: {{ selected_fan_mode }}
                      • Reason: {{ 'Exact match' if desired_medium_fan in available_fans else 'Best equivalent for medium cooling' }}
                    level: debug
                    logger: "blueprints.climate_control.{{ room_name | lower | replace(' ', '_') }}"
          
          - service: input_text.set_value
            target:
              entity_id: !input helper_last_mode
            data:
              value: "cooling"

          # Update mode start time for effectiveness tracking (only if mode changed)
          - condition: template
            value_template: "{{ dynamic_enabled and helper_mode_time not in [none, '', 'unavailable', 'unknown', []] and helper_mode_time is string and helper_mode_time != '' and last_mode != 'cooling' }}"
          - service: input_datetime.set_datetime
            target:
              entity_id: "{{ helper_mode_time }}"
            data:
              datetime: "{{ now() }}"
            continue_on_error: true

      # Comfort zone - Eco mode or OFF mode (Power Saving)
      # When temperature is within acceptable range (e.g., 21-25°C with ±2°C tolerance)
      # OFF mode saves 40-60% more energy vs eco mode but allows temperature drift
      - conditions:
          - condition: template
            value_template: "{{ should_activate }}"
          - condition: template
            value_template: "{{ current_temp >= comfort_min_temp and current_temp <= comfort_max_temp }}"
          - condition: template
            value_template: "{{ time_since_change > runtime_min }}"
        sequence:
          - condition: template
            value_template: "{{ debug_enabled }}"
          - service: system_log.write
            data:
              message: >
                🔍 {{ room_name | upper }} CLIMATE DEBUG: Comfort Zone Action Triggered
                
                🎛️ Control Status:
                    • Control Mode: {{ control_mode }}
                    • Should Activate: {{ should_activate }}
                    • Extreme Temp Detected: {{ extreme_temp_detected }}
                    • Current Temp: {{ current_temp }}°C
                    • Extreme High: {{ extreme_high }}°C
                    • Extreme Low: {{ extreme_low }}°C
                    • Room Presence: {{ room_presence_detected }}
                    • Approaching Home: {{ approaching_home }}
              level: warning
              logger: "blueprints.climate_control.{{ room_name | lower | replace(' ', '_') }}"
          - choose:
              - conditions:
                  - condition: template
                    value_template: "{{ eco_enabled }}"
                sequence:
                  # Eco mode - maintain with reduced fan and wider setpoint (uses ~150-250W)
                  # Set HVAC mode to auto for maximum efficiency in comfort zone
                  - repeat:
                      for_each: "{{ climate_list }}"
                      sequence:
                        - variables:
                            current_entity: "{{ repeat.item }}"
                            available_modes: "{{ state_attr(repeat.item, 'hvac_modes') | list }}"
                        - service: climate.set_hvac_mode
                          target:
                            entity_id: "{{ current_entity }}"
                          data:
                            hvac_mode: >
                              {% if 'auto' in available_modes %}
                                auto
                              {% elif 'Auto' in available_modes %}
                                Auto
                              {% elif 'heat_cool' in available_modes %}
                                heat_cool
                              {% elif 'Heat/Cool' in available_modes %}
                                heat_cool
                              {% else %}
                                {{ available_modes[0] if available_modes else 'off' }}
                              {% endif %}
                  
                  - service: climate.set_temperature
                    target:
                      entity_id: !input climate_entities
                    data:
                      temperature: "{{ target_temp }}"

                  - service: system_log.write
                    data:
                      message: >
                        🌡️ {{ room_name | upper }} TEMP SET: Comfort Zone Maintenance
                        Set AC to: {{ target_temp | round(0) }}°C (rounded for AC compatibility)
                        Actual value: {{ target_temp | round(1) }}°C
                        Reason: Maintaining target temperature within comfort zone
                        Current: {{ current_temp | round(1) }}°C → Target: {{ target_temp | round(1) }}°C
                      level: warning
                      logger: "blueprints.climate_control.{{ room_name | lower | replace(' ', '_') }}"
                  
                  # Set auto fan mode for maximum efficiency in comfort zone
                  - repeat:
                      for_each: "{{ climate_list }}"
                      sequence:
                        - variables:
                            current_entity: "{{ repeat.item }}"
                            available_fans: "{{ state_attr(repeat.item, 'fan_modes') | list }}"
                        - service: climate.set_fan_mode
                          target:
                            entity_id: "{{ current_entity }}"
                          data:
                            fan_mode: >
                              {% if 'Auto' in available_fans %}
                                Auto
                              {% elif 'auto' in available_fans %}
                                auto
                              {% elif 'low' in available_fans %}
                                low
                              {% elif 'Quiet' in available_fans %}
                                Quiet
                              {% elif 'Silence' in available_fans %}
                                Silence
                              {% elif '1' in available_fans %}
                                1
                              {% elif 'Level 1' in available_fans %}
                                Level 1
                              {% else %}
                                {{ available_fans[0] if available_fans else 'Auto' }}
                              {% endif %}
                  
                  # Set swing mode with compatibility check for eco mode
                  - repeat:
                      for_each: "{{ climate_list }}"
                      sequence:
                        - variables:
                            current_entity: "{{ repeat.item }}"
                            available_swings: "{{ state_attr(repeat.item, 'swing_modes') | list }}"
                            desired_swing: !input swing_mode_active
                        - service: climate.set_swing_mode
                          target:
                            entity_id: "{{ current_entity }}"
                          data:
                            swing_mode: >
                              {% if desired_swing in available_swings %}
                                {{ desired_swing }}
                              {% elif desired_swing == 'both' and '3D' in available_swings %}
                                3D
                              {% elif desired_swing == 'both' and 'Both' in available_swings %}
                                Both
                              {% elif desired_swing == 'vertical' and 'Vertical' in available_swings %}
                                Vertical
                              {% elif desired_swing == 'Vertical' and 'vertical' in available_swings %}
                                vertical
                              {% elif desired_swing == 'horizontal' and 'Horizontal' in available_swings %}
                                Horizontal
                              {% elif desired_swing == 'Horizontal' and 'horizontal' in available_swings %}
                                horizontal
                              {% elif 'both' in available_swings %}
                                both
                              {% elif 'Both' in available_swings %}
                                Both
                              {% elif '3D' in available_swings %}
                                3D
                              {% elif 'Vertical' in available_swings %}
                                Vertical
                              {% elif 'vertical' in available_swings %}
                                vertical
                              {% else %}
                                {{ available_swings[0] if available_swings else 'Off' }}
                              {% endif %}
                  
                  - service: input_text.set_value
                    target:
                      entity_id: !input helper_last_mode
                    data:
                      value: "eco"

                  # Update mode start time for effectiveness tracking (only if mode changed)
                  - condition: template
                    value_template: "{{ dynamic_enabled and helper_mode_time not in [none, '', 'unavailable', 'unknown', []] and helper_mode_time is string and helper_mode_time != '' and last_mode != 'eco' }}"
                  - service: input_datetime.set_datetime
                    target:
                      entity_id: "{{ helper_mode_time }}"
                    data:
                      datetime: "{{ now() }}"
                  
                  - condition: template
                    value_template: "{{ enable_notifications }}"
                  
                  - service: !input notification_service
                    data:
                      title: "{{ room_name }} Climate Control"
                      message: "Eco mode - Comfort zone ({{ current_temp }}°C)"
            
            default:
              # OFF mode - Maximum power savings (0W consumption)
              # A/C turns completely off when temperature is acceptable
              # Will restart when temp exits comfort zone (e.g., <21°C or >25°C)
              - service: climate.turn_off
                target:
                  entity_id: !input climate_entities
                continue_on_error: true
              
              - service: input_text.set_value
                target:
                  entity_id: !input helper_last_mode
                data:
                  value: "off"

              # Update mode start time for effectiveness tracking (only if mode changed)
              - condition: template
                value_template: "{{ dynamic_enabled and helper_mode_time not in [none, '', 'unavailable', 'unknown', []] and helper_mode_time is string and helper_mode_time != '' and last_mode != 'off' }}"
              - service: input_datetime.set_datetime
                target:
                  entity_id: "{{ helper_mode_time }}"
                data:
                  datetime: "{{ now() }}"
              
              - condition: template
                value_template: "{{ enable_notifications }}"
              
              - service: !input notification_service
                data:
                  title: "{{ room_name }} Climate Control"
                  message: "Turned off - Comfort zone ({{ current_temp }}°C)"

      # Temperature Stability Auto-Off (Auto/Smart Mode - temperature equilibrium reached AND in comfort zone)
      - conditions:
          - condition: template
            value_template: "{{ temp_stability_enabled }}"
          - condition: template
            value_template: "{{ control_mode in ['Smart', 'Auto'] }}"
          - condition: template
            value_template: "{{ temp_stability_detected }}"
          - condition: template
            value_template: "{{ last_mode not in ['off', 'stability_off', 'stability_eco', 'unavailable', 'unknown', none] }}"
          - condition: template
            value_template: "{{ time_since_change > offtime_min }}"
          # CRITICAL: Allow stability auto-off within reasonable range (comfort zone + aggressiveness buffer)
          - condition: template
            value_template: >
              {% set buffer = aggressiveness | float %}
              {{ current_temp >= (comfort_min_temp - buffer) and current_temp <= (comfort_max_temp + buffer) }}
        sequence:
          - condition: template
            value_template: "{{ debug_enabled }}"
          - service: system_log.write
            data:
              message: >
                🎯 Smart Feature: Temperature Stable & Comfortable
                🌡️ Temperature steady at {{ current_temp | round(1) }}°C for {{ time_in_current_mode | round(0) }} minutes
                ✅ Within acceptable range ({{ comfort_min_temp }}-{{ comfort_max_temp }}°C + {{ aggressiveness }}°C buffer) - switching to {{ stability_behavior|upper }} mode to save energy
                
                📋 Technical: ±{{ stability_tolerance }}°C tolerance, {{ stability_duration }}min requirement, {{ time_since_change | round(1) }}min since last change
              level: warning
              logger: "blueprints.climate_control.{{ room_name | lower | replace(' ', '_') }}"
          
          - choose:
              - conditions:
                  - condition: template
                    value_template: "{{ stability_behavior == 'off' }}"
                sequence:
                  - service: climate.turn_off
                    target:
                      entity_id: !input climate_entities

                  - service: system_log.write
                    data:
                      message: >
                        🌡️ {{ room_name | upper }} TEMP SET: Temperature Stability Auto-Off
                        Set AC to: OFF
                        Actual value: OFF
                        Reason: Temperature equilibrium reached - AC job complete
                        Stable at: {{ current_temp | round(1) }}°C for {{ time_in_current_mode | round(0) }} minutes
                      level: warning
                      logger: "blueprints.climate_control.{{ room_name | lower | replace(' ', '_') }}"
              
              - conditions:
                  - condition: template
                    value_template: "{{ stability_behavior == 'eco' }}"
                sequence:
                  # Set HVAC mode with compatibility check for eco mode
                  - repeat:
                      for_each: "{{ climate_list }}"
                      sequence:
                        - variables:
                            current_entity: "{{ repeat.item }}"
                            available_modes: "{{ state_attr(repeat.item, 'hvac_modes') | list }}"
                        - service: climate.set_hvac_mode
                          target:
                            entity_id: "{{ current_entity }}"
                          data:
                            hvac_mode: >
                              {% if current_temp > comfort_max_temp %}
                                {% if 'cool' in available_modes %}
                                  cool
                                {% elif 'auto' in available_modes %}
                                  auto
                                {% elif 'heat_cool' in available_modes %}
                                  heat_cool
                                {% else %}
                                  {{ available_modes[0] if available_modes else 'off' }}
                                {% endif %}
                              {% elif current_temp < comfort_min_temp %}
                                {% if 'heat' in available_modes %}
                                  heat
                                {% elif 'auto' in available_modes %}
                                  auto
                                {% elif 'heat_cool' in available_modes %}
                                  heat_cool
                                {% else %}
                                  {{ available_modes[0] if available_modes else 'off' }}
                                {% endif %}
                              {% else %}
                                {% if 'auto' in available_modes %}
                                  auto
                                {% elif 'heat_cool' in available_modes %}
                                  heat_cool
                                {% else %}
                                  {{ available_modes[0] if available_modes else 'off' }}
                                {% endif %}
                              {% endif %}
                  
                  - service: climate.set_temperature
                    target:
                      entity_id: !input climate_entities
                    data:
                      temperature: >
                        {% if current_temp > comfort_max_temp %}
                          {{ comfort_max_temp + 1 }}
                        {% elif current_temp < comfort_min_temp %}
                          {{ comfort_min_temp - 1 }}
                        {% else %}
                          {{ target_temp }}
                        {% endif %}

                  - service: system_log.write
                    data:
                      message: >
                        🌡️ {{ room_name | upper }} TEMP SET: Temperature Stability Eco Mode
                        Set AC to: {{ ((comfort_max_temp + 1) if current_temp > comfort_max_temp else (comfort_min_temp - 1) if current_temp < comfort_min_temp else target_temp) | round(0) }}°C (rounded for AC compatibility)
                        Actual value: {{ ((comfort_max_temp + 1) if current_temp > comfort_max_temp else (comfort_min_temp - 1) if current_temp < comfort_min_temp else target_temp) | round(1) }}°C
                        Reason: Temperature stability eco mode (gentle maintenance after equilibrium)
                        Current: {{ current_temp | round(1) }}°C → Stable for {{ time_in_current_mode | round(0) }} minutes
                      level: warning
                      logger: "blueprints.climate_control.{{ room_name | lower | replace(' ', '_') }}"
          
          - service: input_text.set_value
            target:
              entity_id: !input helper_last_mode
            data:
              value: "stability_{{ stability_behavior }}"

          # Update mode start time for effectiveness tracking (only if mode changed)
          - condition: template
            value_template: "{{ dynamic_enabled and helper_mode_time not in [none, '', 'unavailable', 'unknown', []] and helper_mode_time is string and helper_mode_time != '' and last_mode != 'stability_' + stability_behavior }}"
          - service: input_datetime.set_datetime
            target:
              entity_id: "{{ helper_mode_time }}"
            data:
              datetime: "{{ now() }}"
            continue_on_error: true
          
          - condition: template
            value_template: "{{ notification_service is defined and notification_service not in [none, ''] }}"
          - service: "{{ notification_service }}"
            data:
              title: "{{ room_name }} Climate Control"
              message: "Temperature Stability: {{ stability_behavior }} (stable {{ time_in_current_mode | round(0) }}min, {{ current_temp }}°C)"

      
      # Adaptive Control Mode - Auto-switch modes based on room occupancy
      - conditions:
          - condition: template
            value_template: "{{ adaptive_enabled }}"
          # Manual Override Protection
          - condition: template
            value_template: >
              {% if adaptive_override_helper and adaptive_override_helper not in ['', 'unknown', 'unavailable'] %}
                {{ not is_state(adaptive_override_helper, 'on') }}
              {% else %}
                true
              {% endif %}
          - condition: template
            value_template: >
              {% if adaptive_override_timeout > 0 %}
                {% set last_change = states(helper_change) %}
                {% if last_change not in ['unknown', 'unavailable', ''] %}
                  {% set time_since_change = (as_timestamp(now()) - as_timestamp(last_change)) / 3600 %}
                  {{ time_since_change >= adaptive_override_timeout }}
                {% else %}
                  true
                {% endif %}
              {% else %}
                true
              {% endif %}
        sequence:
          # Debug logging for Adaptive Control evaluation
          - condition: template
            value_template: "{{ debug_enabled }}"
          - service: system_log.write
            data:
              message: >
                🔍 {{ room_name | upper }} ADAPTIVE CONTROL: Evaluating conditions...
                
                ⚙️ Adaptive Settings:
                    • Enabled: {{ adaptive_enabled }}
                    • Current Mode: {{ control_mode }}
                    • Room Presence: {{ room_presence_detected }}
                    • Last Presence: {{ states(presence_detected_helper) if presence_detected_helper else 'N/A' }}
                    • Occupied Delay: {{ adaptive_occupied_delay }} min
                    • Vacant Delay: {{ adaptive_vacant_delay }} min
                    • Target Mode: {{ adaptive_target_mode | title }}
                {% if presence_detected_helper and states(presence_detected_helper) not in ['unknown', 'unavailable', ''] %}
                {% set presence_time = (as_timestamp(now()) - as_timestamp(states(presence_detected_helper))) / 60 %}
                    • Time Since Last Presence: {{ presence_time | round(1) }} min
                {% endif %}
              level: debug
              logger: "blueprints.climate_control.{{ room_name | lower | replace(' ', '_') }}"

          # Additional parent conditions debug logging
          - condition: template
            value_template: "{{ debug_enabled }}"
          - service: system_log.write
            data:
              message: >
                🔍 {{ room_name | upper }} ADAPTIVE CONTROL: Parent Conditions Detail...
                
                🔒 Override Protection:
                    • adaptive_enabled: {{ adaptive_enabled }}
                    • override_helper exists: {{ adaptive_override_helper not in ['', 'unknown', 'unavailable'] if adaptive_override_helper else false }}
                {% if adaptive_override_helper and adaptive_override_helper not in ['', 'unknown', 'unavailable'] %}
                    • override_helper state: {{ states(adaptive_override_helper) }}
                    • override_active: {{ is_state(adaptive_override_helper, 'on') }}
                {% endif %}
                    • override_timeout: {{ adaptive_override_timeout }} hours
                {% if helper_change and states(helper_change) not in ['unknown', 'unavailable', ''] %}
                {% set time_since_change = (as_timestamp(now()) - as_timestamp(states(helper_change))) / 3600 %}
                    • time_since_last_change: {{ time_since_change | round(2) }} hours
                    • timeout_met: {{ time_since_change >= adaptive_override_timeout }}
                {% endif %}
              level: debug
              logger: "blueprints.climate_control.{{ room_name | lower | replace(' ', '_') }}"
          
          # Main condition evaluation and action logic
          - condition: or
            conditions:
              # REMOVED: Should NOT auto-switch from Manual to Auto/Smart
              # Manual mode is user's explicit choice - respect it!
              - condition: and
                conditions:
                  - condition: template
                    value_template: "{{ false }}"  # Disabled - Manual mode stays manual
              # Switch to Manual + AC off when room vacant for configured time
              - condition: and
                conditions:
                  - condition: template
                    value_template: "{{ control_mode in ['Auto', 'Smart'] }}"
                  - condition: template
                    value_template: "{{ not room_presence_detected }}"
                  - condition: template
                    value_template: >
                      {% set last_presence = states(presence_detected_helper) %}
                      {% if last_presence not in ['unknown', 'unavailable', ''] %}
                        {% set vacant_time = as_timestamp(now()) - as_timestamp(last_presence) %}
                        {{ (vacant_time / 60) >= adaptive_vacant_delay }}
                      {% else %}
                        false
                      {% endif %}
              # Handle case where already in Manual mode but AC still running and room vacant
              - condition: and
                conditions:
                  - condition: template
                    value_template: "{{ control_mode == 'Manual' }}"
                  - condition: template
                    value_template: "{{ not room_presence_detected }}"
                  - condition: template
                    value_template: >
                      {% set last_presence = states(presence_detected_helper) %}
                      {% if last_presence not in ['unknown', 'unavailable', ''] %}
                        {% set vacant_time = as_timestamp(now()) - as_timestamp(last_presence) %}
                        {{ (vacant_time / 60) >= adaptive_vacant_delay }}
                      {% else %}
                        false
                      {% endif %}
                  - condition: template
                    value_template: >
                      {% if climate_list %}
                        {% set first_climate_state = states(climate_list[0]) %}
                        {{ first_climate_state not in ['off', 'Off', 'unavailable', 'unknown'] }}
                      {% else %}
                        false
                      {% endif %}
          
          # Execute actions when conditions are met
          # Handle occupied room - switch to Auto/Smart mode
          - choose:
            - conditions:
                - condition: template
                  value_template: "{{ control_mode == 'Manual' and room_presence_detected }}"
              sequence:
                - condition: template
                  value_template: "{{ debug_enabled }}"
                - service: system_log.write
                  data:
                    message: >
                      ✅ {{ room_name | upper }} ADAPTIVE CONTROL: OCCUPIED → SWITCHING TO {{ adaptive_target_mode | title }} MODE
                      • Current Mode: {{ control_mode }} → {{ adaptive_target_mode | title }}
                      • Room Presence: {{ room_presence_detected }}
                      • Required Delay: {{ adaptive_occupied_delay }} min
                      {% set last_presence = states(presence_detected_helper) %}
                      {% if last_presence not in ['unknown', 'unavailable', ''] %}
                      {% set presence_time = (as_timestamp(now()) - as_timestamp(last_presence)) / 60 %}
                      • Actual Presence Time: {{ presence_time | round(1) }} min ({{ presence_time | round(1) - adaptive_occupied_delay | round(1) }}min over threshold)
                      • Last Presence Detected: {{ as_timestamp(last_presence) | timestamp_custom('%H:%M:%S') }}
                      {% endif %}
                      • Action: Switching control mode helper to {{ adaptive_target_mode | title }}
                    level: info
                    logger: "blueprints.climate_control.{{ room_name | lower | replace(' ', '_') }}"
                
                - service: input_select.select_option
                  target:
                    entity_id: "{{ control_mode_helper }}"
                  data:
                    option: "{{ adaptive_target_mode | title }}"
                
                # Debug completion log
                - condition: template
                  value_template: "{{ debug_enabled }}"
                - service: system_log.write
                  data:
                    message: "✅ {{ room_name | upper }} ADAPTIVE CONTROL: OCCUPIED actions completed - control mode switched to {{ adaptive_target_mode | title }}"
                    level: debug
                    logger: "blueprints.climate_control.{{ room_name | lower | replace(' ', '_') }}"
                
                - condition: template
                  value_template: "{{ notification_service is defined and notification_service not in [none, ''] }}"
                - service: "{{ notification_service }}"
                  data:
                    title: "{{ room_name }} Adaptive Climate Control"
                    message: "Room occupied - switched to {{ adaptive_target_mode | title }} mode"
            
            # Handle vacant room - switch to Manual mode and turn AC off
            - conditions:
                - condition: template
                  value_template: "{{ control_mode in ['Auto', 'Smart'] and not room_presence_detected }}"
              sequence:
                - condition: template
                  value_template: "{{ debug_enabled }}"
                - service: system_log.write
                  data:
                    message: >
                      🔴 Adaptive Control: VACANT → SWITCHING TO MANUAL & TURNING AC OFF
                      • Current Mode: {{ control_mode }} → Manual
                      • Room Presence: {{ room_presence_detected }}
                      • Required Delay: {{ adaptive_vacant_delay }} min
                      {% set last_presence = states(presence_detected_helper) %}
                      {% if last_presence not in ['unknown', 'unavailable', ''] %}
                      {% set vacant_time = (as_timestamp(now()) - as_timestamp(last_presence)) / 60 %}
                      • Actual Vacant Time: {{ vacant_time | round(1) }} min ({{ vacant_time | round(1) - adaptive_vacant_delay | round(1) }}min over threshold)
                      • Last Presence Detected: {{ as_timestamp(last_presence) | timestamp_custom('%H:%M:%S') }}
                      {% endif %}
                      • Actions: 1) Switch to Manual mode 2) Turn off {{ climate_list | length }} climate entities
                    level: info
                    logger: "blueprints.climate_control.{{ room_name | lower | replace(' ', '_') }}"
                
                - service: input_select.select_option
                  target:
                    entity_id: "{{ control_mode_helper }}"
                  data:
                    option: "Manual"
                
                # Turn off all climate entities
                - repeat:
                    for_each: "{{ climate_list }}"
                    sequence:
                      - condition: template
                        value_template: "{{ debug_enabled }}"
                      - service: system_log.write
                        data:
                          message: "🔴 Adaptive Control: Turning off climate entity {{ repeat.item }} ({{ repeat.index }}/{{ climate_list | length }})"
                          level: debug
                          logger: "blueprints.climate_control.{{ room_name | lower | replace(' ', '_') }}"
                      
                      - service: climate.turn_off
                        target:
                          entity_id: "{{ repeat.item }}"
                
                # Update last mode helper to prevent conflicts
                - service: input_text.set_value
                  target:
                    entity_id: "{{ helper_mode }}"
                  data:
                    value: "adaptive_off"

                # Update mode start time for effectiveness tracking (only if mode changed)
                - condition: template
                  value_template: "{{ dynamic_enabled and helper_mode_time not in [none, '', 'unavailable', 'unknown', []] and helper_mode_time is string and helper_mode_time != '' and last_mode != 'adaptive_off' }}"
                - service: input_datetime.set_datetime
                  target:
                    entity_id: "{{ helper_mode_time }}"
                  data:
                    datetime: "{{ now() }}"
                
                # Debug completion log
                - condition: template
                  value_template: "{{ debug_enabled }}"
                - service: system_log.write
                  data:
                    message: "✅ {{ room_name | upper }} ADAPTIVE CONTROL: VACANT actions completed - mode set to Manual, all {{ climate_list | length }} climate entities turned off, helper set to adaptive_off"
                    level: debug
                    logger: "blueprints.climate_control.{{ room_name | lower | replace(' ', '_') }}"
                
                - condition: template
                  value_template: "{{ notification_service is defined and notification_service not in [none, ''] }}"
                - service: "{{ notification_service }}"
                  data:
                    title: "{{ room_name }} Adaptive Climate Control"
                    message: "Room vacant - switched to Manual mode and turned AC off"

            # REMOVED: Manual mode should NEVER turn off AC based on presence
            # Users in Manual mode have full control - presence doesn't matter
            - conditions:
                - condition: template
                  value_template: "{{ false }}"  # This condition will never trigger
              sequence:
                - condition: template
                  value_template: "{{ debug_enabled }}"
                - service: system_log.write
                  data:
                    message: >
                      🔴 Adaptive Control: ALREADY MANUAL → TURNING AC OFF
                      • Already in Manual mode, room vacant, AC still running
                      • Room Presence: {{ room_presence_detected }}
                      • Required Delay: {{ adaptive_vacant_delay }} min
                      {% set last_presence = states(presence_detected_helper) %}
                      {% if last_presence not in ['unknown', 'unavailable', ''] %}
                      {% set vacant_time = (as_timestamp(now()) - as_timestamp(last_presence)) / 60 %}
                      • Actual Vacant Time: {{ vacant_time | round(1) }} min ({{ vacant_time | round(1) - adaptive_vacant_delay | round(1) }}min over threshold)
                      • Last Presence Detected: {{ as_timestamp(last_presence) | timestamp_custom('%H:%M:%S') }}
                      {% endif %}
                      • Action: Turning off {{ climate_list | length }} climate entities
                    level: info
                    logger: "blueprints.climate_control.{{ room_name | lower | replace(' ', '_') }}"
                
                # Turn off all climate entities
                - repeat:
                    for_each: "{{ climate_list }}"
                    sequence:
                      - condition: template
                        value_template: "{{ debug_enabled }}"
                      - service: system_log.write
                        data:
                          message: "🔴 Adaptive Control: Turning off climate entity {{ repeat.item }} ({{ repeat.index }}/{{ climate_list | length }})"
                          level: debug
                          logger: "blueprints.climate_control.{{ room_name | lower | replace(' ', '_') }}"
                      
                      - service: climate.turn_off
                        target:
                          entity_id: "{{ repeat.item }}"
                
                # Update last mode helper to prevent conflicts
                - service: input_text.set_value
                  target:
                    entity_id: "{{ helper_mode }}"
                  data:
                    value: "adaptive_off"
                
                # Debug completion log
                - condition: template
                  value_template: "{{ debug_enabled }}"
                - service: system_log.write
                  data:
                    message: "✅ {{ room_name | upper }} ADAPTIVE CONTROL: MANUAL+VACANT actions completed - all {{ climate_list | length }} climate entities turned off, helper set to adaptive_off"
                    level: debug
                    logger: "blueprints.climate_control.{{ room_name | lower | replace(' ', '_') }}"
                
                - condition: template
                  value_template: "{{ notification_service is defined and notification_service not in [none, ''] }}"
                - service: "{{ notification_service }}"
                  data:
                    title: "{{ room_name }} Adaptive Climate Control"
                    message: "Room vacant - turned AC off (was already in Manual mode)"

      # Adaptive Control Debug - Log when enabled but conditions not met
      - conditions:
          - condition: template
            value_template: "{{ adaptive_enabled and debug_enabled }}"
          - condition: not
            conditions:
              - condition: or
                conditions:
                  # Check if occupied conditions would be met
                  - condition: and
                    conditions:
                      - condition: template
                        value_template: "{{ control_mode == 'Manual' }}"
                      - condition: template
                        value_template: "{{ room_presence_detected }}"
                      - condition: template
                        value_template: >
                          {% set last_presence = states(presence_detected_helper) %}
                          {% if last_presence not in ['unknown', 'unavailable', ''] %}
                            {% set presence_time = as_timestamp(now()) - as_timestamp(last_presence) %}
                            {{ (presence_time / 60) >= adaptive_occupied_delay }}
                          {% else %}
                            false
                          {% endif %}
                  # Check if vacant conditions would be met
                  - condition: and
                    conditions:
                      - condition: template
                        value_template: "{{ control_mode in ['Auto', 'Smart'] }}"
                      - condition: template
                        value_template: "{{ not room_presence_detected }}"
                      - condition: template
                        value_template: >
                          {% set last_presence = states(presence_detected_helper) %}
                          {% if last_presence not in ['unknown', 'unavailable', ''] %}
                            {% set vacant_time = as_timestamp(now()) - as_timestamp(last_presence) %}
                            {{ (vacant_time / 60) >= adaptive_vacant_delay }}
                          {% else %}
                            false
                          {% endif %}
        sequence:
          - service: system_log.write
            data:
              message: >
                ⏳ Adaptive Control: Conditions not met
                {% set last_presence = states(presence_detected_helper) %}
                {% if last_presence not in ['unknown', 'unavailable', ''] %}
                {% set time_diff = (as_timestamp(now()) - as_timestamp(last_presence)) / 60 %}
                {% if control_mode == 'Manual' and room_presence_detected %}
                • OCCUPIED scenario: Need {{ adaptive_occupied_delay }}min, have {{ time_diff | round(1) }}min ({{ (adaptive_occupied_delay - time_diff) | round(1) }}min remaining)
                {% elif control_mode in ['Auto', 'Smart'] and not room_presence_detected %}
                • VACANT scenario: Need {{ adaptive_vacant_delay }}min, have {{ time_diff | round(1) }}min ({{ (adaptive_vacant_delay - time_diff) | round(1) }}min remaining)
                {% else %}
                • Mode: {{ control_mode }}, Presence: {{ room_presence_detected }}, Time: {{ time_diff | round(1) }}min - No matching scenario
                {% endif %}
                {% else %}
                • No valid presence timestamp available from helper: {{ presence_detected_helper }}
                {% endif %}
              level: debug
              logger: "blueprints.climate_control.{{ room_name | lower | replace(' ', '_') }}"

      # Manual Mode - Immediately clear automation states when detected
      - conditions:
          - condition: template
            value_template: "{{ control_mode == 'Manual' }}"
          - condition: template
            value_template: "{{ last_mode in ['stability_off', 'stability_eco', 'smart_off', 'smart_eco', 'smart_maintain', 'adaptive_off', 'eco', 'cooling', 'heating'] }}"
        sequence:
          - condition: template
            value_template: "{{ debug_enabled }}"
          - service: system_log.write
            data:
              message: >
                🎮 Manual Mode: IMMEDIATE Control Restored
                🔄 Clearing "{{ last_mode }}" state instantly
                💡 You now have full manual control - no automation interference
                
                📋 Technical: Manual mode = instant automation disable
              level: warning
              logger: "blueprints.climate_control.{{ room_name | lower | replace(' ', '_') }}"

          - service: input_text.set_value
            target:
              entity_id: !input helper_last_mode
            data:
              value: "manual"

          # Update mode start time for effectiveness tracking (only if mode changed)
          - condition: template
            value_template: "{{ dynamic_enabled and helper_mode_time not in [none, '', 'unavailable', 'unknown', []] and helper_mode_time is string and helper_mode_time != '' and last_mode != 'manual' }}"
          - service: input_datetime.set_datetime
            target:
              entity_id: "{{ helper_mode_time }}"
            data:
              datetime: "{{ now() }}"
            continue_on_error: true
          
          # CRITICAL: Update helper_change to start 3-hour override protection
          - condition: template
            value_template: "{{ helper_change not in [none, '', 'unavailable', 'unknown', []] and helper_change is string and helper_change != '' }}"
          - service: input_datetime.set_datetime
            target:
              entity_id: "{{ helper_change }}"
            data:
              datetime: "{{ now() }}"
              
          # Turn off AC when switching to Manual mode from automation-controlled state
          - service: climate.turn_off
            target:
              entity_id: !input climate_entities

      # Smart Mode room absence (room empty but still home)
      - conditions:
          - condition: template
            value_template: "{{ control_mode == 'Smart' }}"
          - condition: template
            value_template: "{{ not room_presence_detected }}"
          - condition: template
            value_template: "{{ minutes_since_presence >= presence_timeout }}"
          - condition: template
            value_template: "{{ proximity_zone == 'home' or anyone_home }}"
          - condition: template
            value_template: "{{ time_since_change > offtime_min }}"
        sequence:
          - condition: template
            value_template: "{{ debug_enabled }}"
          - service: system_log.write
            data:
              message: >
                🚪 Smart Mode: Room Empty
                💡 Nobody detected in room for {{ minutes_since_presence | round(0) }} minutes
                🏠 You're still home, so switching to {{ smart_behavior|upper }} mode to save energy
                
                📋 Technical: {{ time_since_change | round(1) }}min since last change
              level: warning
              logger: "blueprints.climate_control.{{ room_name | lower | replace(' ', '_') }}"
          
          - choose:
              - conditions:
                  - condition: template
                    value_template: "{{ smart_behavior == 'off' }}"
                sequence:
                  - service: climate.turn_off
                    target:
                      entity_id: !input climate_entities
              
              - conditions:
                  - condition: template
                    value_template: "{{ smart_behavior == 'eco' }}"
                sequence:
                  # Set HVAC mode with auto preference and fallback for room eco mode
                  - repeat:
                      for_each: "{{ climate_list }}"
                      sequence:
                        - variables:
                            current_entity: "{{ repeat.item }}"
                            available_modes: "{{ state_attr(repeat.item, 'hvac_modes') | list }}"
                        - service: climate.set_hvac_mode
                          target:
                            entity_id: "{{ current_entity }}"
                          data:
                            hvac_mode: >
                              {% if 'auto' in available_modes %}
                                auto
                              {% elif 'Auto' in available_modes %}
                                Auto
                              {% elif 'heat_cool' in available_modes %}
                                heat_cool
                              {% elif 'Heat/Cool' in available_modes %}
                                heat_cool
                              {% elif current_temp > comfort_max_temp and 'cool' in available_modes %}
                                cool
                              {% elif current_temp < comfort_min_temp and 'heat' in available_modes %}
                                heat
                              {% else %}
                                {{ available_modes[0] if available_modes else 'off' }}
                              {% endif %}
                  
                  - service: climate.set_temperature
                    target:
                      entity_id: !input climate_entities
                    data:
                      temperature: >
                        {% if current_temp > comfort_max_temp %}
                          {{ comfort_max_temp + eco_mode_setpoint_offset }}
                        {% elif current_temp < comfort_min_temp %}
                          {{ comfort_min_temp - eco_mode_setpoint_offset }}
                        {% else %}
                          {{ target_temp }}
                        {% endif %}

                  - service: system_log.write
                    data:
                      message: >
                        🌡️ {{ room_name | upper }} TEMP SET: Smart Room Eco Mode
                        Set AC to: {{ ((comfort_max_temp + eco_mode_setpoint_offset) if current_temp > comfort_max_temp else (comfort_min_temp - eco_mode_setpoint_offset) if current_temp < comfort_min_temp else target_temp) | round(0) }}°C (rounded for AC compatibility)
                        Actual value: {{ ((comfort_max_temp + eco_mode_setpoint_offset) if current_temp > comfort_max_temp else (comfort_min_temp - eco_mode_setpoint_offset) if current_temp < comfort_min_temp else target_temp) | round(1) }}°C
                        Reason: {{ 'Eco cooling (comfort max +' + eco_mode_setpoint_offset|string + '°C)' if current_temp > comfort_max_temp else 'Eco heating (comfort min -' + eco_mode_setpoint_offset|string + '°C)' if current_temp < comfort_min_temp else 'Direct target (within comfort zone)' }}
                        Current: {{ current_temp | round(1) }}°C → Comfort range: {{ comfort_min_temp | round(1) }}-{{ comfort_max_temp | round(1) }}°C
                      level: warning
                      logger: "blueprints.climate_control.{{ room_name | lower | replace(' ', '_') }}"
              
              - conditions:
                  - condition: template
                    value_template: "{{ smart_behavior == 'maintain' }}"
                sequence:
                  - service: climate.set_temperature
                    target:
                      entity_id: !input climate_entities
                    data:
                      temperature: "{{ target_temp }}"

                  - service: system_log.write
                    data:
                      message: >
                        🌡️ {{ room_name | upper }} TEMP SET: Smart Room Maintain
                        Set AC to: {{ target_temp | round(0) }}°C (rounded for AC compatibility)
                        Actual value: {{ target_temp | round(1) }}°C
                        Reason: Smart mode maintain current temperature (room empty but still home)
                        Current: {{ current_temp | round(1) }}°C → Target: {{ target_temp | round(1) }}°C
                      level: warning
                      logger: "blueprints.climate_control.{{ room_name | lower | replace(' ', '_') }}"
          
          - service: input_text.set_value
            target:
              entity_id: !input helper_last_mode
            data:
              value: "smart_{{ smart_behavior }}"

          # Update mode start time for effectiveness tracking (only if mode changed)
          - condition: template
            value_template: "{{ dynamic_enabled and helper_mode_time not in [none, '', 'unavailable', 'unknown', []] and helper_mode_time is string and helper_mode_time != '' and last_mode != 'smart_' + smart_behavior }}"
          - service: input_datetime.set_datetime
            target:
              entity_id: "{{ helper_mode_time }}"
            data:
              datetime: "{{ now() }}"
            continue_on_error: true
          
          - condition: template
            value_template: "{{ enable_notifications }}"
          
          - service: !input notification_service
            data:
              title: "{{ room_name }} Climate Control"
              message: "Smart Mode: {{ smart_behavior }} (room empty {{ minutes_since_presence | round(0) }}min, {{ current_temp }}°C)"

      # Away mode (Global - nobody home)
      - conditions:
          - condition: template
            value_template: "{{ enable_away_mode }}"
          - condition: template
            value_template: "{{ not should_activate }}"
          - condition: template
            value_template: "{{ time_since_change > offtime_min }}"
        sequence:
          - condition: template
            value_template: "{{ debug_enabled }}"
          - service: system_log.write
            data:
              message: >
                🔍 {{ room_name | upper }} CLIMATE DEBUG: Away mode activated
                Action: {{ away_action }}
                Nobody home detected
                Time since last change: {{ time_since_change | round(1) }} min
              level: warning
              logger: "blueprints.climate_control.{{ room_name | lower | replace(' ', '_') }}"
          
          - choose:
              - conditions:
                  - condition: template
                    value_template: "{{ away_action == 'off' }}"
                sequence:
                  - service: climate.turn_off
                    target:
                      entity_id: !input climate_entities
              
              - conditions:
                  - condition: template
                    value_template: "{{ away_action == 'eco' }}"
                sequence:
                  # Set HVAC mode with auto preference and fallback for away eco mode
                  - repeat:
                      for_each: "{{ climate_list }}"
                      sequence:
                        - variables:
                            current_entity: "{{ repeat.item }}"
                            available_modes: "{{ state_attr(repeat.item, 'hvac_modes') | list }}"
                        - service: climate.set_hvac_mode
                          target:
                            entity_id: "{{ current_entity }}"
                          data:
                            hvac_mode: >
                              {% if 'auto' in available_modes %}
                                auto
                              {% elif 'Auto' in available_modes %}
                                Auto
                              {% elif 'heat_cool' in available_modes %}
                                heat_cool
                              {% elif 'Heat/Cool' in available_modes %}
                                heat_cool
                              {% elif current_temp > comfort_max_temp and 'cool' in available_modes %}
                                cool
                              {% elif current_temp < comfort_min_temp and 'heat' in available_modes %}
                                heat
                              {% else %}
                                {{ available_modes[0] if available_modes else 'off' }}
                              {% endif %}
                  
                  - service: climate.set_temperature
                    target:
                      entity_id: !input climate_entities
                    data:
                      temperature: >
                        {% if current_temp > comfort_max_temp %}
                          {{ cooling_target_temp + eco_mode_setpoint_offset }}
                        {% else %}
                          {{ heating_target_temp - eco_mode_setpoint_offset }}
                        {% endif %}

                  - service: system_log.write
                    data:
                      message: >
                        🌡️ {{ room_name | upper }} TEMP SET: Away Eco Mode
                        Set AC to: {{ ((cooling_target_temp + eco_mode_setpoint_offset) if current_temp > comfort_max_temp else (heating_target_temp - eco_mode_setpoint_offset)) | round(0) }}°C (rounded for AC compatibility)
                        Actual value: {{ ((cooling_target_temp + eco_mode_setpoint_offset) if current_temp > comfort_max_temp else (heating_target_temp - eco_mode_setpoint_offset)) | round(1) }}°C
                        Reason: {{ 'Eco cooling (target +' + eco_mode_setpoint_offset|string + '°C offset)' if current_temp > comfort_max_temp else 'Eco heating (target -' + eco_mode_setpoint_offset|string + '°C offset)' }}
                        Current: {{ current_temp | round(1) }}°C → Comfort range: {{ comfort_min_temp | round(1) }}-{{ comfort_max_temp | round(1) }}°C
                      level: warning
                      logger: "blueprints.climate_control.{{ room_name | lower | replace(' ', '_') }}"
                  
                  # Set auto fan mode for efficient away eco mode
                  - repeat:
                      for_each: "{{ climate_list }}"
                      sequence:
                        - variables:
                            current_entity: "{{ repeat.item }}"
                            available_fans: "{{ state_attr(repeat.item, 'fan_modes') | list }}"
                        - service: climate.set_fan_mode
                          target:
                            entity_id: "{{ current_entity }}"
                          data:
                            fan_mode: >
                              {% if 'Auto' in available_fans %}
                                Auto
                              {% elif 'auto' in available_fans %}
                                auto
                              {% elif 'low' in available_fans %}
                                low
                              {% elif 'Quiet' in available_fans %}
                                Quiet
                              {% elif 'Silence' in available_fans %}
                                Silence
                              {% elif '1' in available_fans %}
                                1
                              {% elif 'Level 1' in available_fans %}
                                Level 1
                              {% else %}
                                {{ available_fans[0] if available_fans else 'Auto' }}
                              {% endif %}
          
          - condition: template
            value_template: "{{ enable_notifications }}"
          
          - service: !input notification_service
            data:
              title: "Climate Away Mode"
              message: "{{ away_action | title }} mode activated - nobody home"
      
      # High cooling mode - Maximum cooling when temperature is very high
      # Activates at cooling_high_temp (e.g., 26°C) but continues until target reached
      # Uses maximum fan speed for rapid cooling
      # NEW: Prevents short cycling by using target-based hysteresis logic
      - conditions:
          - condition: template
            value_template: "{{ should_activate }}"
          - condition: template
            value_template: >
              {% if last_mode == 'cooling' %}
                {# Already cooling - continue until target reached #}
                {{ current_temp > target_temp }}
              {% else %}
                {# Not cooling - start if high threshold exceeded #}
                {% set last_transition_val = states(helper_last_transition) if helper_last_transition else 'none' %}
                {% if last_transition_val == 'heating' %}
                  {{ current_temp >= (cooling_high_temp + hysteresis_tolerance) }}
                {% else %}
                  {{ current_temp >= cooling_high_temp }}
                {% endif %}
              {% endif %}
          - condition: template
            value_template: "{{ time_since_change > runtime_min or last_mode != 'cooling' }}"
        sequence:
          - service: system_log.write
            data:
              message: "🔥 {{ room_name | upper }} HIGH COOLING ACTIVATED - Temperature {{ current_temp }}°C exceeds {{ cooling_high_temp }}°C (high threshold)"
              level: warning
              logger: "blueprints.climate_control.{{ room_name | lower | replace(' ', '_') }}"
          - condition: template
            value_template: "{{ debug_enabled }}"
          - service: system_log.write
            data:
              message: >
                🔍 {{ room_name | upper }} CLIMATE DEBUG: HIGH COOLING MODE
                Temperature: {{ current_temp }}°C > {{ cooling_high_temp }}°C
                Target: {{ cooling_target_temp }}°C
                Fan: Maximum
              level: warning
              logger: "blueprints.climate_control.{{ room_name | lower | replace(' ', '_') }}"
          
          # Set HVAC mode with auto preference and fallback
          - repeat:
              for_each: "{{ climate_list }}"
              sequence:
                - variables:
                    current_entity: "{{ repeat.item }}"
                    available_modes: "{{ state_attr(repeat.item, 'hvac_modes') | list }}"
                - service: climate.set_hvac_mode
                  target:
                    entity_id: "{{ current_entity }}"
                  data:
                    hvac_mode: >
                      {% if 'cool' in available_modes %}
                        cool
                      {% elif 'Cool' in available_modes %}
                        Cool
                      {% elif 'auto' in available_modes %}
                        auto
                      {% elif 'Auto' in available_modes %}
                        Auto
                      {% elif 'heat_cool' in available_modes %}
                        heat_cool
                      {% elif 'Heat/Cool' in available_modes %}
                        Heat/Cool
                      {% else %}
                        {{ available_modes[0] if available_modes else 'off' }}
                      {% endif %}
          
          - service: climate.set_temperature
            target:
              entity_id: !input climate_entities
            data:
              temperature: >
                {% if gradual_enabled %}
                  {{ [weather_adjusted_target, current_temp - 2] | min }}
                {% else %}
                  {{ weather_adjusted_target }}
                {% endif %}

          - service: system_log.write
            data:
              message: >
                🌡️ {{ room_name | upper }} TEMP SET: High Cooling
                Set AC to: {{ ([weather_adjusted_target, current_temp - 2] | min if gradual_enabled else weather_adjusted_target) | round(0) }}°C (rounded for AC compatibility)
                Actual value: {{ ([weather_adjusted_target, current_temp - 2] | min if gradual_enabled else weather_adjusted_target) | round(1) }}°C
                Reason: {{ 'Gradual adjustment (target ' + weather_adjusted_target|string + '°C, current-2°C = ' + (current_temp-2)|round(1)|string + '°C, chose minimum)' if gradual_enabled else 'Direct target temperature' }}
                Current: {{ current_temp | round(1) }}°C → Target: {{ weather_adjusted_target | round(1) }}°C
              level: warning
              logger: "blueprints.climate_control.{{ room_name | lower | replace(' ', '_') }}"
          
          # Update hysteresis tracking - we're now cooling
          - condition: template
            value_template: "{{ helper_last_transition is defined and helper_last_transition != none }}"
          - service: input_text.set_value
            target:
              entity_id: "{{ helper_last_transition }}"
            data:
              value: "cooling"
          
          # Set fan mode with compatibility check
          - repeat:
              for_each: "{{ climate_list }}"
              sequence:
                - variables:
                    current_entity: "{{ repeat.item }}"
                    available_fans: "{{ state_attr(repeat.item, 'fan_modes') | list }}"
                    desired_max_fan: !input fan_speed_max
                - service: climate.set_fan_mode
                  target:
                    entity_id: "{{ current_entity }}"
                  data:
                    fan_mode: >
                      {% if desired_max_fan in available_fans %}
                        {{ desired_max_fan }}
                      {% elif '5' in available_fans %}
                        5
                      {% elif 'Level 5' in available_fans %}
                        Level 5
                      {% elif 'high' in available_fans %}
                        high
                      {% elif 'Auto' in available_fans %}
                        Auto
                      {% elif 'auto' in available_fans %}
                        auto
                      {% else %}
                        {{ available_fans[-1] if available_fans else 'Auto' }}
                      {% endif %}
          
          # Set swing mode with compatibility check
          - repeat:
              for_each: "{{ climate_list }}"
              sequence:
                - variables:
                    current_entity: "{{ repeat.item }}"
                    available_swings: "{{ state_attr(repeat.item, 'swing_modes') | list }}"
                    desired_swing: !input swing_mode_active
                - service: climate.set_swing_mode
                  target:
                    entity_id: "{{ current_entity }}"
                  data:
                    swing_mode: >
                      {% if desired_swing in available_swings %}
                        {{ desired_swing }}
                      {% elif desired_swing == 'both' and '3D' in available_swings %}
                        3D
                      {% elif desired_swing == 'both' and 'Both' in available_swings %}
                        Both
                      {% elif desired_swing == 'vertical' and 'Vertical' in available_swings %}
                        Vertical
                      {% elif desired_swing == 'Vertical' and 'vertical' in available_swings %}
                        vertical
                      {% elif desired_swing == 'horizontal' and 'Horizontal' in available_swings %}
                        Horizontal
                      {% elif desired_swing == 'Horizontal' and 'horizontal' in available_swings %}
                        horizontal
                      {% elif 'both' in available_swings %}
                        both
                      {% elif 'Both' in available_swings %}
                        Both
                      {% elif '3D' in available_swings %}
                        3D
                      {% elif 'Vertical' in available_swings %}
                        Vertical
                      {% elif 'vertical' in available_swings %}
                        vertical
                      {% else %}
                        {{ available_swings[0] if available_swings else 'off' }}
                      {% endif %}
          
          - service: input_text.set_value
            target:
              entity_id: !input helper_last_mode
            data:
              value: "cooling"
          
          # Update mode start time for effectiveness tracking (only if it exists)
          - condition: template
            value_template: "{{ dynamic_enabled and helper_mode_time not in [none, '', 'unavailable', 'unknown', []] and helper_mode_time is string and helper_mode_time != '' and last_mode != 'cooling' }}"
          - service: input_datetime.set_datetime
            target:
              entity_id: "{{ helper_mode_time }}"
            data:
              datetime: "{{ now() }}"
            continue_on_error: true
          
          - condition: template
            value_template: "{{ enable_notifications }}"
          
          - service: !input notification_service
            data:
              title: "{{ room_name }} Climate Control"
              message: "High cooling mode - {{ current_temp }}°C"
      
      # Medium cooling mode
      - conditions:
          - condition: template
            value_template: "{{ should_activate }}"
          - condition: template
            value_template: "{{ current_temp >= cooling_medium_temp and current_temp < cooling_high_temp }}"
          - condition: template
            value_template: "{{ time_since_change > runtime_min or last_mode != 'cooling' }}"
        sequence:
          - condition: template
            value_template: "{{ debug_enabled }}"
          - service: system_log.write
            data:
              message: >
                🔍 {{ room_name | upper }} CLIMATE DEBUG: MEDIUM COOLING MODE
                Temperature: {{ current_temp }}°C ({{ cooling_medium_temp }}-{{ cooling_high_temp }}°C range)
                Target: {{ cooling_target_temp }}°C
                Fan: Auto/Medium (efficient)
                
                MANUAL MODE DEBUG:
                Control Mode: {{ control_mode }}
                Should Activate: {{ should_activate }}
                Extreme Temp Detected: {{ extreme_temp_detected }}
                Room Presence: {{ room_presence_detected }}
                Approaching Home: {{ approaching_home }}
                Extreme High Threshold: {{ extreme_high }}°C
              level: warning
              logger: "blueprints.climate_control.{{ room_name | lower | replace(' ', '_') }}"
          
          # Set HVAC mode to auto for efficient medium cooling
          - repeat:
              for_each: "{{ climate_list }}"
              sequence:
                - variables:
                    current_entity: "{{ repeat.item }}"
                    available_modes: "{{ state_attr(repeat.item, 'hvac_modes') | list }}"
                - service: climate.set_hvac_mode
                  target:
                    entity_id: "{{ current_entity }}"
                  data:
                    hvac_mode: >
                      {% if 'auto' in available_modes %}
                        auto
                      {% elif 'heat_cool' in available_modes %}
                        heat_cool
                      {% elif 'cool' in available_modes %}
                        cool
                      {% else %}
                        {{ available_modes[0] if available_modes else 'off' }}
                      {% endif %}
          
          - service: climate.set_temperature
            target:
              entity_id: !input climate_entities
            data:
              temperature: >
                {% if gradual_enabled %}
                  {{ [cooling_target_temp, current_temp - 1] | min }}
                {% else %}
                  {{ cooling_target_temp }}
                {% endif %}

          - service: system_log.write
            data:
              message: >
                🌡️ {{ room_name | upper }} TEMP SET: Medium Cooling
                Set AC to: {{ ([cooling_target_temp, current_temp - 1] | min if gradual_enabled else cooling_target_temp) | round(0) }}°C (rounded for AC compatibility)
                Actual value: {{ ([cooling_target_temp, current_temp - 1] | min if gradual_enabled else cooling_target_temp) | round(1) }}°C
                Reason: {{ 'Gradual adjustment (target ' + cooling_target_temp|string + '°C, current-1°C = ' + (current_temp-1)|round(1)|string + '°C, chose minimum)' if gradual_enabled else 'Direct target temperature' }}
                Current: {{ current_temp | round(1) }}°C → Target: {{ cooling_target_temp | round(1) }}°C
              level: warning
              logger: "blueprints.climate_control.{{ room_name | lower | replace(' ', '_') }}"
          
          # Set auto/medium fan mode for efficient cooling
          - repeat:
              for_each: "{{ climate_list }}"
              sequence:
                - variables:
                    current_entity: "{{ repeat.item }}"
                    available_fans: "{{ state_attr(repeat.item, 'fan_modes') | list }}"
                - service: climate.set_fan_mode
                  target:
                    entity_id: "{{ current_entity }}"
                  data:
                    fan_mode: >
                      {% if 'Auto' in available_fans %}
                        Auto
                      {% elif 'auto' in available_fans %}
                        auto
                      {% elif 'medium' in available_fans %}
                        medium
                      {% elif 'Level 3' in available_fans %}
                        Level 3
                      {% elif '3' in available_fans %}
                        3
                      {% else %}
                        {{ available_fans[-1] if available_fans else 'Auto' }}
                      {% endif %}
          
          # Set swing mode with compatibility check
          - repeat:
              for_each: "{{ climate_list }}"
              sequence:
                - variables:
                    current_entity: "{{ repeat.item }}"
                    available_swings: "{{ state_attr(repeat.item, 'swing_modes') | list }}"
                    desired_swing: !input swing_mode_active
                - service: climate.set_swing_mode
                  target:
                    entity_id: "{{ current_entity }}"
                  data:
                    swing_mode: >
                      {% if desired_swing in available_swings %}
                        {{ desired_swing }}
                      {% elif desired_swing == 'both' and '3D' in available_swings %}
                        3D
                      {% elif desired_swing == 'both' and 'Both' in available_swings %}
                        Both
                      {% elif desired_swing == 'vertical' and 'Vertical' in available_swings %}
                        Vertical
                      {% elif desired_swing == 'Vertical' and 'vertical' in available_swings %}
                        vertical
                      {% elif desired_swing == 'horizontal' and 'Horizontal' in available_swings %}
                        Horizontal
                      {% elif desired_swing == 'Horizontal' and 'horizontal' in available_swings %}
                        horizontal
                      {% elif 'both' in available_swings %}
                        both
                      {% elif 'Both' in available_swings %}
                        Both
                      {% elif '3D' in available_swings %}
                        3D
                      {% elif 'Vertical' in available_swings %}
                        Vertical
                      {% elif 'vertical' in available_swings %}
                        vertical
                      {% else %}
                        {{ available_swings[0] if available_swings else 'off' }}
                      {% endif %}
          
          - service: input_text.set_value
            target:
              entity_id: !input helper_last_mode
            data:
              value: "cooling"
          
          # Update mode start time for effectiveness tracking (only if it exists)
          - condition: template
            value_template: "{{ dynamic_enabled and helper_mode_time not in [none, '', 'unavailable', 'unknown', []] and helper_mode_time is string and helper_mode_time != '' and last_mode != 'cooling' }}"
          - service: input_datetime.set_datetime
            target:
              entity_id: "{{ helper_mode_time }}"
            data:
              datetime: "{{ now() }}"
            continue_on_error: true
      
      # Low heating mode - Gentle heating when temperature just falls below comfort zone
      # Activates at heating_low_temp (e.g., 19.9°C) but continues until target reached
      # Uses low fan speed for maximum efficiency with minimal power consumption
      # NEW: Prevents short cycling by using target-based hysteresis logic
      - conditions:
          - condition: template
            value_template: "{{ should_activate }}"
          - condition: template
            value_template: >
              {% if last_mode == 'heating' %}
                {# Already heating - continue until target reached or moved to higher tier #}
                {{ current_temp < target_temp and current_temp > heating_medium_temp }}
              {% else %}
                {# Not heating - start if low threshold exceeded and above medium #}
                {{ current_temp <= heating_low_temp and current_temp > heating_medium_temp }}
              {% endif %}
          - condition: template
            value_template: "{{ time_since_change > runtime_min or last_mode != 'heating' }}"
        sequence:
          - service: system_log.write
            data:
              message: >
                {% set current_mode = states(helper_mode) | default('off') %}
                🌡️ {{ room_name | upper }} LOW HEATING ACTIVATED
                {% if current_mode == 'heating_low' %}
                📊 CONTINUE MODE: Already heating low, continuing until target
                • Current: {{ current_temp }}°C → Target: {{ target_temp }}°C ({{ (target_temp - current_temp) | round(1) }}°C to go)
                {% else %}
                🚀 START MODE: Starting LOW heating
                • Trigger: {{ current_temp }}°C ≤ {{ heating_low_temp }}°C (threshold)
                • Target: {{ target_temp }}°C ({{ (target_temp - current_temp) | round(1) }}°C to heat)
                {% endif %}
                • Last Mode: {{ current_mode }}
              level: warning
              logger: "blueprints.climate_control.{{ room_name | lower | replace(' ', '_') }}"
          - condition: template
            value_template: "{{ debug_enabled }}"
          - service: system_log.write
            data:
              message: >
                🔍 {{ room_name | upper }} CLIMATE DEBUG: LOW HEATING MODE
                Temperature: {{ current_temp }}°C < {{ heating_low_temp }}°C
                Target: {{ heating_target_temp }}°C
                Fan: Low/Minimum (efficient)
              level: warning
              logger: "blueprints.climate_control.{{ room_name | lower | replace(' ', '_') }}"
          
          - service: climate.set_hvac_mode
            target:
              entity_id: !input climate_entities
            data:
              hvac_mode: >
                {% if current_temp > comfort_max_temp %}
                  cool
                {% elif current_temp < comfort_min_temp %}
                  heat
                {% else %}
                  heat_cool
                {% endif %}
          
          - service: climate.set_temperature
            target:
              entity_id: !input climate_entities
            data:
              temperature: "{{ heating_target_temp }}"

          - service: system_log.write
            data:
              message: >
                🌡️ {{ room_name | upper }} TEMP SET: Low Heating
                Set AC to: {{ heating_target_temp | round(0) }}°C (rounded for AC compatibility)
                Actual value: {{ heating_target_temp | round(1) }}°C
                Reason: Direct target temperature (no gradual adjustment for heating)
                Current: {{ current_temp | round(1) }}°C → Target: {{ heating_target_temp | round(1) }}°C
              level: warning
              logger: "blueprints.climate_control.{{ room_name | lower | replace(' ', '_') }}"
          
          # Set low fan mode with compatibility check
          - repeat:
              for_each: "{{ climate_list }}"
              sequence:
                - variables:
                    current_entity: "{{ repeat.item }}"
                    available_fans: "{{ state_attr(repeat.item, 'fan_modes') | list }}"
                    
                    # Smart low fan mode selection
                    selected_fan_mode: >
                      {% set fans = state_attr(repeat.item, 'fan_modes') | list %}
                      
                      {# Try low speed equivalents in order of preference #}
                      {% if 'low' in fans %}
                        low
                      {% elif 'Low' in fans %}
                        Low
                      {% elif '1' in fans %}
                        1
                      {% elif 'Level 1' in fans %}
                        Level 1
                      {% elif 'quiet' in fans %}
                        quiet
                      {% elif 'Quiet' in fans %}
                        Quiet
                      {% elif 'eco' in fans %}
                        eco
                      {% elif 'Eco' in fans %}
                        Eco
                      {% elif 'auto' in fans %}
                        auto
                      {% elif 'Auto' in fans %}
                        Auto
                      {# Fallback to available options #}
                      {% elif '2' in fans %}
                        2
                      {% elif 'Level 2' in fans %}
                        Level 2
                      {% else %}
                        {{ fans[0] if fans else 'Auto' }}
                      {% endif %}
                
                - service: climate.set_fan_mode
                  target:
                    entity_id: "{{ current_entity }}"
                  data:
                    fan_mode: "{{ selected_fan_mode }}"
                
                - condition: template
                  value_template: "{{ debug_enabled }}"
                - service: system_log.write
                  data:
                    message: >
                      🔍 SMART LOW HEAT FAN: {{ repeat.item }}
                      • Available: {{ available_fans | join(', ') }}
                      • Selected: {{ selected_fan_mode }}
                      • Reason: Low fan speed for gentle heating
                    level: debug
                    logger: "blueprints.climate_control.{{ room_name | lower | replace(' ', '_') }}"
          
          # Set swing mode with compatibility check
          - repeat:
              for_each: "{{ climate_list }}"
              sequence:
                - variables:
                    current_entity: "{{ repeat.item }}"
                    available_swings: "{{ state_attr(repeat.item, 'swing_modes') | list }}"
                    desired_swing: !input swing_mode_active
                - service: climate.set_swing_mode
                  target:
                    entity_id: "{{ current_entity }}"
                  data:
                    swing_mode: >
                      {% if desired_swing in available_swings %}
                        {{ desired_swing }}
                      {% elif desired_swing == 'both' and '3D' in available_swings %}
                        3D
                      {% elif desired_swing == 'both' and 'Both' in available_swings %}
                        Both
                      {% elif desired_swing == 'vertical' and 'Vertical' in available_swings %}
                        Vertical
                      {% elif desired_swing == 'Vertical' and 'vertical' in available_swings %}
                        vertical
                      {% elif desired_swing == 'horizontal' and 'Horizontal' in available_swings %}
                        Horizontal
                      {% elif desired_swing == 'Horizontal' and 'horizontal' in available_swings %}
                        horizontal
                      {% elif 'both' in available_swings %}
                        both
                      {% elif 'Both' in available_swings %}
                        Both
                      {% elif '3D' in available_swings %}
                        3D
                      {% elif 'Vertical' in available_swings %}
                        Vertical
                      {% elif 'vertical' in available_swings %}
                        vertical
                      {% else %}
                        {{ available_swings[0] if available_swings else 'off' }}
                      {% endif %}
          
          - service: input_text.set_value
            target:
              entity_id: !input helper_last_mode
            data:
              value: "heating"
          
          # Update hysteresis tracking - we're now heating
          - condition: template
            value_template: "{{ helper_last_transition is defined and helper_last_transition != none }}"
          - service: input_text.set_value
            target:
              entity_id: "{{ helper_last_transition }}"
            data:
              value: "heating"
          
          # Update mode start time for effectiveness tracking (only if it exists)
          - condition: template
            value_template: "{{ dynamic_enabled and helper_mode_time not in [none, '', 'unavailable', 'unknown', []] and helper_mode_time is string and helper_mode_time != '' and last_mode != 'cooling' }}"
          - service: input_datetime.set_datetime
            target:
              entity_id: "{{ helper_mode_time }}"
            data:
              datetime: "{{ now() }}"
            continue_on_error: true
          
          - condition: template
            value_template: "{{ enable_notifications }}"
          
          - service: !input notification_service
            data:
              title: "{{ room_name }} Climate Control"
              message: "Low heating mode - {{ current_temp }}°C"
      
      # Medium heating mode - Balanced heating when temperature drops further
      # Activates at heating_medium_temp (e.g., 19°C) for moderate heating
      # Uses medium fan speed for balanced efficiency and performance
      - conditions:
          - condition: template
            value_template: >
              {% if last_mode == 'heating_medium' %}
                {# Already heating medium - continue until target reached or moved to higher tier #}
                {{ current_temp < target_temp and current_temp > heating_high_temp }}
              {% else %}
                {# Not heating medium - start if medium threshold exceeded and above high #}
                {{ should_activate and current_temp <= heating_medium_temp and current_temp > heating_high_temp and (time_since_change > runtime_min or last_mode != 'heating_medium') }}
              {% endif %}
        sequence:
          - condition: template
            value_template: "{{ debug_enabled }}"
          - service: system_log.write
            data:
              message: >
                🔍 {{ room_name | upper }} CLIMATE DEBUG: MEDIUM HEATING MODE
                Temperature: {{ current_temp }}°C ({{ heating_low_temp }}-{{ heating_medium_temp }}°C range)
                Target: {{ heating_target_temp }}°C
                Fan: Auto/Medium (efficient)
              level: warning
              logger: "blueprints.climate_control.{{ room_name | lower | replace(' ', '_') }}"
          
          # Set HVAC mode to auto for efficient medium heating
          - repeat:
              for_each: "{{ climate_list }}"
              sequence:
                - variables:
                    current_entity: "{{ repeat.item }}"
                    available_modes: "{{ state_attr(repeat.item, 'hvac_modes') | list }}"
                - service: climate.set_hvac_mode
                  target:
                    entity_id: "{{ current_entity }}"
                  data:
                    hvac_mode: >
                      {% if 'auto' in available_modes %}
                        auto
                      {% elif 'heat_cool' in available_modes %}
                        heat_cool
                      {% elif 'heat' in available_modes %}
                        heat
                      {% else %}
                        {{ available_modes[0] if available_modes else 'off' }}
                      {% endif %}
          
          - service: climate.set_temperature
            target:
              entity_id: !input climate_entities
            data:
              temperature: "{{ heating_target_temp }}"

          - service: system_log.write
            data:
              message: >
                🌡️ {{ room_name | upper }} TEMP SET: Medium Heating
                Set AC to: {{ heating_target_temp | round(0) }}°C (rounded for AC compatibility)
                Actual value: {{ heating_target_temp | round(1) }}°C
                Reason: Direct target temperature (no gradual adjustment for heating)
                Current: {{ current_temp | round(1) }}°C → Target: {{ heating_target_temp | round(1) }}°C
              level: warning
              logger: "blueprints.climate_control.{{ room_name | lower | replace(' ', '_') }}"
          
          # Set auto/medium fan mode for efficient heating
          - repeat:
              for_each: "{{ climate_list }}"
              sequence:
                - variables:
                    current_entity: "{{ repeat.item }}"
                    available_fans: "{{ state_attr(repeat.item, 'fan_modes') | list }}"
                - service: climate.set_fan_mode
                  target:
                    entity_id: "{{ current_entity }}"
                  data:
                    fan_mode: >
                      {% if 'Auto' in available_fans %}
                        Auto
                      {% elif 'auto' in available_fans %}
                        auto
                      {% elif 'medium' in available_fans %}
                        medium
                      {% elif 'Level 3' in available_fans %}
                        Level 3
                      {% elif '3' in available_fans %}
                        3
                      {% else %}
                        {{ available_fans[-1] if available_fans else 'Auto' }}
                      {% endif %}
          
          # Set swing mode with compatibility check
          - repeat:
              for_each: "{{ climate_list }}"
              sequence:
                - variables:
                    current_entity: "{{ repeat.item }}"
                    available_swings: "{{ state_attr(repeat.item, 'swing_modes') | list }}"
                    desired_swing: !input swing_mode_active
                - service: climate.set_swing_mode
                  target:
                    entity_id: "{{ current_entity }}"
                  data:
                    swing_mode: >
                      {% if desired_swing in available_swings %}
                        {{ desired_swing }}
                      {% elif desired_swing == 'both' and '3D' in available_swings %}
                        3D
                      {% elif desired_swing == 'both' and 'Both' in available_swings %}
                        Both
                      {% elif desired_swing == 'vertical' and 'Vertical' in available_swings %}
                        Vertical
                      {% elif desired_swing == 'Vertical' and 'vertical' in available_swings %}
                        vertical
                      {% elif desired_swing == 'horizontal' and 'Horizontal' in available_swings %}
                        Horizontal
                      {% elif desired_swing == 'Horizontal' and 'horizontal' in available_swings %}
                        horizontal
                      {% elif 'both' in available_swings %}
                        both
                      {% elif 'Both' in available_swings %}
                        Both
                      {% elif '3D' in available_swings %}
                        3D
                      {% elif 'Vertical' in available_swings %}
                        Vertical
                      {% elif 'vertical' in available_swings %}
                        vertical
                      {% else %}
                        {{ available_swings[0] if available_swings else 'off' }}
                      {% endif %}
          
          - service: input_text.set_value
            target:
              entity_id: !input helper_last_mode
            data:
              value: "heating"
          
          # Update hysteresis tracking - we're now heating
          - condition: template
            value_template: "{{ helper_last_transition is defined and helper_last_transition != none }}"
          - service: input_text.set_value
            target:
              entity_id: "{{ helper_last_transition }}"
            data:
              value: "heating"
          
          # Update mode start time for effectiveness tracking (only if it exists)
          - condition: template
            value_template: "{{ dynamic_enabled and helper_mode_time not in [none, '', 'unavailable', 'unknown', []] and helper_mode_time is string and helper_mode_time != '' and last_mode != 'cooling' }}"
          - service: input_datetime.set_datetime
            target:
              entity_id: "{{ helper_mode_time }}"
            data:
              datetime: "{{ now() }}"
            continue_on_error: true
          
          - condition: template
            value_template: "{{ enable_notifications }}"
          
          - service: !input notification_service
            data:
              title: "{{ room_name }} Climate Control"
              message: "Medium heating mode - {{ current_temp }}°C"
      
      # High heating mode - Maximum heating when temperature drops significantly
      # Activates at heating_high_temp (e.g., 18°C) for emergency heating
      # Uses maximum fan speed for rapid temperature recovery
      - conditions:
          - condition: template
            value_template: >
              {% if last_mode == 'heating_high' %}
                {# Already heating high - continue until target reached #}
                {{ current_temp < target_temp }}
              {% else %}
                {# Not heating high - start if high threshold exceeded #}
                {{ should_activate and current_temp <= heating_high_temp and (time_since_change > runtime_min or last_mode != 'heating_high') }}
              {% endif %}
        sequence:
          # Debug logging at exact condition evaluation time
          - condition: template
            value_template: "{{ debug_enabled }}"
          - service: system_log.write
            data:
              message: >
                🔍 {{ room_name | upper }} DEBUG: HIGH HEATING CONDITION EVALUATION
                📊 At Exact Condition Time (condition was TRUE):
                    • last_mode: "{{ last_mode }}"
                    • should_activate: {{ should_activate }}
                    • current_temp: {{ current_temp }}°C
                    • heating_high_temp: {{ heating_high_temp }}°C
                    • time_since_change: {{ time_since_change | round(1) }}min
                    • runtime_min: {{ runtime_min }}min
                    • temp_check: {{ current_temp <= heating_high_temp }}
                    • time_check: {{ time_since_change > runtime_min }}
                    • mode_check: {{ last_mode != 'heating_high' }}
                    • FULL_CONDITION: {{ should_activate and current_temp <= heating_high_temp and (time_since_change > runtime_min or last_mode != 'heating_high') }}
              level: warning
              logger: "blueprints.climate_control.{{ room_name | lower | replace(' ', '_') }}"

          - service: system_log.write
            data:
              message: >
                {% set current_mode = states(helper_mode) | default('off') %}
                🔥 {{ room_name | upper }} HIGH HEATING ACTIVATED
                {% if current_mode == 'heating_high' %}
                📊 CONTINUE MODE: Already heating high, continuing until target
                • Current: {{ current_temp }}°C → Target: {{ target_temp }}°C ({{ (target_temp - current_temp) | round(1) }}°C to go)
                {% else %}
                🚀 START MODE: Starting HIGH heating
                • Trigger: {{ current_temp }}°C ≤ {{ heating_high_temp }}°C (threshold)
                • Target: {{ target_temp }}°C ({{ (target_temp - current_temp) | round(1) }}°C to heat)
                {% endif %}
                • Last Mode: {{ current_mode }}
              level: warning
              logger: "blueprints.climate_control.{{ room_name | lower | replace(' ', '_') }}"
          - condition: template
            value_template: "{{ debug_enabled }}"
          - service: system_log.write
            data:
              message: >
                🔍 {{ room_name | upper }} CLIMATE DEBUG: HIGH HEATING MODE
                Temperature: {{ current_temp }}°C < {{ heating_high_temp }}°C
                Target: {{ heating_target_temp }}°C
                Fan: Maximum (emergency)
              level: warning
              logger: "blueprints.climate_control.{{ room_name | lower | replace(' ', '_') }}"
          
          - service: climate.set_hvac_mode
            target:
              entity_id: !input climate_entities
            data:
              hvac_mode: >
                {% if current_temp > comfort_max_temp %}
                  cool
                {% elif current_temp < comfort_min_temp %}
                  heat
                {% else %}
                  heat_cool
                {% endif %}
          
          - service: climate.set_temperature
            target:
              entity_id: !input climate_entities
            data:
              temperature: "{{ heating_target_temp }}"

          - service: system_log.write
            data:
              message: >
                🌡️ {{ room_name | upper }} TEMP SET: High Heating
                Set AC to: {{ heating_target_temp | round(0) }}°C (rounded for AC compatibility)
                Actual value: {{ heating_target_temp | round(1) }}°C
                Reason: Direct target temperature (emergency heating mode)
                Current: {{ current_temp | round(1) }}°C → Target: {{ heating_target_temp | round(1) }}°C
              level: warning
              logger: "blueprints.climate_control.{{ room_name | lower | replace(' ', '_') }}"
          
          # Set max fan mode for emergency heating
          - repeat:
              for_each: "{{ climate_list }}"
              sequence:
                - variables:
                    current_entity: "{{ repeat.item }}"
                    available_fans: "{{ state_attr(repeat.item, 'fan_modes') | list }}"
                    desired_max_fan: !input fan_speed_max
                - service: climate.set_fan_mode
                  target:
                    entity_id: "{{ current_entity }}"
                  data:
                    fan_mode: >
                      {% if desired_max_fan in available_fans %}
                        {{ desired_max_fan }}
                      {% elif '5' in available_fans %}
                        5
                      {% elif 'Level 5' in available_fans %}
                        Level 5
                      {% elif 'high' in available_fans %}
                        high
                      {% elif 'High' in available_fans %}
                        High
                      {% elif 'turbo' in available_fans %}
                        turbo
                      {% elif 'Turbo' in available_fans %}
                        Turbo
                      {% elif 'max' in available_fans %}
                        max
                      {% elif 'Max' in available_fans %}
                        Max
                      {% elif '4' in available_fans %}
                        4
                      {% elif 'Level 4' in available_fans %}
                        Level 4
                      {% elif 'Auto' in available_fans %}
                        Auto
                      {% elif 'auto' in available_fans %}
                        auto
                      {% else %}
                        {{ available_fans[-1] if available_fans else 'Auto' }}
                      {% endif %}
                
                - condition: template
                  value_template: "{{ debug_enabled }}"
                - service: system_log.write
                  data:
                    message: >
                      🔍 SMART HIGH HEAT FAN: {{ repeat.item }}
                      • Available: {{ available_fans | join(', ') }}
                      • Selected: {{ (desired_max_fan if desired_max_fan in available_fans else ('5' if '5' in available_fans else ('high' if 'high' in available_fans else available_fans[-1]))) }}
                      • Reason: Maximum fan speed for emergency heating
                    level: debug
                    logger: "blueprints.climate_control.{{ room_name | lower | replace(' ', '_') }}"
          
          # Set swing mode with compatibility check
          - repeat:
              for_each: "{{ climate_list }}"
              sequence:
                - variables:
                    current_entity: "{{ repeat.item }}"
                    available_swings: "{{ state_attr(repeat.item, 'swing_modes') | list }}"
                    desired_swing: !input swing_mode_active
                - service: climate.set_swing_mode
                  target:
                    entity_id: "{{ current_entity }}"
                  data:
                    swing_mode: >
                      {% if desired_swing in available_swings %}
                        {{ desired_swing }}
                      {% elif desired_swing == 'both' and '3D' in available_swings %}
                        3D
                      {% elif desired_swing == 'both' and 'Both' in available_swings %}
                        Both
                      {% elif desired_swing == 'vertical' and 'Vertical' in available_swings %}
                        Vertical
                      {% elif desired_swing == 'Vertical' and 'vertical' in available_swings %}
                        vertical
                      {% elif desired_swing == 'horizontal' and 'Horizontal' in available_swings %}
                        Horizontal
                      {% elif desired_swing == 'Horizontal' and 'horizontal' in available_swings %}
                        horizontal
                      {% elif 'both' in available_swings %}
                        both
                      {% elif 'Both' in available_swings %}
                        Both
                      {% elif '3D' in available_swings %}
                        3D
                      {% elif 'Vertical' in available_swings %}
                        Vertical
                      {% elif 'vertical' in available_swings %}
                        vertical
                      {% else %}
                        {{ available_swings[0] if available_swings else 'off' }}
                      {% endif %}
          
          - service: input_text.set_value
            target:
              entity_id: !input helper_last_mode
            data:
              value: "heating"
          
          # Update hysteresis tracking - we're now heating
          - condition: template
            value_template: "{{ helper_last_transition is defined and helper_last_transition != none }}"
          - service: input_text.set_value
            target:
              entity_id: "{{ helper_last_transition }}"
            data:
              value: "heating"
          
          # Update mode start time for effectiveness tracking (only if it exists)
          - condition: template
            value_template: "{{ dynamic_enabled and helper_mode_time not in [none, '', 'unavailable', 'unknown', []] and helper_mode_time is string and helper_mode_time != '' and last_mode != 'cooling' }}"
          - service: input_datetime.set_datetime
            target:
              entity_id: "{{ helper_mode_time }}"
            data:
              datetime: "{{ now() }}"
            continue_on_error: true
          
          - condition: template
            value_template: "{{ enable_notifications }}"
          
          - service: !input notification_service
            data:
              title: "{{ room_name }} Climate Control"
              message: "High heating mode - {{ current_temp }}°C"
      

      # FALLBACK: Dynamic Escalation - Only if no normal modes triggered
      - conditions:
          - condition: template
            value_template: "{{ should_escalate }}"
          - condition: template
            value_template: "{{ should_activate }}"
        sequence:
          - condition: template
            value_template: "{{ debug_enabled }}"
          - service: system_log.write
            data:
              message: >
                🔥 {{ room_name | upper }} SMART ESCALATION LEVEL {{ escalation_level }}/4 ACTIVATED
                Effectiveness: {{ current_effectiveness }}% • Distance: {{ distance_from_target }}°C • Runtime: {{ time_in_current_mode }}min
                Reason: {% if escalation_level == 4 %}🚨 EMERGENCY - {{ 'Far from target' if distance_from_target > 4 else 'Completely ineffective' if current_effectiveness <= 10 else 'Wrong direction trend' }}
                {% elif escalation_level == 3 %}🔥 HIGH - {{ 'Still far after 5min' if distance_from_target > 3 else 'Poor effectiveness' if current_effectiveness <= 25 else 'Progress stalled' }}
                {% elif escalation_level == 2 %}⚡ MEDIUM - {{ 'Moderate distance' if distance_from_target > 2 else 'Below average performance' }}
                {% elif escalation_level == 1 %}📈 LOW - {{ 'Minor distance issue' if distance_from_target > 1 else 'Mediocre effectiveness' }}
                {% endif %}
                Power Level: {{ 'MAXIMUM' if escalation_level == 4 else 'HIGH' if escalation_level == 3 else 'ELEVATED' if escalation_level == 2 else 'MODERATE' if escalation_level == 1 else 'NORMAL' }}
              level: warning
              logger: "blueprints.climate_control.{{ room_name | lower | replace(' ', '_') }}"
          
          # Force maximum cooling/heating mode
          - repeat:
              for_each: "{{ climate_list }}"
              sequence:
                - variables:
                    current_entity: "{{ repeat.item }}"
                    available_modes: "{{ state_attr(repeat.item, 'hvac_modes') | list }}"
                - service: climate.set_hvac_mode
                  target:
                    entity_id: "{{ current_entity }}"
                  data:
                    hvac_mode: >
                      {% if current_temp > comfort_max_temp and 'cool' in available_modes %}
                        cool
                      {% elif current_temp < comfort_min_temp and 'heat' in available_modes %}
                        heat
                      {% elif 'auto' in available_modes %}
                        auto
                      {% elif 'heat_cool' in available_modes %}
                        heat_cool
                      {% else %}
                        {{ available_modes[0] if available_modes else 'off' }}
                      {% endif %}
          
          - service: climate.set_temperature
            target:
              entity_id: !input climate_entities
            data:
              temperature: >
                {% if current_temp > comfort_max_temp %}
                  {{ cooling_target_temp }}
                {% elif current_temp < comfort_min_temp %}
                  {{ heating_target_temp }}
                {% else %}
                  {{ target_temp }}
                {% endif %}

          - service: system_log.write
            data:
              message: >
                🌡️ {{ room_name | upper }} TEMP SET: Emergency Escalation
                Set AC to: {{ (cooling_target_temp if current_temp > comfort_max_temp else heating_target_temp if current_temp < comfort_min_temp else target_temp) | round(0) }}°C (rounded for AC compatibility)
                Actual value: {{ (cooling_target_temp if current_temp > comfort_max_temp else heating_target_temp if current_temp < comfort_min_temp else target_temp) | round(1) }}°C
                Reason: {{ 'Emergency cooling (trend ineffective)' if current_temp > comfort_max_temp else 'Emergency heating (trend ineffective)' if current_temp < comfort_min_temp else 'Emergency target (stalled temperature)' }}
                Current: {{ current_temp | round(1) }}°C → Target: {{ (cooling_target_temp if current_temp > comfort_max_temp else heating_target_temp if current_temp < comfort_min_temp else target_temp) | round(1) }}°C
              level: warning
              logger: "blueprints.climate_control.{{ room_name | lower | replace(' ', '_') }}"
          
          # Set graduated fan speed based on escalation level
          - repeat:
              for_each: "{{ climate_list }}"
              sequence:
                - variables:
                    current_entity: "{{ repeat.item }}"
                    available_fans: "{{ state_attr(repeat.item, 'fan_modes') | list }}"
                    
                    # Smart escalation fan speed selection
                    selected_fan_mode: >
                      {% set fans = state_attr(repeat.item, 'fan_modes') | list %}
                      {% if escalation_level == 4 %}
                        {# Level 4: MAXIMUM power - use highest available speed #}
                        {% set desired = fan_speed_max %}
                        {% if desired in fans %}
                          {{ desired }}
                        {% elif 'turbo' in fans %}
                          turbo
                        {% elif 'Turbo' in fans %}
                          Turbo
                        {% elif '5' in fans %}
                          5
                        {% elif 'Level 5' in fans %}
                          Level 5
                        {% elif 'high' in fans %}
                          high
                        {% elif 'High' in fans %}
                          High
                        {% elif 'max' in fans %}
                          max
                        {% elif 'Max' in fans %}
                          Max
                        {% else %}
                          {{ fans[-1] if fans else 'Auto' }}
                        {% endif %}
                      {% elif escalation_level == 3 %}
                        {# Level 3: HIGH power - use high speed #}
                        {% if 'high' in fans %}
                          high
                        {% elif 'High' in fans %}
                          High
                        {% elif '4' in fans %}
                          4
                        {% elif 'Level 4' in fans %}
                          Level 4
                        {% elif '5' in fans %}
                          5
                        {% elif 'Level 5' in fans %}
                          Level 5
                        {% else %}
                          {{ fans[-1] if fans else 'Auto' }}
                        {% endif %}
                      {% elif escalation_level == 2 %}
                        {# Level 2: ELEVATED power - use medium-high speed #}
                        {% if '4' in fans %}
                          4
                        {% elif 'Level 4' in fans %}
                          Level 4
                        {% elif 'high' in fans %}
                          high
                        {% elif 'High' in fans %}
                          High
                        {% elif '3' in fans %}
                          3
                        {% elif 'Level 3' in fans %}
                          Level 3
                        {% elif 'medium' in fans %}
                          medium
                        {% elif 'Medium' in fans %}
                          Medium
                        {% else %}
                          {{ fans[-1] if fans else 'Auto' }}
                        {% endif %}
                      {% elif escalation_level == 1 %}
                        {# Level 1: MODERATE power - use medium speed #}
                        {% set desired = fan_speed_medium %}
                        {% if desired in fans %}
                          {{ desired }}
                        {% elif '3' in fans %}
                          3
                        {% elif 'Level 3' in fans %}
                          Level 3
                        {% elif 'medium' in fans %}
                          medium
                        {% elif 'Medium' in fans %}
                          Medium
                        {% elif 'auto' in fans %}
                          auto
                        {% elif 'Auto' in fans %}
                          Auto
                        {% else %}
                          {{ fans[0] if fans else 'Auto' }}
                        {% endif %}
                      {% else %}
                        Auto
                      {% endif %}
                
                - service: climate.set_fan_mode
                  target:
                    entity_id: "{{ current_entity }}"
                  data:
                    fan_mode: "{{ selected_fan_mode }}"
                
                - condition: template
                  value_template: "{{ debug_enabled }}"
                - service: system_log.write
                  data:
                    message: >
                      🔍 SMART ESCALATION FAN LEVEL {{ escalation_level }}: {{ repeat.item }}
                      • Available: {{ available_fans | join(', ') }}
                      • Selected: {{ selected_fan_mode }}
                      • Power: {{ 'MAXIMUM' if escalation_level == 4 else 'HIGH' if escalation_level == 3 else 'ELEVATED' if escalation_level == 2 else 'MODERATE' if escalation_level == 1 else 'NORMAL' }}
                    level: debug
                    logger: "blueprints.climate_control.{{ room_name | lower | replace(' ', '_') }}"
          
          - service: input_text.set_value
            target:
              entity_id: !input helper_last_mode
            data:
              value: >
                {% if current_temp > comfort_max_temp %}
                  cooling_escalated
                {% else %}
                  heating_escalated  
                {% endif %}
          
          # Update mode start time for effectiveness tracking (only if it exists)
          - condition: template
            value_template: "{{ dynamic_enabled and helper_mode_time not in [none, '', 'unavailable', 'unknown', []] and helper_mode_time is string and helper_mode_time != '' and last_mode != 'cooling' }}"
          - service: input_datetime.set_datetime
            target:
              entity_id: "{{ helper_mode_time }}"
            data:
              datetime: "{{ now() }}"
            continue_on_error: true
    
    # DEFAULT ACTION: Turn off AC when temperature reaches target and no conditions met
    default:
      - condition: and
        conditions:
          - condition: template
            value_template: "{{ actual_ac_state == 'on' }}"
          - condition: template
            value_template: "{{ current_temp >= comfort_min_temp and current_temp <= comfort_max_temp }}"
      - service: system_log.write
        data:
          message: >
            🎯 {{ room_name | upper }} TARGET REACHED - Turning off AC
            
            ✅ Smart Hysteresis Complete:
                • Temperature: {{ current_temp }}°C reached target zone ({{ comfort_min_temp }}-{{ comfort_max_temp }}°C)
                • Previous mode: {{ last_mode }}
                • Action: Turning off AC (target achieved)
          level: warning
          logger: "blueprints.climate_control.{{ room_name | lower | replace(' ', '_') }}"
      - service: climate.turn_off
        target:
          entity_id: !input climate_entities
        continue_on_error: true
      - service: input_text.set_value
        target:
          entity_id: !input helper_last_mode
        data:
          value: "off"
      - service: input_datetime.set_datetime
        target:
          entity_id: !input helper_last_change
        data:
          datetime: "{{ now() }}"
        continue_on_error: true

  # Debug: Show which conditions were evaluated but not met
  - condition: template
    value_template: "{{ debug_enabled }}"
  - service: system_log.write
    data:
      message: >
        🔍 {{ room_name | upper }} DEBUG: Choose block completed - no conditions matched
        
        📊 Final Summary:
            • should_activate: {{ should_activate }}
            • smart_presence_active: {{ smart_presence_active }}
            • current_temp: {{ current_temp }}°C
            • Control mode: {{ control_mode }}
            • AC state: {{ actual_ac_state }}
            • Time since change: {{ time_since_change | round(1) }}min
            • Room presence: {{ room_presence_detected }}
            • smart_behavior: {{ smart_behavior }}
            • extreme_temp_detected: {{ extreme_temp_detected }}
        
        🌡️ Thresholds that were checked:
            • Cooling LOW: {{ current_temp }} >= {{ cooling_low_temp }} AND < {{ cooling_medium_temp }} = {{ current_temp >= cooling_low_temp and current_temp < cooling_medium_temp }}
            • Cooling MEDIUM: {{ current_temp }} >= {{ cooling_medium_temp }} AND < {{ cooling_high_temp }} = {{ current_temp >= cooling_medium_temp and current_temp < cooling_high_temp }}
            • Cooling HIGH: {{ current_temp }} >= {{ cooling_high_temp }} = {{ current_temp >= cooling_high_temp }}
            • Heating LOW: {{ current_temp }} <= {{ heating_low_temp }} AND > {{ heating_medium_temp }} = {{ current_temp <= heating_low_temp and current_temp > heating_medium_temp }}
            • Heating MEDIUM: {{ current_temp }} <= {{ heating_medium_temp }} AND > {{ heating_high_temp }} = {{ current_temp <= heating_medium_temp and current_temp > heating_high_temp }}
            • Heating HIGH: {{ current_temp }} <= {{ heating_high_temp }} = {{ current_temp <= heating_high_temp }}
            • Escalation: {{ should_escalate }}
      level: warning
      logger: "blueprints.climate_control.{{ room_name | lower | replace(' ', '_') }}"

  # Adaptive Control Status Debug Log  
  - service: system_log.write
    data:
      message: >
        🔍 {{ room_name | upper }} ADAPTIVE CONTROL: Status Check
        
        📊 Current Status:
            • Adaptive Enabled: {{ adaptive_enabled }}
            • Control Mode: {{ control_mode }}
            • Room Presence: {{ room_presence_detected }}
            • Last Presence: {{ states(presence_detected_helper) if presence_detected_helper else 'N/A' }}
            • Required Delay: {{ adaptive_occupied_delay }} min
        {% if presence_detected_helper and states(presence_detected_helper) not in ['unknown', 'unavailable', ''] %}
        {% set presence_time = (as_timestamp(now()) - as_timestamp(states(presence_detected_helper))) / 60 %}
            • Time Present: {{ presence_time | round(1) }} min
            • Ready to Switch: {{ presence_time >= adaptive_occupied_delay }}
        {% endif %}
            • Override Timeout: {{ adaptive_override_timeout }}h
        {% if adaptive_override_helper and adaptive_override_helper not in ['', 'unknown', 'unavailable'] %}
            • Override Helper: {{ states(adaptive_override_helper) }}
        {% endif %}
            • DEBUG: manual_change_detected={{ manual_change_detected }}, helper_mode={{ states(helper_mode) | default('off') }}, actual_ac={{ actual_ac_state }}, time_since_change={{ ((as_timestamp(now()) - as_timestamp(states(helper_change))) / 60) | round(1) if states(helper_change) not in ['unknown', 'unavailable', ''] else 'N/A' }}min
        {% if adaptive_override_timeout > 0 %}
        {% set last_change = states(helper_change) %}
        {% if last_change not in ['unknown', 'unavailable', ''] %}
        {% set time_since_change = (as_timestamp(now()) - as_timestamp(last_change)) / 3600 %}
            • Time Since Manual Override: {{ time_since_change | round(1) }}h ({{ 'normal automatic operation' if time_since_change >= 24 else 'recent manual change' }})
            • Override Active: {{ time_since_change < adaptive_override_timeout }}
        {% endif %}
        {% endif %}
      level: warning
      logger: "blueprints.climate_control.{{ room_name | lower | replace(' ', '_') }}"

  # Debug: No conditions matched
  - if:
      - condition: template
        value_template: "{{ debug_enabled }}"
    then:
      - service: system_log.write
        data:
          message: >
            ✅ {{ room_name | upper }} CLIMATE CONTROL: All Good
            
            {% if current_temp >= comfort_min_temp and current_temp <= comfort_max_temp %}🎯 Status: Temperature {{ current_temp }}°C is perfect - no changes needed{% else %}⏸️ Status: No action required right now{% endif %}
            
            📋 Technical Details:
                • Temperature: {{ current_temp }}°C
                • Occupancy: {{ 'Someone home' if anyone_home else 'Nobody home' }}
                • Current Mode: {{ last_mode }} mode
          level: warning
          logger: "blueprints.climate_control.{{ room_name | lower | replace(' ', '_') }}"

mode: restart